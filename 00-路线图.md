# 00 · 路线图

本章确定边界：推理引擎是什么、不是什么、与相邻系统的接口在哪里，以及后续章节要解决的问题清单。

---

## 1. 推理栈的分层

一次大模型调用从上到下经过五层。不同岗位的工作落在不同层，招聘描述中的「推理」可能指其中任何一层。

| 层 | 内容 | 代表系统 |
|---|---|---|
| 应用层 | 提示词构造、Agent 编排、RAG、工具调用 | LangChain、自研 Agent 后端 |
| 网关层 | 鉴权、限流、计费、模型路由、多租户隔离 | LiteLLM、自研网关 |
| 服务层 | 多实例管理、负载均衡、弹性扩缩、健康检查、灰度 | Kubernetes + 自研控制面、KServe、Ray Serve |
| 引擎层 | 请求调度、KV cache 管理、批处理、采样、模型执行 | vLLM、SGLang、TensorRT-LLM |
| 算子层 | attention、GEMM、归一化、量化 kernel | FlashAttention、CUTLASS、cuBLAS、Triton |

本教材的主线是引擎层（第 05-14 章），向下延伸到算子层（第 11 章），向上延伸到服务层（第 16 章）。应用层与网关层不覆盖。

### 1.1 层与层之间的接口

| 接口 | 形式 | 涉及章节 |
|---|---|---|
| 网关 → 服务 | HTTP，OpenAI 兼容格式（`/v1/chat/completions`） | 01、16 |
| 服务 → 引擎 | HTTP 或 gRPC，同上格式；或引擎的原生 Python API | 01、16 |
| 引擎 → 算子 | PyTorch 算子调用、自定义 CUDA kernel、Triton kernel | 11 |
| 引擎 → 引擎 | KV cache 传输（PD 分离场景） | 10 |

理解每一层能对上一层隐藏什么，是判断问题归属的基础。举例：TTFT 变高可能来自网关排队、服务层路由不均、引擎调度队列积压、prefill 计算本身，四层的排查手段完全不同。第 19 章给出分层定位方法。

---

## 2. 推理与训练的差异

推理不是训练的简化版。两者的约束不同，导致系统设计的取舍相反。

| 项 | 训练 | 推理 |
|---|---|---|
| 输入 | 批量数据，长度可对齐 | 在线请求，长度不可预知，到达时刻随机 |
| 计算形态 | 前向 + 反向 | 只有前向，且分为两个特性不同的阶段 |
| 主要瓶颈 | 算力与集合通信 | 显存容量与显存带宽 |
| 优化目标 | 单位时间处理的样本数 | 在延迟约束下的吞吐 |
| 状态 | 优化器状态、梯度、激活 | KV cache |
| 失败处理 | checkpoint 重启，分钟级可接受 | 请求级重试，秒级不可接受 |
| 资源弹性 | 作业固定规模运行数小时至数天 | 负载随时间波动，需要分钟级扩缩 |

对系统方向的读者，最重要的差别是：**推理引擎是一个在线服务，同时又是一个内存分配器与调度器**。它的核心难题在显存这一有限资源上的分配、回收与超售，这与操作系统的内存管理和集群调度是同一类问题。第 06、08、09 章围绕这一点展开。

---

## 3. 关键量级

后续章节的推导反复用到这几个数字。先建立量级概念，具体数值以厂商文档为准。

### 3.1 硬件

| 项 | A100 80GB | H100 SXM | 说明 |
|---|---|---|---|
| 显存容量 | 80 GB | 80 GB | HBM |
| 显存带宽 | 约 2.0 TB/s | 约 3.35 TB/s | 决定 decode 速度 |
| FP16 算力（稠密） | 约 312 TFLOPS | 约 990 TFLOPS | 决定 prefill 速度 |
| FP8 算力 | 不支持 | 约 1979 TFLOPS | |
| 卡间互联 | NVLink 约 600 GB/s | NVLink 约 900 GB/s | 张量并行的通信通道 |
| PCIe | Gen4 约 64 GB/s | Gen5 约 128 GB/s | 主机内存与显存之间 |

算力与带宽的比值（每字节可执行的浮点运算次数）是 roofline 分析的核心参数。A100 约为 312e12 / 2.0e12 = 156，H100 约为 990e12 / 3.35e12 = 295。这个比值越高，访存受限的算子越吃亏。第 02 章展开。

### 3.2 模型

以常见的开源模型为例，用于估算显存占用。

| 模型 | 参数量 | 层数 | 隐藏维度 | 注意力头 | KV 头 | fp16 权重 |
|---|---|---|---|---|---|---|
| Llama-3-8B | 8.0 B | 32 | 4096 | 32 | 8 | 16 GB |
| Qwen2.5-7B | 7.6 B | 28 | 3584 | 28 | 4 | 15 GB |
| Llama-2-13B | 13 B | 40 | 5120 | 40 | 40 | 26 GB |
| Qwen2.5-72B | 72 B | 80 | 8192 | 64 | 8 | 145 GB |

fp16 权重的字节数约为参数量的 2 倍。这是显存预算中最先扣除的部分，剩余部分才用于 KV cache 与运行时开销。

### 3.3 时间

| 事件 | 量级 |
|---|---|
| 一次 kernel 启动 | 5-10 微秒 |
| 一层 decode 的计算 | 数十微秒 |
| 一步 decode（全部层） | 5-30 毫秒 |
| 一次 prefill（2K token，7B 模型） | 50-200 毫秒 |
| 一次 all-reduce（单机 8 卡，小张量） | 10-50 微秒 |
| Python 侧的调度与准备开销 | 每步 1-10 毫秒 |

最后一行是引擎设计中容易忽略的部分。当一步 decode 只需 10 毫秒时，Python 层的调度逻辑本身就成为瓶颈，这是 vLLM V1 架构重构的动因之一（第 17 章）。

---

## 4. 术语表

按首次出现的章节排列。面试中这些词会被直接使用，不解释。

| 术语 | 含义 | 章 |
|---|---|---|
| prefill | 处理输入 prompt、生成第一个 token 的阶段 | 04 |
| decode | 逐个生成后续 token 的阶段 | 04 |
| TTFT | time to first token，首 token 延迟 | 01 |
| TPOT / ITL | time per output token / inter-token latency，每个输出 token 的间隔 | 01 |
| goodput | 满足延迟约束的有效吞吐 | 19 |
| KV cache | 缓存的历史 key 与 value 张量 | 05 |
| MHA / MQA / GQA / MLA | 四种注意力的 KV 组织方式 | 05 |
| block / page | KV cache 的固定大小分配单位 | 06 |
| block table | 逻辑 token 位置到物理 block 的映射表 | 06 |
| prefix caching | 跨请求复用相同前缀的 KV | 07 |
| RadixAttention | SGLang 用基数树管理前缀缓存的方案 | 07 |
| continuous batching | 每步迭代重新组织 batch 的调度方式 | 08 |
| iteration-level scheduling | 同上，Orca 论文的原始表述 | 08 |
| preemption | 显存不足时中止已运行请求 | 09 |
| swap / recompute | 抢占后保存 KV 或丢弃 KV 的两种处理 | 09 |
| chunked prefill | 把长 prompt 的 prefill 切分到多步执行 | 10 |
| PD 分离 | prefill 与 decode 由不同实例承担 | 10 |
| FlashAttention | 分块计算 attention、不实体化注意力矩阵的算法 | 11 |
| CUDA Graph | 把 kernel 序列录制为图以减少启动开销 | 11 |
| logits | 模型输出的、softmax 之前的词表分数 | 12 |
| speculative decoding | 先低成本生成候选 token 再批量验证 | 14 |
| TP / PP / EP | 张量并行 / 流水并行 / 专家并行 | 15 |

---

## 5. 后续章节要回答的问题

阅读时带着这些问题，读完每章应当能独立回答。第 20 章给出完整清单与回答边界。

| 章 | 问题 |
|---|---|
| 01 | 一个请求从进入到返回，经过哪些队列，在哪些位置可能排队？ |
| 02 | 为什么 decode 阶段增大 batch 几乎不增加单步耗时？ |
| 03 | 7B 模型一步 decode 需要读多少字节、算多少次浮点运算？ |
| 04 | prefill 与 decode 的算术强度相差多少倍？ |
| 05 | GQA 把 KV cache 降到 MHA 的几分之一？代价是什么？ |
| 06 | 没有分页的实现，显存利用率为什么只有 20%-40%？ |
| 07 | 前缀缓存的命中会不会影响输出结果？ |
| 08 | 调度器每一步要做哪些决策？决策的输入是什么？ |
| 09 | 换出与重算如何选择？分界点在哪里？ |
| 10 | chunked prefill 的 chunk 大小如何影响 TTFT 与 TPOT？ |
| 11 | FlashAttention 省的是显存还是计算？ |
| 12 | 采样参数在什么位置生效？为什么它不影响 KV cache 的复用？ |
| 13 | 权重量化为什么能加速 decode 而对 prefill 收益有限？ |
| 14 | 投机解码在什么条件下会变慢？ |
| 15 | 张量并行每层引入几次通信？通信量与什么成正比？ |
| 16 | 前缀感知路由与轮询路由的差别体现在哪个指标上？ |
| 17 | vLLM 的一步迭代，从调度器到模型执行经过哪些对象？ |
| 19 | 一份可比较的压测报告必须包含哪些字段？ |

---

## 6. 本机环境

本教材的代码在以下环境验证：

```
Python 3.9.6
numpy 2.0.2
torch 2.8.0 (CPU)
macOS，无 NVIDIA GPU
```

无 GPU 的影响：

| 内容 | 本机可做 | 本机不可做 |
|---|---|---|
| 显存容量与 KV cache 计算 | 可，纯算术 | — |
| 调度器与 block manager 逻辑 | 可，用模拟器 | — |
| attention 算法正确性 | 可，用 numpy 或 CPU torch | — |
| kernel 性能对比 | — | 需要 GPU 与 nsys |
| 真实 TTFT / TPOT 曲线 | — | 需要 GPU 与真实模型 |
| 张量并行与 PD 分离 | 只能算通信量 | 需要多卡 |

因此本教材的代码定位是**验证逻辑与量级**，不是性能测量。凡是需要真机数据的结论，正文中会标注来源或标注未验证。

下一章从一个请求的完整路径开始。
