# 01 · 一个请求的生命周期

本章用一条完整路径把后续所有章节的概念串起来。读完后应当能画出请求经过的全部队列，并说明每一段延迟由什么决定。

---

## 1. 问题

面试中常见的第一个问题是「说一下一个请求进来之后发生了什么」。这个问题考察的不是流程背诵，而是能否指出**请求在哪些位置会等待**。延迟问题的定位全部依赖这张图。

一个 2000 token 输入、生成 500 token 输出的请求，端到端耗时可能是 8 秒。这 8 秒中真正用于计算的部分是多少，其余部分在哪里，是本章要回答的。

---

## 2. 完整路径

以 vLLM 的 OpenAI 兼容服务为例。其他引擎的模块命名不同，阶段划分一致。

```
HTTP 请求
   │
   ├─ 1. 反序列化与参数校验
   ├─ 2. 聊天模板渲染（messages → 单一字符串）
   ├─ 3. tokenize（字符串 → token id 序列）
   │
   ├─ 4. 构造请求对象，放入 waiting 队列          ← 排队点 A
   │
   │  ┌──── 调度循环，每步执行一次 ────┐
   │  │                                  │
   ├─ 5. 调度器决定本步的 batch 组成      ← 排队点 B（显存不足）
   ├─ 6. block manager 分配 KV block      ← 排队点 C（block 耗尽）
   ├─ 7. 准备输入张量（拼接、位置编码、block table）
   ├─ 8. 模型前向                          ← 计算
   ├─ 9. 采样，得到每个序列的下一个 token
   ├─ 10. 更新序列状态，检查结束条件
   │  │                                  │
   │  └──────────────────────────────────┘
   │
   ├─ 11. detokenize（token id → 文本片段）
   ├─ 12. 流式返回一个 chunk
   │
   └─ 13. 请求结束，释放全部 block         ← 资源回收
```

第 5-10 步构成一次迭代。首次迭代对该请求执行 prefill，之后每次迭代生成一个 token。

### 2.1 逐步说明

**1. 反序列化与参数校验**

解析 JSON，校验 `model`、`max_tokens`、`temperature`、`stop` 等字段。这一步的耗时通常在微秒级，但当请求体包含很长的 prompt（例如 100 KB 文本）时，JSON 解析本身可达毫秒级。高并发下这部分占用 API 进程的 CPU，与调度循环争抢，是 vLLM 把 API 进程与引擎进程分离的原因之一。

**2. 聊天模板渲染**

`messages` 数组按模型的 chat template（Jinja2 模板）渲染为单一字符串，插入 `<|im_start|>` 一类的特殊标记。模板由模型的 tokenizer 配置携带。模板不一致会导致输出质量下降，且不报错，是部署阶段的常见问题。

**3. tokenize**

字符串转为 token id 序列。BPE 分词的耗时与文本长度成正比，一个 2000 token 的 prompt 约需 1-5 毫秒（CPU）。这一步在 CPU 上串行执行，高并发时是可观测的开销，因此引擎通常把它放在独立进程或线程池。

**4. 进入 waiting 队列**

请求变为引擎内部的对象，携带 token id 序列、采样参数、请求 id。此时它还没有占用任何显存。

**排队点 A**：当运行中的请求已经占满显存，新请求在此等待。这是 TTFT 的主要构成部分之一。队列长度是引擎最重要的可观测指标，第 19 章展开。

**5. 调度器决策**

调度器在每步迭代开始时决定：哪些等待中的请求可以进入运行状态，哪些运行中的请求需要被抢占。决策依据是剩余的空闲 block 数量、每个请求的预估需求、调度策略配置。第 08 章展开。

**排队点 B**：调度器判断显存不足，请求继续留在 waiting 队列。与排队点 A 是同一个队列，区别在于 A 是刚到达，B 是被评估后退回。

**6. 分配 KV block**

被选中的请求需要为它的 token 分配 KV cache 空间。prefill 阶段一次分配 `ceil(prompt_len / block_size)` 个 block；decode 阶段每写满一个 block 才需要新增一个。若启用前缀缓存，命中的部分不需要新分配，直接引用已有 block（第 07 章）。

**排队点 C**：block 池耗尽。此时调度器要么让请求等待，要么抢占其他请求腾出 block（第 09 章）。

**7. 准备输入张量**

把 batch 内所有序列的 token 拼成一维张量，同时构造：

| 张量 | 内容 |
|---|---|
| `input_ids` | 本步要处理的全部 token，prefill 请求贡献多个，decode 请求各贡献 1 个 |
| `positions` | 每个 token 在其序列中的位置，用于旋转位置编码 |
| `slot_mapping` | 每个 token 的 KV 要写入的物理槽位编号 |
| `block_tables` | 每个序列的逻辑块到物理块的映射 |
| `seq_lens` | 每个序列的当前长度，attention kernel 用它确定读取范围 |

这一步在 CPU 上执行并拷贝到 GPU。当 batch 很大时，Python 层构造这些张量的耗时可达毫秒级，与一步 decode 的耗时同量级。消除这部分开销的手段包括增量更新（只更新变化的部分）、把构造过程下沉到 C++、用 CUDA Graph 固定执行路径。

**8. 模型前向**

一次前向处理整个 batch。prefill 与 decode 的请求可以混在同一批（第 10 章的 chunked prefill），也可以分开。输出是每个序列最后一个位置的 logits，形状为 `[序列数, 词表大小]`。词表大小通常在 3 万到 15 万之间，因此这个张量本身可达数十 MB。

**9. 采样**

对 logits 依次应用 logits 处理器（重复惩罚、logit bias、结构化输出的掩码）、温度缩放、top-k / top-p 截断，然后按概率采样。同一个 batch 内不同请求的采样参数不同，因此这一步需要按请求分组处理，是 GPU 上一个形状不规则的操作。第 12 章展开。

**10. 更新状态与结束判定**

新 token 追加到序列。结束条件：

| 条件 | 说明 |
|---|---|
| 生成了 EOS token | 模型自行判定结束 |
| 达到 `max_tokens` | 请求指定的上限 |
| 命中 stop 字符串 | 需要在文本层判断，因此依赖 detokenize 的结果 |
| 客户端断开 | 引擎需要主动检测并中止，否则浪费算力 |

stop 字符串的判断有一个细节：token 与字符不是一一对应，一个 stop 字符串可能跨越两个 token，因此必须在增量解码出的文本上做匹配，并处理已经多输出了几个字符的情况（需要回退截断）。

**11. detokenize**

token id 转回文本。增量解码需要保留分词器的状态，因为一个 UTF-8 字符可能由多个 token 编码（尤其是中文与 emoji），单独解码某个 token 会得到替换字符。引擎的做法是维护每个序列已解码的前缀，每次解码时带上少量历史 token 再取差值。

**12. 流式返回**

按 SSE（server-sent events）格式发送增量。若客户端未启用流式，则累积到结束后一次返回。

**13. 释放**

请求结束后，其占用的全部 block 归还池中。若启用前缀缓存，这些 block 不会立即清零，而是进入可复用状态，等待被后续请求命中或被淘汰（第 07 章）。

---

## 3. 延迟的构成

把上述路径按时间归类，一个请求的端到端延迟为：

```
端到端延迟 = TTFT + TPOT × (输出 token 数 - 1)

TTFT = 前处理时间 + 排队时间 + prefill 计算时间 + 首次采样与解码时间
TPOT = 单步迭代时间（受 batch 内其他请求影响）
```

各部分的量级（7B 模型，单卡，2000 token 输入）：

| 组成 | 量级 | 主要影响因素 |
|---|---|---|
| 前处理（1-3 步） | 1-5 ms | prompt 长度、CPU 争抢 |
| 排队（4-6 步） | 0 到数秒 | 并发数、显存余量、队列策略 |
| prefill 计算 | 50-200 ms | prompt 长度、是否命中前缀缓存、是否 chunked |
| 单步 decode | 5-30 ms | batch 大小、序列长度、显存带宽 |
| 后处理（11-12 步） | 0.1-1 ms | 词表大小、是否有结构化约束 |

结论：**低负载时 TTFT 由 prefill 计算决定，高负载时由排队决定**。这两种情况的优化手段完全不同。前者靠前缀缓存、chunked prefill、更快的 kernel；后者靠扩容、调度策略、准入控制。第 19 章给出区分方法：观察队列长度指标，队列长度接近 0 说明处于前一种情况。

---

## 4. 三个容易被忽略的位置

### 4.1 batch 内的相互影响

decode 的单步耗时由整个 batch 决定，不由单个请求决定。一个请求的 TPOT 会因为同批中加入了一个长 prompt 的 prefill 而突然变大。这是 TPOT 出现毛刺的主要原因，也是 chunked prefill 与 PD 分离要解决的问题（第 10 章）。

因此单请求压测得到的延迟数据没有参考价值，必须在目标并发下测量。

### 4.2 CPU 与 GPU 的重叠

第 7 步（准备输入）与第 11 步（detokenize）在 CPU 上执行，第 8 步在 GPU 上。若串行执行，GPU 会在 CPU 工作期间空闲。引擎的做法是把 CPU 工作与上一步的 GPU 执行重叠：在 GPU 执行第 N 步时，CPU 已经在准备第 N+1 步的输入。

这一优化要求调度决策不依赖第 N 步的输出。实际上采样结果会影响哪些序列结束，因此需要用推测的方式先准备，事后修正。vLLM V1 的异步调度即为此设计。

### 4.3 请求的取消

客户端断开后若引擎不感知，该请求会继续生成到 `max_tokens`，占用显存与算力。在 Agent 场景中用户频繁中断，这部分浪费可观。引擎需要在每步迭代检查连接状态，并在检测到断开时立即释放 block。

---

## 5. 引擎实现对照

| 项 | vLLM | SGLang | TensorRT-LLM |
|---|---|---|---|
| API 与引擎的进程关系 | V1 中分离为两个进程，通过 ZMQ 通信 | 分离，Router 与 Scheduler 独立 | 通常由 Triton 承担 API 层 |
| tokenize 位置 | API 进程 | 独立的 tokenizer 进程 | Triton 的预处理模型 |
| 调度粒度 | 每步迭代 | 每步迭代 | 每步迭代 |
| detokenize | 独立进程 | 独立进程 | 后处理模型 |

共同点是把 CPU 密集的前后处理与 GPU 执行循环隔离到不同进程，避免 Python GIL 影响调度循环的节奏。

---

## 6. 代码：生命周期追踪

`code/ch01_lifecycle.py` 实现了一个简化的引擎循环，包含 waiting 队列、block 池、迭代调度与结束判定，并打印每个请求在各阶段的时间线。它不做真实计算，用参数化的耗时模型代替，目的是把排队点与延迟构成可视化。

运行：

```bash
python3 code/ch01_lifecycle.py
```

输出包含：

- 每个请求的到达时刻、开始 prefill 时刻、首 token 时刻、结束时刻
- 每个请求的 TTFT 分解（排队 vs 计算）
- 每步迭代的 batch 组成与 block 占用

默认配置（到达率 12 请求/秒，block 池 600 个 block，即 9600 token 的 KV 空间）的运行结果：

| 指标 | 数值 |
|---|---|
| TTFT 平均 / P50 / P99 | 3.349 / 3.038 / 9.566 s |
| 排队在 TTFT 中的占比 | 97.1% |
| 抢占次数 | 14 |
| 输出吞吐 | 632.8 token/s |

三组对照：

| 配置 | P50 TTFT | 排队占比 | 抢占次数 | 输出吞吐 |
|---|---|---|---|---|
| 默认 | 3.038 s | 97.1% | 14 | 632.8 token/s |
| 到达率降到 2 | 0.059 s | 80.9% | 12 | 482.1 token/s |
| block 池提到 3000 | 0.076 s | 78.4% | 0 | 1202.3 token/s |
| 两项同时放宽 | 0.052 s | 14.1% | 0 | 547.3 token/s |

其中第二行值得注意：只降低到达率，P50 TTFT 改善了 50 倍，但抢占仍然发生，排队占比仍高，因为 P99 由少数被抢占后重新排队的请求主导。只有同时放宽显存约束，才进入「TTFT 由 prefill 计算决定」的状态。

**这说明单看平均值会掩盖问题**。第 19 章会展开：延迟指标必须报告分位数，且要与队列长度、抢占次数一起看。

模拟器中还实现了一个最简单的抢占：当运行中的序列需要新 block 而池已空时，中止最晚加入的请求，丢弃其 KV，退回队列重新 prefill。没有这个机制时循环会死锁——运行中的请求无法前进，等待中的请求无法进入。这解释了为什么抢占不是可选优化，而是正确性要求。第 09 章展开策略选择。

后续章节会把这个模拟器中被简化的部分逐个替换为真实机制：第 06 章替换 block 分配，第 07 章加入前缀缓存，第 08 章替换调度策略，第 09 章加入抢占。
