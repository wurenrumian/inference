# 02 · 性能模型

推理优化的每一个手段，最终都归结为改变三个量之一：计算量、访存量、并行度。本章建立一套可以手算的模型，用来判断一项优化是否值得做，以及能带来多少收益。

---

## 1. 问题

面试中常见的追问是「你说这样能快，快多少？」。没有性能模型就只能给出定性回答。本章要建立的能力是：给定模型规模、硬件参数、batch 大小与序列长度，估算出单步耗时的量级，并指出瓶颈在哪一侧。

估算的精度目标是量级正确（误差 2 倍以内），不追求准确。真实系统的误差来源包括 kernel 效率、并行度不足、启动开销、通信，第 6 节列出。

---

## 2. 从一个能手算的例子开始

本节的全部结论都从一个 32 个参数的线性层推出来。数字很小，可以逐步验证，之后再放大到真实规模。

### 2.1 数清楚一次矩阵乘做了多少次运算

一个线性层：输入 4 维，输出 8 维。权重矩阵 `W` 的形状是 `[4, 8]`，共 **32 个参数**。

输入一个 token，它的向量是 `x = [x0, x1, x2, x3]`。输出的第 1 个元素这样算：

```
y0 = x0·W[0][0] + x1·W[1][0] + x2·W[2][0] + x3·W[3][0]
```

数一下：4 次乘法，3 次加法。工程上统一按「4 次乘加」计，即 **4 次乘法加 4 次加法，共 8 次浮点运算**（最后一次加法实际不存在，但在大矩阵里这个差别可以忽略）。

输出有 8 个元素，每个都这样算一遍：

```
总运算次数 = 8 个输出元素 × 4 次乘加 × 2 = 64 次浮点运算
```

再看这个数和参数量的关系：

```
64 = 2 × 32 = 2 × 参数量
```

**这不是巧合。** 每一个权重 `W[i][j]` 在计算中恰好被用到一次：它与 `x[i]` 相乘，结果累加到 `y[j]` 上。一次乘法加一次加法，就是 2 次运算。32 个参数各用一次，就是 64 次运算。

推广到任意形状：矩阵乘 `[m, k] × [k, n]` 的计算量是 `2 × m × k × n`。其中 `k × n` 是权重的参数量，`m` 是 token 数。

### 2.2 处理多个 token 会怎样

输入变成 2 个 token（`x` 和 `x'`），权重不变：

```
每个 token 各要 64 次运算
总计 128 次运算 = 2 × 32 参数 × 2 个 token
```

所以：

```
计算量 = 2 × 参数量 × token 数
```

放大到真实模型。Llama-3-8B 有 80.3 亿个参数，处理一个 token：

```
2 × 8.03e9 = 1.606e10 = 16.1 GFLOPs
```

batch 32 的一步 decode（32 个序列各处理 1 个 token）：

```
16.1 GFLOPs × 32 = 514 GFLOPs = 0.514 TFLOPs
```

A100 的 fp16 峰值算力是 312 TFLOPS（每秒 312 万亿次），所以纯计算需要：

```
0.514 / 312 = 0.00165 秒 = 1.65 ms
```

这个数字后面要用。

（这个式子忽略了 attention 中与序列长度平方相关的项。序列短时可以忽略，长时不行，第 03 章给出完整拆解。）

### 2.3 数清楚要读多少字节

计算之前，权重必须从显存（HBM）读到芯片上的计算单元里。回到那个 32 参数的例子，用 fp16 存储（每个参数 2 字节）：

```
访存量 = 32 参数 × 2 字节 = 64 字节
```

**关键点：处理 2 个 token 时，这 64 字节不需要读两遍。** 权重读进来之后，可以先和第一个 token 算，再和第二个 token 算，中间不需要重新读。

所以：

```
计算量 = 2 × 参数量 × token 数     ← 与 token 数成正比
权重访存量 = 参数量 × 2 字节        ← 与 token 数无关
```

这两行的差别是本章后面一切结论的来源。

放大到真实模型，Llama-3-8B 的 fp16 权重：

```
8.03e9 × 2 字节 = 16.06 GB
```

A100 的显存带宽是 2 TB/s，读完这 16.06 GB 需要：

```
16.06 / 2000 = 0.00803 秒 = 8.03 ms
```

无论 batch 是 1 还是 128，这 8.03 ms 都要花。

### 2.4 算术强度：把两者相除

```
算术强度 = 计算量 / 访存量        单位：FLOPs per byte
```

它的含义是：**从显存读进来一个字节，能拿它做多少次浮点运算。**

回到 32 参数的例子：

| token 数 | 计算量 | 访存量 | 算术强度 |
|---|---|---|---|
| 1 | 64 次 | 64 字节 | 1 |
| 2 | 128 次 | 64 字节 | 2 |
| 8 | 512 次 | 64 字节 | 8 |
| 32 | 2048 次 | 64 字节 | 32 |

**算术强度恰好等于 token 数。** 因为分子与 token 数成正比、分母与 token 数无关，相除之后就只剩 token 数。

用真实模型验证，batch 32 的一步 decode：

```
计算量 = 0.514 TFLOPs = 5.14e11
访存量 = 16.06 GB     = 1.606e10 字节
算术强度 = 5.14e11 / 1.606e10 = 32
```

同样等于 batch。这就是第 3.1 节「decode 的算术强度约等于 batch」的由来。

### 2.5 为什么算术强度决定瓶颈

一个算子的执行时间，是「算完所需时间」与「读完所需时间」中的较大者——因为读取和计算可以重叠进行，但重叠再充分，也不能比其中慢的那一项更快：

```
执行时间 = max(计算量 / 峰值算力, 访存量 / 显存带宽)
```

代入 Llama-3-8B、A100、batch 32：

```
计算需要 1.65 ms
读取需要 8.03 ms
执行时间 = max(1.65, 8.03) = 8.03 ms
```

**读取是慢的那一项，所以这一步是访存受限的。** 计算单元在其中 6.38 ms 里无事可做。

什么时候会反过来？把两项相等的条件写出来：

```
计算量 / 峰值算力 = 访存量 / 显存带宽
计算量 / 访存量   = 峰值算力 / 显存带宽
算术强度         = 峰值算力 / 显存带宽
```

右边只与硬件有关，称为**机器平衡点**：

```
A100 80GB:  312 TFLOPS / 2.0 TB/s  = 156 FLOPs/byte
H100 SXM:   990 TFLOPS / 3.35 TB/s = 296 FLOPs/byte
```

它的含义是：这块卡每从显存读一个字节，配套有能力做 156 次浮点运算。算子如果达不到这个比例，多出来的算力就浪费了。

| 关系 | 判定 | 优化方向 |
|---|---|---|
| 算术强度 < 平衡点 | 访存受限（memory-bound） | 减少访存量：量化、GQA、算子融合；或提高算术强度：增大 batch |
| 算术强度 > 平衡点 | 计算受限（compute-bound） | 减少计算量、提高 kernel 效率、用低精度乘法单元 |

decode 的算术强度等于 batch，要达到 156 需要 batch 超过 156，而 KV cache 容量通常在此之前就限制住了（第 05 章）。所以 **decode 几乎总是访存受限的**。

硬件代际更新中算力增长快于带宽增长（A100 到 H100，算力涨 3.2 倍，带宽只涨 1.7 倍），因此平衡点在右移，越来越多的算子落入访存受限区间。

### 2.6 KV cache 是另一回事

上面只算了权重。decode 时还要读 KV cache，而它的行为与权重相反：

| 访存来源 | 与 batch 的关系 | 与序列长度的关系 |
|---|---|---|
| 模型权重 | 无关（读一遍服务全部序列） | 无关 |
| KV cache | 成正比（每个序列各读各的） | 成正比 |

原因在第 03 章 2.1 节：只有 attention 是跨位置的算子，每个序列的 attention 只能读它自己的历史 KV，无法与其他序列共享。

Llama-3-8B、batch 32、序列长度 2048：

```
权重访存    = 16.06 GB
KV 访存     = 32 序列 × 2048 token × 128 KB/token = 8.59 GB
访存合计    = 24.65 GB  →  12.32 ms
```

算术强度因此从 32 降到：

```
5.14e11 / 2.465e10 = 20.8
```

**KV cache 拉低了算术强度**，且序列越长、batch 越大，拉得越低。这就是「增大 batch 的收益递减」的机制：权重那部分被摊薄了，KV 那部分没有（第 8 节的表给出完整曲线）。

### 2.7 三个必须记住的推论

到这里，前面各章的三类优化手段都可以从这个模型直接推出来：

**推论一：增大 batch 提高吞吐，几乎不增加单步耗时。** 因为权重访存量不变，单步耗时基本不变，而产出的 token 数按 batch 倍增。第 8 节的数据：batch 1 到 32，单步从 13.66 ms 到 19.61 ms（增加 44%），吞吐从 73 到 1632 token/s（提高 22 倍）。对应连续批处理（第 08 章）。

**推论二：量化直接减少访存量，在 decode 上收益接近线性。** 权重从 fp16 换成 int8，访存量减半，访存受限下的耗时也减半。对应第 13 章。

**推论三：让一次读取产出更多 token。** batch 是让一次读取服务更多**序列**，投机解码是让一次读取为同一序列产出更多**token**，两者作用在同一个瓶颈上。对应第 14 章。

反过来也成立：**任何只减少计算量而不减少访存量的优化，在 decode 阶段没有收益**，因为计算本来就不是瓶颈。这是判断一项优化值不值得做的最快方法。

---

## 3. Roofline：把第 2 节的结论画成一张图

第 2 节得到的三个式子：

```
执行时间 = max(计算量 / 峰值算力, 访存量 / 显存带宽)
算术强度 = 计算量 / 访存量
机器平衡点 = 峰值算力 / 显存带宽
```

把它们画在一张图上，横轴为算术强度，纵轴为实际能达到的算力：

```
可达算力 (FLOPS)
    ▲
    │              ┌──────────────  峰值算力（计算受限区）
    │             ╱
    │            ╱  斜线的斜率 = 显存带宽
    │           ╱   （访存受限区）
    │          ╱
    └─────────┴──────────────────────▶ 算术强度 (FLOPs/byte)
              拐点 = 峰值算力 / 带宽
```

对应的方程：

```
可达算力 = min(峰值算力, 算术强度 × 显存带宽)
```

### 3.0 怎么读这张图

两个轴的含义：

- **横轴是算子自身的属性**，与硬件无关：从显存读进来一个字节，能拿它做多少次浮点运算。
- **纵轴是实际能达到的运算速度**，不是硬件标称的峰值。

**左边为什么是一条过原点的斜线。** 若某个算子受限于访存，它的执行时间就是把数据读完的时间：

```
时间     = 访存量 / 显存带宽
可达算力 = 计算量 / 时间
        = 计算量 × 显存带宽 / 访存量
        = 算术强度 × 显存带宽
```

最后一行就是斜线的方程 `y = 带宽 × x`：过原点，斜率等于显存带宽。代入 A100（2 TB/s）：算术强度 1 对应 2 TFLOPS，算术强度 10 对应 20 TFLOPS，算术强度 100 对应 200 TFLOPS。

**右边为什么是水平线。** 算力不可能超过硬件峰值。A100 的 fp16 峰值是 312 TFLOPS，斜线涨到这个值就被截断。

**拐点在哪。** 两条线的交点：`算术强度 × 2e12 = 312e12`，解得 156。它的含义是：一个算子的算术强度必须达到 156，才能把这块卡的算力用满。

把两个阶段标上去：

| 场景 | 算术强度 | 位置 | 可达算力 | 占峰值 |
|---|---|---|---|---|
| decode，batch 1 | 1 | 斜线上 | 2 TFLOPS | 0.6% |
| decode，batch 32 | 32 | 斜线上 | 64 TFLOPS | 21% |
| decode，batch 156 | 156 | 拐点 | 312 TFLOPS | 100% |
| prefill，prompt 2048 | 1648 | 水平线上 | 312 TFLOPS | 100% |

batch 32 的 decode 只用了 21% 的算力，另外 79% 空闲。这是投机解码有空间的原因（第 14 章）。prefill 已经吃满算力，所以量化权重对它几乎没有收益——瓶颈不在那一侧。

**这张图的用法**：

1. 算出算子的算术强度，与硬件平衡点比较，确定落在哪一边
2. 按位置选优化方向：斜线上减少访存量（量化、GQA、算子融合），水平线上减少计算量或提高 kernel 效率
3. 考虑能否移动位置：增大 batch 把 decode 沿斜线往右上推，这就是连续批处理的收益——不是让单步变快，而是让同一次访存服务更多请求

这条折线给出的是**性能上界**。任何算子的实测值只能落在它下方，实测离它越远说明实现上的问题越大。第 19 章说的「实测与估算差 3 倍以上说明存在配置问题」就是这个用法。名称 roofline 来自这条折线的形状：一段斜的、一段平的。

### 3.1 应用到 decode

第 2.4 节已推出：忽略 KV cache 时，decode 的算术强度约等于 batch。在 A100 上的判定：

| batch | 算术强度 | 判定 |
|---|---|---|
| 1 | 1 | 访存受限，严重 |
| 16 | 16 | 访存受限 |
| 128 | 128 | 访存受限，接近拐点 |
| 156 | 156 | 拐点 |
| 256 | 256 | 计算受限 |

要让 decode 进入计算受限区，batch 需要超过 156。实际系统中 KV cache 容量通常在此之前就限制住了（第 05 章），因此**decode 几乎总是访存受限**。

计入 KV cache 后强度还会更低（第 2.6 节：batch 32、序列 2048 时从 32 降到 20.8），所以这个判定是偏乐观的。

### 3.2 应用到 prefill

prefill 时一个序列一次贡献它全部的 prompt token，因此「token 数」不再是 batch，而是 prompt 长度 S：

```
计算量 = 2 × 8.03e9 × S
访存量 = 1.606e10 字节（权重）+ 激活值
算术强度 ≈ S
```

同样是「计算量与 token 数成正比、权重访存与 token 数无关」这一条，只是这次 token 来自同一个序列。

S 通常在数百到数万，远超拐点 156，因此**prefill 是计算受限的**。推论：

1. prefill 阶段量化权重收益有限，因为瓶颈不在访存。
2. prefill 阶段的吞吐上限由峰值算力决定，无法通过增大 batch 提升。
3. prefill 与 decode 的最优优化手段相反，这是 PD 分离的理论依据（第 10 章）。

---

## 4. GPU 执行模型中影响估算的部分

roofline 假设硬件能达到峰值。实际达不到，主要有四个原因。

### 4.1 并行度不足

GPU 的算力来自大量并行单元（A100 有 108 个 SM）。一个 kernel 若无法产生足够多的独立工作项，部分 SM 空闲。

decode 阶段的矩阵乘形状是 `[B, k] × [k, n]`，当 B = 1 时退化为矩阵向量乘。这类形状的并行度低，实际带宽利用率通常只有峰值的 60%-80%。这是 decode 实际速度低于 roofline 估算的主要原因。

### 4.2 kernel 启动开销

每次 kernel 启动有 5-10 微秒的固定开销。一个 32 层的模型，每层若有 10 个 kernel，一步 decode 就有 320 次启动，仅启动开销就是 1.6-3.2 毫秒。当一步 decode 的计算本身只需 5 毫秒时，这部分占比可观。

对应的优化：算子融合（减少 kernel 数量）、CUDA Graph（把整个序列录制为一个图，一次提交）。第 11 章展开。

### 4.3 精度与算力的对应

峰值算力随精度变化。同一张卡上 fp16、bf16、fp8、int8 的峰值算力不同，估算时必须用对应的数值。此外，只有走 tensor core 的矩阵乘才能达到峰值；逐元素操作（激活函数、归一化、位置编码）走的是普通单元，算力低两个量级，但这些操作本身是访存受限的，因此实际影响有限。

### 4.4 非矩阵乘部分

归一化、激活、残差相加、旋转位置编码这些操作的计算量小但访存量不小。以一个 `[B, S, H]` 的激活张量为例，一次逐元素操作要读一遍写一遍，即 `2 × B × S × H × 2` 字节。这类操作在 prefill 阶段（S 大）会累积成可观的开销，是算子融合的主要目标。

---

## 5. 一个完整的估算流程

第 2 节是推导，本节是可以照着做的步骤，并补上第 2 节没算的效率修正。

给定：Llama-3-8B（fp16，32 层，8 个 KV 头，head_dim 128）、A100 80GB、batch 32、平均序列长度 2048。

**第一步：权重访存**

```
8.03e9 × 2 = 16.06 GB
16.06 GB / 2.0 TB/s = 8.03 ms
```

**第二步：KV cache 访存**

decode 时每个序列要读取自己的全部历史 KV。用第 05 章的公式：

```
每 token 每序列 = 2 × 32 × 8 × 128 × 2 字节 = 131072 字节 = 128 KB
batch 32、长度 2048 = 32 × 2048 × 128 KB = 8.59 GB
8.59 GB / 2.0 TB/s = 4.29 ms
```

**第三步：计算**

```
2 × 8.03e9 × 32 = 5.14e11 FLOPs
5.14e11 / 312e12 = 1.65 ms
```

**第四步：取较大者**

```
访存总计 = 16.06 + 8.59 = 24.65 GB → 12.32 ms
计算     = 1.65 ms
单步耗时 ≈ 12.32 ms（访存受限）
```

**第五步：修正**

乘以效率系数 0.7（并行度与启动开销），加上框架侧开销 2 ms：

```
实际单步 ≈ 12.32 / 0.7 + 2 = 19.61 ms
每序列 TPOT ≈ 19.6 ms
系统输出吞吐 ≈ 32 / 0.0196 ≈ 1632 token/s
```

这个流程的价值在于第二步：**KV cache 的访存量在长序列大 batch 时会超过权重访存量**。计算器给出的占比：

| batch \ 序列长度 | 512 | 2048 | 8192 | 32768 |
|---|---|---|---|---|
| 1 | 0% | 2% | 6% | 21% |
| 8 | 3% | 12% | 35% | 68% |
| 32 | 12% | 35% | 68% | 90% |
| 128 | 35% | 68% | 90% | 97% |

表中为 KV cache 在总访存量中的占比。在 batch 32、长度 32768 的场景下 KV 占 90%，此时权重量化几乎没有收益，而 GQA 与 KV 量化的收益接近线性。这是长上下文场景下 decode 变慢的直接原因，也是选择优化手段的依据。

---

## 6. 估算与实测的差距来源

| 来源 | 影响 | 是否可估算 |
|---|---|---|
| kernel 效率不足 | 实际达到峰值的 50%-85% | 用经验系数 |
| kernel 启动开销 | 每步 1-3 ms | 可按 kernel 数量估 |
| Python 调度开销 | 每步 1-10 ms | 与实现相关 |
| batch 内序列长度不齐 | padding 浪费或不规则访存 | 与实现相关 |
| 张量并行通信 | 每层 2 次 all-reduce | 第 15 章 |
| 显存碎片与预留 | 可用 KV 空间少于理论值 | 第 06 章 |

因此估算结果应当理解为**下界**。若实测比估算慢 3 倍以上，说明存在上表之外的问题，需要 profile 定位（第 19 章）。

---

## 7. 引擎实现对照

| 引擎 | 与性能模型相关的设计 |
|---|---|
| vLLM | 启动时按 `gpu_memory_utilization` 预分配 KV 池；用 CUDA Graph 覆盖固定 batch 分档以降低启动开销 |
| SGLang | 同样预分配；调度上更激进地追求大 batch 以提高算术强度 |
| TensorRT-LLM | 编译期为具体形状生成 kernel 并做融合，减少启动次数与非矩阵乘开销 |
| llama.cpp | 面向 batch 1 的 CPU/边缘场景，主要优化点是权重量化以降低访存量 |

四者的差异可以用本章的模型解释：都在压访存量、提算术强度、降固定开销。

---

## 8. 代码：roofline 计算器

`code/ch02_roofline.py` 实现上述估算流程。运行：

```bash
python3 code/ch02_roofline.py
```

输出包含：

- 常见硬件的机器平衡点
- 给定模型在不同 batch 下的算术强度与瓶颈判定
- decode 单步耗时随 batch 与序列长度的变化表
- 权重量化到 int8 与 fp8 后的预估收益
- 吞吐随 batch 的曲线，以及边际收益消失的位置

两组值得记住的输出。

**量化的收益上界**（Llama-3-8B，A100，batch 32，长度 2048，只计访存）：

| 配置 | 访存量 | 单步耗时 | 相对基线 |
|---|---|---|---|
| fp16 权重 / fp16 KV | 24.65 GB | 19.61 ms | 1.00x |
| int8 权重 / fp16 KV | 16.62 GB | 13.87 ms | 1.48x |
| int4 权重 / fp16 KV | 12.60 GB | 11.00 ms | 1.96x |
| fp16 权重 / fp8 KV | 20.35 GB | 16.54 ms | 1.21x |
| int8 权重 / fp8 KV | 12.32 GB | 10.80 ms | 2.00x |
| int4 权重 / fp8 KV | 8.31 GB | 7.94 ms | 2.97x |

这是上界，未计入反量化的计算开销与精度损失（第 13 章）。

**吞吐与延迟的权衡**（同上配置，扫描 batch）：

| batch | TPOT | 吞吐 | 吞吐的边际增益 |
|---|---|---|---|
| 1 | 13.66 ms | 73 token/s | — |
| 8 | 15.01 ms | 533 token/s | 1.90x |
| 32 | 19.61 ms | 1632 token/s | 1.69x |
| 64 | 25.74 ms | 2486 token/s | 1.52x |
| 128 | 38.01 ms | 3367 token/s | 1.35x |
| 256 | 62.56 ms | 4092 token/s | 1.22x |

batch 每翻倍，吞吐的增益从 1.90x 递减到 1.22x，TPOT 则持续上升。**存在一个由 SLO 决定的最优 batch**：若 TPOT 的约束是 30 ms，则 batch 上限约为 64。服务配置的本质是在给定 SLO 下求这个上限，第 19 章给出测量方法。

修改顶部的 `MODEL` 与 `DEV` 常量可以换模型与硬件。建议对比 Llama-3-8B（GQA，8 个 KV 头）与 Llama-2-13B（MHA，40 个 KV 头）在长序列下 KV 访存占比的差别，这是第 05 章的铺垫。
