# 03 · Transformer 推理的计算结构

第 02 章用「计算量约等于 2 倍参数量」做粗估。本章把它拆开，逐个算子给出计算量与访存量，用于判断优化应当施加在哪个算子上。

---

## 1. 问题

「优化 attention」是一句无法执行的话。需要先知道：在目标场景下 attention 占单步耗时的多少。同一个模型，prompt 长度 128 与 32768 时，各算子的占比相差一个量级，优化重点完全不同。

本章要建立的能力是：给定模型结构与场景，列出各算子的计算量与访存量占比，并指出前三名。

---

## 2. 一层的结构

以 Llama 系列为例（现代开源模型基本相同）。一层包含两个子块，每个子块前有归一化，后有残差相加。

```
输入 x  [B, S, H]
  │
  ├─────────────────────────┐
  │                         │
RMSNorm                     │
  │                         │
  ├─ W_q → Q  [B, S, Hq]    │      Hq = heads × head_dim
  ├─ W_k → K  [B, S, Hkv]   │      Hkv = kv_heads × head_dim
  └─ W_v → V  [B, S, Hkv]   │
  │                         │
RoPE（施加于 Q 与 K）        │
  │                         │
写入 KV cache               │
  │                         │
Attention（Q 与全部历史 KV） │
  │                         │
  W_o                       │
  │                         │
  └──────── + ──────────────┘   残差
            │
  ├─────────────────────────┐
  │                         │
RMSNorm                     │
  │                         │
  ├─ W_gate → g [B, S, I]   │      I = intermediate_size
  └─ W_up   → u [B, S, I]   │
  │                         │
SiLU(g) * u                 │
  │                         │
  W_down → [B, S, H]        │
  │                         │
  └──────── + ──────────────┘   残差
            │
          输出
```

其中 `B` 为 batch，`S` 为本步处理的 token 数（prefill 时等于 prompt 长度，decode 时为 1），`H` 为隐藏维度，`I` 为 MLP 的中间维度（通常约为 3.5 × H）。

模型的最外层还有：token embedding（查表，计算量可忽略）、最后一层的 RMSNorm、lm_head（`[H, V]` 的矩阵乘，V 为词表大小）。

### 2.1 只有 attention 是跨位置的

这是整个架构中最值得单独记住的一条性质：

| 算子 | 是否跨位置 |
|---|---|
| embedding | 否 |
| RMSNorm | 否，只在 hidden 维度上归一化 |
| W_q / W_k / W_v / W_o | 否，每个位置各自过矩阵乘 |
| **attention** | **是，全模型唯一** |
| RoPE | 否，只依赖自己的位置编号 |
| MLP（gate / up / down、SiLU） | 否 |
| 残差相加 | 否 |
| lm_head | 否 |

其余全部算子都是逐位置独立的：位置 i 的输出只依赖位置 i 的输入。位置之间的信息交换全部发生在 attention 一处。

这条性质直接推出后面几章的三个结论：

**1. KV cache 只需要存 K 和 V。** 跨位置的依赖只有 attention，而 attention 需要的历史信息只有 K 和 V。MLP 的中间结果、归一化的结果都不需要保留，因为下一个 token 的计算完全不依赖它们（第 05 章）。

**2. q 不需要缓存。** 历史 token 的 q 只在它们自己那一步用过。因果注意力下，新 token 不会去查询旧 token 的 q。所以叫 KV cache 而不是 QKV cache。

**3. attention 是唯一需要专门 kernel 的算子。** 其他算子的形状只与 hidden 维度相关，是固定的；attention 的形状随序列长度变化，且访存模式随分页而不连续。第 11 章的全部内容都围绕这一个算子。

decode 时这条性质表现为：输入 1 个 token，每层用它的 q 去 KV cache 中聚合全部历史，其余算子都只处理这 1 个位置。因此矩阵乘部分处理 1 个 token 而 attention 部分要读 N 个位置的 KV——这就是权重访存与 batch 无关、KV 访存与 batch 成正比的直接原因（第 3.2 节）。

---

## 3. 逐算子的计算量与访存量

### 3.1 矩阵乘部分

设 `L` 为层数，`T = B × S` 为本步处理的 token 总数。

| 算子 | 权重形状 | 每层计算量 | 每层权重访存 |
|---|---|---|---|
| W_q | [H, Hq] | 2 T H Hq | 2 H Hq |
| W_k | [H, Hkv] | 2 T H Hkv | 2 H Hkv |
| W_v | [H, Hkv] | 2 T H Hkv | 2 H Hkv |
| W_o | [Hq, H] | 2 T Hq H | 2 Hq H |
| W_gate | [H, I] | 2 T H I | 2 H I |
| W_up | [H, I] | 2 T H I | 2 H I |
| W_down | [I, H] | 2 T I H | 2 I H |

权重访存量中的系数 2 为 fp16 的字节数，与 T 无关。这一列的总和乘以层数，即为第 02 章中的「权重访存」。

lm_head：计算量 `2 B Hv V`（只对每个序列的最后一个位置计算），权重访存 `2 H V`。词表 128256、隐藏维度 4096 时权重为 1.05 GB，占 8B 模型的 6.5%，不可忽略。

### 3.2 attention 部分

attention 不含权重，其计算量与访存量都与序列长度相关。设当前序列长度为 `N`（历史加当前）。

| 阶段 | 计算量 | KV 访存量 |
|---|---|---|
| prefill（S = N） | 2 × 2 × L × heads × head_dim × N² | 写入 2 L Hkv N × 2 字节 |
| decode（S = 1） | 2 × 2 × L × heads × head_dim × N | 读取 2 L Hkv N × 2 字节 |

其中外层的 2 表示 QK^T 与 AV 两次矩阵乘。

关键差别：

- **prefill 的 attention 计算量与 N² 成正比**。因果掩码使实际只需计算下三角，因此上表的结果要乘 0.5。第 02 章的计算器显示：Llama-3-8B 在 prompt 长度 2048 时 attention 占总计算量的 3.2%，8192 时为 11.8%，32768 时为 34.8%。长上下文场景下 attention 成为 prefill 的主要成本。
- **decode 的 attention 计算量很小，但访存量大**。每个序列要把自己的全部 KV 读一遍，算术强度约为 1（读一个 KV 元素只做常数次运算），是整个模型中算术强度最低的部分。

这一差异决定了 kernel 的设计目标不同：prefill 的 attention kernel 要提高计算效率（FlashAttention 的分块与 tensor core 利用），decode 的 attention kernel 要提高访存效率（PagedAttention kernel 的并行划分与合并访存）。第 11 章展开。

### 3.3 逐元素部分

| 算子 | 计算量 | 访存量 |
|---|---|---|
| RMSNorm（每层 2 次） | 约 4 T H | 2 × 2 T H（读写各一次） |
| RoPE | 约 6 T (Hq + Hkv) | 2 × 2 T (Hq + Hkv) |
| SiLU 与逐元素乘 | 约 5 T I | 3 × 2 T I |
| 残差相加（每层 2 次） | T H | 3 × 2 T H |

这些算子的计算量占比通常低于 1%，但访存量在 prefill 阶段（T 大）会累积到可观比例。它们是算子融合的主要目标：把 RMSNorm 与后续矩阵乘融合、把 SiLU 与逐元素乘融合，可以省掉中间张量的一次写和一次读。

---

## 4. 两个场景下的占比

用第 8 节的代码统计 Llama-3-8B（32 层，8 个 KV 头，I = 14336，词表 128256）的分布。

### 4.1 decode，batch 32，历史长度 8192

decode 是访存受限的，因此看访存量占比：

| 算子 | 访存量 | 占比 |
|---|---|---|
| attention（读取 KV cache） | 34360 MB | 69.2% |
| MLP gate+up | 7575 MB | 15.3% |
| MLP down | 3767 MB | 7.6% |
| QKV 投影 | 1623 MB | 3.3% |
| O 投影 | 1082 MB | 2.2% |
| lm_head | 1059 MB | 2.1% |
| 逐元素部分合计 | 193 MB | 0.4% |

按类别归并：KV cache 69.2%，矩阵乘权重 30.4%，激活与逐元素 0.4%。

结论：在这个场景下 KV cache 的访存量是权重的两倍以上，优化重点应当是 GQA、KV 量化、缩短有效上下文，而不是权重量化。若把历史长度改为 2048，KV 占比降到 35%，结论反过来。**优化手段的选择依赖于场景参数，不存在通用答案**。

decode 的整体算术强度为 12.4 FLOPs/byte，远低于 A100 的平衡点 156，确认访存受限。

### 4.2 prefill，batch 1，prompt 长度 8192

prefill 是计算受限的，因此看计算量占比：

| 算子 | 计算量 | 占比 |
|---|---|---|
| MLP gate+up | 61573 GFLOPs | 46.7% |
| MLP down | 30786 GFLOPs | 23.3% |
| attention | 17592 GFLOPs | 13.3% |
| QKV 投影 | 13194 GFLOPs | 10.0% |
| O 投影 | 8796 GFLOPs | 6.7% |
| 逐元素部分合计 | 33 GFLOPs | 0.02% |

整体算术强度 1498 FLOPs/byte，远高于平衡点，确认计算受限。

结论：MLP 占七成，但它是标准的大矩阵乘，kernel 效率通常已经接近上限；attention 占比随长度快速上升（长度 32768 时超过三分之一），是算法层面仍有空间的部分。

### 4.3 一个容易被忽略的数字

prefill 场景下，逐元素算子的计算量占比是 0.02%，但**访存量占比是 56.1%**：

| 类别 | 访存量 | 占比 |
|---|---|---|
| 激活与逐元素中间张量 | 49459 MB | 56.1% |
| 矩阵乘权重 | 37558 MB | 42.6% |
| KV cache 写入 | 1074 MB | 1.2% |

这个统计假设算子之间不融合，每个逐元素操作都要把中间张量写回 HBM 再读出来。虽然 prefill 整体是计算受限的（1498 远高于 156），但这部分访存不会被计算完全掩盖，实测中通常表现为百分之十几的额外耗时。

这是算子融合的直接收益来源：把 RMSNorm 融入后续矩阵乘、把 SiLU 与逐元素乘融合、把残差相加融入前一个算子，可以消掉这 56% 中的大部分。第 11 章展开。

---

## 5. 结构差异带来的影响

不同模型的结构选择会显著改变上述分布。

| 结构选择 | 影响 | 代表模型 |
|---|---|---|
| GQA（kv_heads < heads） | KV cache 与 KV 访存按比例下降 | Llama-3、Qwen2.5 |
| MLA（KV 压缩到低维隐空间） | KV cache 大幅下降，增加投影计算 | DeepSeek 系 |
| MoE（每 token 只激活部分专家） | 计算量下降，权重访存量不变或上升 | Mixtral、Qwen-MoE、DeepSeek-V3 |
| 更大的词表 | lm_head 权重与计算量上升 | Qwen 系（152064） |
| 更深更窄 vs 更浅更宽 | 影响 kernel 并行度与层间同步次数 | — |

MoE 值得单独说明：它把「计算量约等于 2 倍参数量」这一近似打破。以每 token 激活 2 个专家（共 8 个）为例，计算量按激活参数量算，而权重访存量在 batch 较大时接近全部参数量（因为不同 token 会命中不同专家）。因此 **MoE 在 decode 阶段的访存压力远高于同等激活参数量的稠密模型**，其部署通常需要专家并行（第 15 章）。

---

## 6. 一个反直觉的结论

decode 阶段增大 batch 不增加权重访存，因此「batch 32 的单步耗时与 batch 1 接近」。但这不意味着可以无限增大：

1. KV 访存量与 batch 成正比，长序列时它会超过权重访存（第 02 章的表）。
2. batch 超过机器平衡点后进入计算受限区，单步耗时开始随 batch 线性增长。
3. KV cache 容量限制了 batch 的上限（第 05 章）。

三者中通常是第 3 条先触发。因此**实际系统里 batch 的上限由显存容量决定，而不是由性能拐点决定**。这也解释了为什么 KV cache 的容量优化（分页、量化、GQA）在工程上的优先级高于计算优化。

---

## 7. 引擎实现对照

| 引擎 | 与算子结构相关的设计 |
|---|---|
| vLLM | 模型按算子组合写在 `vllm/model_executor/models/` 下；QKV 三个投影合并为一次矩阵乘，gate 与 up 合并为一次；attention 走统一的 backend 接口 |
| SGLang | 结构类似，attention backend 可切换（FlashInfer、Triton 等） |
| TensorRT-LLM | 编译期做图级融合，把归一化、残差、激活融进相邻矩阵乘 |

QKV 合并与 gate/up 合并是所有引擎的共同做法：三次 `[H, x]` 的矩阵乘合并为一次 `[H, x1+x2+x3]`，减少 kernel 启动次数并提高并行度。这也是读源码时会看到 `qkv_proj` 而非三个独立层的原因。

---

## 8. 代码：逐算子统计

`code/ch03_ops.py` 做两件事：

1. 按上述公式统计每个算子的计算量与访存量，输出占比排序表，支持切换 prefill 与 decode 场景。
2. 用 torch（CPU）搭一个单层 Llama 结构，验证各张量的形状与参数量统计一致，并实际测量 CPU 上各算子的耗时占比作为参照。

运行：

```bash
python3 code/ch03_ops.py
```

CPU 上测得的耗时占比与 GPU 不同（CPU 的算力带宽比远低于 GPU），不能直接用于推断 GPU 上的分布。它的用途是验证公式统计的算子清单没有遗漏，以及观察形状的变化。

建议的观察：把 `SEQ_LEN` 从 512 调到 32768，看 attention 在 prefill 计算量中的占比从不足 2% 升到 50% 以上；把 `KV_HEADS` 从 8 改为 32（即退化为 MHA），看 decode 场景下 KV 访存占比的变化。
