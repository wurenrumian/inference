# 04 · prefill 与 decode

第 02、03 章已经说明两个阶段的瓶颈不同。本章讨论这一差异在调度与工程上的后果：为什么批处理对两者的作用相反，为什么它们放在一起会互相干扰，以及由此派生出的全部优化手段。

---

## 1. 定义

| 项 | prefill | decode |
|---|---|---|
| 输入 | 整个 prompt，长度 N | 上一步产生的 1 个 token |
| 每序列每步处理的 token 数 | N | 1 |
| 计算形态 | 矩阵乘矩阵 | 矩阵乘向量 |
| 算术强度 | 约等于 N | 约等于 batch |
| 瓶颈 | 算力 | 显存带宽 |
| 产出 | 第 1 个输出 token，以及 N 个位置的 KV | 1 个输出 token，以及 1 个位置的 KV |
| 执行次数 | 每请求 1 次 | 每请求「输出长度 - 1」次 |
| 对应指标 | TTFT | TPOT |
| 显存占用变化 | 一次性增加 N 个 token 的 KV | 每步增加 1 个 token 的 KV |

一个输入 2000、输出 500 的请求，prefill 执行 1 次处理 2000 个 token，decode 执行 499 次各处理 1 个 token。**总 token 数上 prefill 占多数，总时间上 decode 占多数**。第 5 节给出具体比例。

---

## 2. 指标的精确定义

指标定义不统一会导致压测数据无法比较。以下是本教材采用的定义。

| 指标 | 定义 | 常见分歧 |
|---|---|---|
| TTFT | 请求到达服务端到收到第一个 token 的时间 | 是否包含网络往返；是否包含 tokenize |
| TPOT | （端到端时间 - TTFT）/（输出 token 数 - 1） | 分母用不用减 1；是否等同于 ITL |
| ITL | 相邻两个 token 之间的间隔 | TPOT 是 ITL 的平均值，但 ITL 的分布更能反映毛刺 |
| 端到端延迟 | 请求到达到最后一个 token 返回 | 流式与非流式的差别 |
| 输出吞吐 | 单位时间系统产出的输出 token 总数 | 是否计入输入 token |
| 总吞吐 | 单位时间处理的输入加输出 token 总数 | 两者相差可达数倍，报告时必须写明 |
| 请求吞吐 | 单位时间完成的请求数 | 依赖输出长度分布，跨场景不可比 |
| goodput | 满足 SLO 约束（如 TTFT < 1 s 且 P99 ITL < 50 ms）的请求所贡献的吞吐 | 约束条件必须写明 |

**只报告平均吞吐的压测结果没有意义**。同一个系统，把 batch 调大可以让吞吐数字翻倍，同时让 TPOT 超出 SLO。goodput 是唯一同时反映两侧的指标，但它依赖 SLO 定义，因此报告中必须写明约束。第 19 章给出完整的报告字段清单。

---

## 3. 批处理的作用相反

这是本章最重要的结论。

### 3.1 decode：批处理提高吞吐，几乎不增加延迟

decode 的权重访存量与 batch 无关（第 02 章）。因此：

```
batch 1  : 单步 13.7 ms，吞吐 73 token/s
batch 32 : 单步 19.6 ms，吞吐 1632 token/s
```

batch 增大 32 倍，单步耗时只增加 43%，吞吐提高 22 倍。这就是连续批处理（第 08 章）的收益来源。

### 3.2 prefill：批处理几乎不提高吞吐

prefill 已经是计算受限的。把两个 prompt 一起做 prefill，计算量翻倍，耗时也翻倍，单位时间处理的 token 数不变。计算器的输出：

| 一起处理的序列数 | prefill 耗时 | prefill 吞吐 | decode 单步 | decode 吞吐 |
|---|---|---|---|---|
| 1 | 157.6 ms | 12992 token/s | 13.66 ms | 73 token/s |
| 8 | 1247.1 ms | 13138 token/s | 15.01 ms | 533 token/s |
| 32 | 4982.3 ms | 13154 token/s | 19.61 ms | 1632 token/s |
| 128 | 19923.1 ms | 13158 token/s | 38.01 ms | 3367 token/s |

每个序列长度均为 2048。prefill 的吞吐从 12992 到 13158，基本不变；decode 的吞吐从 73 到 3367，提高 46 倍。

因此**prefill 的「批处理」不是为了提高吞吐，而是为了填满一步的 token 预算**。当一步只有一个 128 token 的短 prompt 时，GPU 的并行度不足，此时把多个短 prompt 拼在一起有收益；当单个 prompt 已经足够长（通常 512 token 以上）时，拼接没有额外收益。

这个区别在调度器设计上体现为：prefill 的调度以 **token 预算**为单位（`max_num_batched_tokens`），decode 的调度以 **序列数**为单位（`max_num_seqs`）。第 08 章展开。

---

## 4. 两阶段的相互干扰

### 4.1 现象

把 prefill 与 decode 放在同一步执行时，该步的耗时由 prefill 决定。以第 02 章的数据为例：

```
纯 decode 步（batch 32）        : 19.6 ms
混入一个 2048 token 的 prefill   : 19.6 + 157.6 ≈ 177 ms
```

对正在 decode 的 32 个序列而言，这一步的 ITL 从 19.6 ms 跳到 177 ms，是 9 倍的毛刺。用户侧的表现是输出突然卡顿。

### 4.2 三种处理方式

| 方式 | 做法 | 代价 |
|---|---|---|
| prefill 优先 | 有 prefill 就先做，decode 让路 | TTFT 好，ITL 毛刺大。vLLM V0 的默认行为 |
| decode 优先 | 先做完 decode，空闲时再做 prefill | ITL 平稳，TTFT 差且队列容易积压 |
| chunked prefill | 把长 prefill 切成小块，每步混入一小块 | 单步耗时均匀，prefill 的总耗时略增 |
| PD 分离 | 用不同实例分别承担，中间传输 KV | 两侧可独立扩缩，需要高速链路传 KV |

第 10 章展开后两者。此处需要记住的是：**这个选择本质上是在 TTFT 与 ITL 之间做分配，没有同时改善两者的方案**（PD 分离通过增加资源绕开，不算同一层面的选择）。

### 4.3 一个容易搞错的点

「prefill 优先会让 TTFT 更好」只在低负载时成立。高负载时 prefill 优先会导致：新请求不断插队做 prefill，已在运行的请求 decode 变慢、占用 KV 更久、显存进一步紧张、触发抢占、被抢占的请求重新排队。第 01 章的模拟器默认配置就处于这个状态，抢占次数 14 次，P99 TTFT 达到 9.6 秒。

因此调度策略必须包含准入控制：**在显存余量不足时拒绝启动新的 prefill**，而不是无条件优先。第 08、09 章展开。

---

## 5. 两阶段的时间占比

一个请求的总计算时间中两阶段各占多少，决定了优化的投入方向。

设 prompt 长度 N，输出长度 M，则：

```
prefill 时间 ≈ N × 单 token prefill 耗时
decode 时间 ≈ M × 单步 decode 耗时（受 batch 影响）
```

以 Llama-3-8B、A100、batch 32 为例（数据来自第 8 节的代码）：

| 场景 | N | M | prefill | decode | prefill 占比 |
|---|---|---|---|---|---|
| 短问答 | 128 | 256 | 13.7 ms | 3.6 s | 0.4% |
| 常规对话 | 2048 | 512 | 157.6 ms | 10.4 s | 1.5% |
| 多轮对话 | 8192 | 512 | 684.9 ms | 19.8 s | 3.3% |
| 长文摘要 | 32768 | 512 | 3700.4 ms | 57.4 s | 6.1% |
| 长文抽取 | 32768 | 64 | 3700.4 ms | 7.0 s | 34.5% |
| 代码补全 | 8192 | 32 | 684.9 ms | 1.2 s | 36.7% |
| 长文生成 | 2048 | 4096 | 157.6 ms | 105.4 s | 0.1% |

decode 时间按 batch 32 计算，且随序列增长逐步变慢（取中点估算）。这里的 decode 时间是「该请求在系统中经历的 decode 总时长」，不是它独占 GPU 的时间——batch 32 意味着这段时间被 32 个请求共享。

结论：

- **输出长的场景（对话、生成）由 decode 主导**，优化重点是提高 batch、降低 KV 访存。
- **输入长输出短的场景（摘要、抽取、补全、重排）由 prefill 主导**，优化重点是前缀缓存、更快的 attention kernel、chunked prefill 的开销控制。

面试中被问到「你会怎么优化」时，先问清楚输入输出长度分布，是一个有效的回答方式。同一套系统在两种负载下的最优配置不同。

---

## 6. 从两阶段差异派生的优化手段

后续章节的内容可以按它作用于哪个阶段来组织。

| 手段 | 作用阶段 | 机制 | 章 |
|---|---|---|---|
| 连续批处理 | decode | 提高 batch，摊薄权重访存 | 08 |
| PagedAttention | 两者 | 提高显存利用率，从而提高可达 batch | 06 |
| 前缀缓存 | prefill | 跳过重复前缀的计算 | 07 |
| GQA / MLA | decode | 减少 KV 访存量与容量 | 05 |
| KV 量化 | decode | 同上 | 05、13 |
| 权重量化 | decode | 减少权重访存量 | 13 |
| chunked prefill | 两者 | 均匀化单步耗时 | 10 |
| PD 分离 | 两者 | 消除干扰，独立扩缩 | 10 |
| FlashAttention | prefill | 减少 attention 的访存与显存占用 | 11 |
| PagedAttention kernel | decode | 支持非连续 KV 的高效读取 | 11 |
| 算子融合 | prefill | 减少中间张量的访存 | 11 |
| CUDA Graph | decode | 减少 kernel 启动开销 | 11 |
| 投机解码 | decode | 用一次前向产出多个 token，提高算术强度 | 14 |
| 张量并行 | 两者 | 降低单卡权重访存量与显存占用 | 15 |

其中投机解码值得单独指出：它是唯一直接改变 decode「一步一个 token」这一前提的手段。decode 访存受限意味着算力有富余，投机解码把富余的算力用来验证多个候选 token，从而把访存成本摊到多个 token 上。第 14 章展开。

---

## 7. 引擎实现对照

| 引擎 | 两阶段的处理 |
|---|---|
| vLLM V0 | prefill 优先，一步要么全 prefill 要么全 decode；可开启 chunked prefill |
| vLLM V1 | 默认启用 chunked prefill，prefill 与 decode 在同一步混合 |
| SGLang | 默认混合调度，可配置 prefill 与 decode 的比例 |
| TensorRT-LLM | 支持 in-flight batching，混合执行；有独立的 PD 分离方案 |

从 V0 到 V1 的变化方向是把「二选一」改为「混合」，代价是调度器和 kernel 都要能处理一步内既有多 token 序列又有单 token 序列的情况。这也是 attention kernel 需要支持变长输入的原因。

---

## 8. 代码：两阶段的对比

`code/ch04_two_phase.py` 输出四组数据：

1. batch 对 prefill 与 decode 吞吐的不同影响（验证第 3 节）
2. 混合批的干扰量化：一步中混入不同长度的 prefill 后，decode 序列的 ITL 变化（验证第 4 节）
3. 不同输入输出长度组合下两阶段的时间占比（验证第 5 节）
4. chunked prefill 的 chunk 大小对单步耗时分布的影响（第 10 章的铺垫）

运行：

```bash
python3 code/ch04_two_phase.py
```

第 2 组的输出量化了干扰的幅度（纯 decode 步 19.6 ms 为基准）：

| 混入的 prefill 长度 | 该步耗时 | ITL 放大 |
|---|---|---|
| 0 | 19.6 ms | 1.0x |
| 128 | 33.3 ms | 1.7x |
| 512 | 59.6 ms | 3.0x |
| 2048 | 177.2 ms | 9.0x |
| 8192 | 704.6 ms | 35.9x |
| 32768 | 3720.0 ms | 189.7x |

第 4 组给出 chunked prefill 的代价（32768 token 的 prompt，与 32 个 decode 序列共存）：

| chunk | 步数 | 最大单步耗时 | prefill 总耗时 | 相对一次性完成 |
|---|---|---|---|---|
| 32768 | 1 | 3720.0 ms | 3720.0 ms | 1.00x |
| 8192 | 4 | 1187.9 ms | 3784.8 ms | 1.02x |
| 2048 | 16 | 328.3 ms | 4044.1 ms | 1.09x |
| 1024 | 32 | 176.2 ms | 4389.8 ms | 1.18x |
| 512 | 64 | 99.2 ms | 5081.2 ms | 1.37x |
| 256 | 128 | 60.5 ms | 6464.1 ms | 1.74x |

**这张表是 chunked prefill 的核心依据**：chunk 从 32768 降到 512，最大单步耗时下降 37 倍（ITL 毛刺基本消除），代价是 prefill 总耗时（即 TTFT）上升 37%。chunk 再降到 256 时，收益变小而代价加速上升，因为每步都要重新读一遍 16 GB 的模型权重，权重访存开始主导。

实践中 chunk 通常取 512 到 2048。第 10 章讨论如何按 SLO 选择。

