# 05 · KV cache

KV cache 是推理引擎中唯一随请求动态增长的大块显存。它决定并发上限，进而决定吞吐与单位成本。本章给出它的容量计算、显存预算方法，以及降低它的全部手段。

---

## 1. 问题

### 1.1 KV cache 是什么

自回归生成第 t 个 token 时，attention 需要用当前位置的 query 去和前面所有位置的 key、value 做计算。前 t-1 个位置的 key 与 value 在之前的步骤中已经算过，且不会改变（因果注意力下，新 token 不影响旧位置的 KV）。把它们缓存下来即可避免重复计算。

### 1.2 不缓存的代价

假设不缓存，每生成一个 token 都要对整个已有序列重新做一次前向。生成 M 个 token 的总计算量：

```
有缓存  : prefill(N) + M 次 decode，每次约 2 × 参数量
无缓存  : sum over t in [N, N+M) of prefill(t)
```

以 N = 2048、M = 512、Llama-3-8B 为例，用第 02 章的计算器：

| 方式 | 总计算量 | 相对 |
|---|---|---|
| 有 KV cache | 42.2 TFLOPs | 1x |
| 无 KV cache | 19656.1 TFLOPs | 466x |

**KV cache 用显存换计算，换取的比例在两个量级以上**。因此它不是可选优化，是自回归推理的必要组成。由此产生的问题是显存怎么管，这是第 06 章往后的全部内容。

---

## 2. 容量计算

### 2.1 公式

```
每 token 每序列的 KV 字节数
    = 2 × 层数 × KV头数 × head_dim × 精度字节数
```

其中：

- 系数 2 表示 key 与 value 各一份
- **KV头数** 而不是 Q 头数。GQA 与 MQA 的收益全部来自这一项
- 与 batch 无关（每个序列各有一份）
- 与序列长度成正比

常见模型的单价：

| 模型 | 层数 | Q 头 | KV 头 | KB/token/序列 | 2048 token 的序列 |
|---|---|---|---|---|---|
| Llama-3-8B | 32 | 32 | 8 | 128.0 | 268 MB |
| Llama-2-13B | 40 | 40 | 40 | 800.0 | 1678 MB |
| Qwen2.5-7B | 28 | 28 | 4 | 56.0 | 117 MB |
| Qwen2.5-72B | 80 | 64 | 8 | 320.0 | 671 MB |

注意 Llama-2-13B 与 Llama-3-8B 的对比：参数量只差 60%，KV cache 单价差 6.25 倍。原因是前者用 MHA（40 个 KV 头），后者用 GQA（8 个）。**KV cache 的大小由结构决定，与参数量没有固定关系**，估算时不能按参数量比例推。

### 2.2 显存总账

一张卡的显存分为四部分：

| 部分 | 大小 | 特点 |
|---|---|---|
| 模型权重 | 参数量 × 精度字节数 | 固定，启动时确定 |
| KV cache | 引擎预分配的池 | 固定池大小，池内动态分配 |
| 激活值与临时张量 | 与 batch 和序列长度相关 | 动态，峰值出现在 prefill |
| 框架开销 | CUDA context、通信缓冲、碎片 | 1-2 GB 起 |

引擎的做法是：加载权重后，用一次试探性的前向测出激活峰值，把剩余显存的绝大部分划给 KV 池。vLLM 的 `--gpu-memory-utilization`（默认 0.9）控制这个比例。

计算器给出的预算表：

| 模型 | 硬件 | 显存 | 权重 | 运行时预留 | KV 可用 | 可容纳 token |
|---|---|---|---|---|---|---|
| Llama-3-8B | A100-80GB | 80 GB | 16.1 GB | 8.0 GB | 55.9 GB | 426788 |
| Llama-3-8B | RTX4090 | 24 GB | 16.1 GB | 2.4 GB | 5.5 GB | 42267 |
| Qwen2.5-7B | RTX4090 | 24 GB | 15.2 GB | 2.4 GB | 6.4 GB | 110910 |
| Llama-2-13B | A100-80GB | 80 GB | 26.0 GB | 8.0 GB | 46.0 GB | 56152 |
| Qwen2.5-72B | A100-80GB | 80 GB | 145.4 GB | — | 装不下 | — |

RTX4090 上的两行值得对比：Qwen2.5-7B 比 Llama-3-8B 参数量更小、KV 单价只有一半（4 个 KV 头对 8 个），可容纳的 token 数是后者的 2.6 倍。**选模型时 KV 单价的影响可能大于参数量**。

### 2.3 最大并发

```
最大并发 = KV 可用字节数 / (每 token 单价 × 平均序列长度)
```

Llama-3-8B 在 A100 上（55.9 GB KV 预算）：

| 序列长度 | 最大并发 | 单序列 KV | 该并发下的吞吐 |
|---|---|---|---|
| 512 | 833 | 67 MB | 9945 token/s |
| 2048 | 208 | 268 MB | 3899 token/s |
| 8192 | 52 | 1074 MB | 975 token/s |
| 32768 | 13 | 4295 MB | 244 token/s |
| 131072 | 3 | 17180 MB | 60 token/s |

序列长度每增加 4 倍，并发降到四分之一，吞吐随之下降。**长上下文服务的单位成本与上下文长度近似成正比**，这是长上下文按 token 计价且价格高于短上下文的成本依据。

这张表也解释了第 03 章的结论：实际系统中 batch 的上限由显存容量决定（8192 长度时只能到 52），远早于性能拐点（A100 上是 156）。

---

## 3. 降低 KV cache 的手段

按是否需要重新训练模型分为两类。

### 3.1 结构层面（需在预训练时确定）

| 方案 | 做法 | KV 相对 MHA | 8K 序列的并发 |
|---|---|---|---|
| MHA | 每个 Q 头有独立的 KV 头 | 1.00x | 13 |
| GQA-8 | 每 4 个 Q 头共享 1 组 KV | 0.25x | 52 |
| GQA-4 | 每 8 个 Q 头共享 1 组 KV | 0.12x | 104 |
| MQA | 全部 Q 头共享 1 组 KV | 0.03x | 416 |
| MLA | KV 压缩到低维隐空间，用时投影回来 | 0.07x | 185 |

（32 层、32 个 Q 头、head_dim 128；MLA 按 DeepSeek-V2 的 512 维压缩加 64 维 RoPE 计算）

这些是模型作者的选择，部署时无法更改。当前主流开源模型基本都用 GQA，KV 头数在 4 到 8 之间。

**MLA 的机制**值得单独说明：它不缓存完整的 K 与 V，而是缓存一个低维的隐向量，attention 时再用矩阵投影恢复。这把显存成本换成了计算成本。由于 decode 是访存受限的（算力有富余），这个交换在 decode 阶段是划算的。代价是 attention 的实现复杂度上升，且需要专门的 kernel。

### 3.2 部署层面（可在部署时选择）

| 方案 | 做法 | 收益 | 代价 |
|---|---|---|---|
| KV 量化 | 用 fp8 / int8 / int4 存储 | 容量与访存量按比例下降 | 精度损失，需要 kernel 支持 |
| 分页管理 | 消除预留浪费 | 有效容量提高 2-4 倍 | 实现复杂度，第 06 章 |
| 前缀共享 | 相同前缀只存一份 | 取决于命中率，第 07 章 | 需要引用计数与淘汰策略 |
| 换出到主机内存 | 把不活跃序列的 KV 移到 CPU 内存 | 提高可挂起的请求数 | PCIe 带宽，第 09 章 |
| 滑动窗口 / 稀疏注意力 | 只保留部分历史 | 容量随窗口大小固定 | 改变模型语义，需模型支持 |

其中分页与前缀共享是纯粹的工程收益（不损失精度、不改变输出），因此优先级最高。这是 PagedAttention 成为标配的原因。

### 3.3 KV 量化的双重收益

KV 量化同时作用于两个维度。计算器的输出（Llama-3-8B，A100，序列长度 8192）：

| KV 精度 | KB/token | 最大并发 | 满并发时的单步 | 满并发吞吐 | 并发固定 52 时的单步 |
|---|---|---|---|---|---|
| fp16 | 128.0 | 52 | 53.35 ms | 975 token/s | 53.35 ms |
| fp8 / int8 | 64.0 | 104 | 53.35 ms | 1949 token/s | 33.41 ms |
| int4 | 32.0 | 208 | 53.35 ms | 3899 token/s | 23.44 ms |

两种用法：

- 把省下的显存用于提高并发：单步耗时不变（KV 池本来就是满的，总访存字节数不变），吞吐线性提高。
- 保持并发不变：单步耗时下降，即 TPOT 改善。

「满并发时单步耗时不变」这一点容易被误解为量化无效。实际情况是收益转移到了吞吐上。选择哪一种由 SLO 决定：TPOT 已经贴着约束时选后者，吞吐不够时选前者。

---

## 4. KV cache 的布局

KV cache 在显存中的组织方式影响 attention kernel 的访存效率。常见的两种：

```
布局 A (NHD): [block, token, head, dim]   同一 token 的所有 head 连续
布局 B (HND): [block, head, dim, token]   同一 head 的所有 token 连续
```

decode 阶段的访存模式是：写入 1 个 token 的全部 head，读取全部历史 token 的某个 head。

| 布局 | 写入 | 读取 |
|---|---|---|
| A | 一次连续写 | 每个 head 的历史不连续（跨 token 有跳跃） |
| B | 每个 head 分散写 | 同一 head 的历史连续 |

由于 decode 每步只写 1 个 token 但要读全部历史，读的数据量远大于写，因此读友好的布局通常更优。实际选择由 attention kernel 的实现决定，不同 backend（FlashAttention、FlashInfer、Triton）的偏好不同，引擎中会有布局转换或多套 kernel。

分页之后还有一层：block 内部用哪种布局，block 之间通过 block table 索引。第 06 章展开。

---

## 5. 一个常见的估算错误

「模型 16 GB，卡有 80 GB，所以能放 4 个模型」——这个推理忽略了 KV cache。实际情况：

```
80 GB 卡跑 Llama-3-8B：
  权重       16.1 GB   (20%)
  运行时预留  8.0 GB   (10%)
  KV cache   55.9 GB   (70%)
```

**KV cache 占用的显存通常是权重的 2-4 倍**。因此单卡跑多个模型实例几乎总是错误的配置：两个实例各分 40 GB，KV 预算从 55.9 GB 降到 2 × 23.9 = 47.8 GB，总并发下降，而权重被存了两份。

正确的做法是单实例配大 KV 池，靠连续批处理提高利用率。只有在需要隔离（不同模型、不同租户的强隔离要求）时才在一张卡上跑多个实例。

---

## 6. 引擎实现对照

| 引擎 | KV cache 的管理 |
|---|---|
| vLLM | 启动时按 `gpu_memory_utilization` 探测并预分配 block 池；`--kv-cache-dtype fp8` 开启 KV 量化；block_size 默认 16 |
| SGLang | 类似的预分配；`--mem-fraction-static` 控制比例；KV 池与基数树前缀缓存共用 |
| TensorRT-LLM | `KvCacheConfig` 控制，支持 paged 与非 paged 两种模式；支持 fp8 KV |
| llama.cpp | `--ctx-size` 直接指定上下文长度，按最大值预留，无分页 |

共同点是**预分配**：启动时一次性划走显存，运行期不再向 CUDA 申请。原因是运行期分配会引入不确定的延迟，且容易产生碎片。这与操作系统中数据库自管缓冲池的做法一致。

---

## 7. 代码

`code/ch05_kvcache.py` 包含：

1. 各模型的 KV 单价计算
2. 显存预算表：给定模型与卡型，算出 KV 可用量与可容纳 token 数
3. 最大并发随序列长度的变化
4. MHA / GQA / MQA / MLA 的对比
5. KV 量化的双重收益
6. 用 torch 验证公式与实际张量大小一致，以及两种布局的差别

运行：

```bash
python3 code/ch05_kvcache.py
```

建议的实验：

- 把 `RUNTIME_OVERHEAD_FRAC` 从 0.10 改为 0.05，观察 KV 预算的变化。这对应把 `--gpu-memory-utilization` 从 0.90 调到 0.95，是部署中最常用的调参手段之一，代价是 OOM 风险上升。
- 在第 2 节的表中加入你手上的卡型与目标模型，得到实际可用的并发数。这个数字是容量规划的起点（第 16 章）。
