# 06 · PagedAttention 与 block manager

第 05 章确定了 KV cache 的总量。本章解决它怎么分配：为什么连续分配会失效，分页如何解决，以及 block manager 需要处理的全部情况。

对有操作系统背景的读者，本章的机制是熟悉的：这就是虚拟内存的分页，只是页表由 GPU kernel 读取。差异在第 6 节。

---

## 1. 问题：连续分配为什么失效

### 1.1 三种浪费

早期的推理实现为每个请求分配一块连续显存。分配多大？

**方案一：按 `max_seq_len` 预留。** 一个支持 4096 上下文的服务，每个请求预留 4096 个 token 的 KV 空间。若平均实际长度是 611，浪费 85%。上下文上限越大浪费越严重：支持 128K 上下文时，浪费超过 99%。

**方案二：按当前长度分配，增长时搬移。** 每次超出已分配空间就找一块更大的连续区，把已有 KV 拷过去。代价是拷贝量与序列长度成正比，且搬移期间该序列无法参与计算。序列越长搬移越贵，而长序列恰恰是搬移最频繁的。

**方案三：按预测长度分配。** 需要预测输出长度。输出长度取决于模型的生成内容，无法准确预测；预测偏小则退化为方案二，偏大则退化为方案一。

三个方案的共同困难是：**输出长度在生成结束之前不可知**。这是推理与训练最本质的差异之一，也是连续分配方案失效的根本原因。

### 1.2 外部碎片

即使能预知长度，连续分配还有外部碎片问题：不同请求以不同顺序结束，空闲空间被切成互不相邻的小段，总量足够但没有足够长的连续段。

第 7 节的代码用同一容量对比三种方式：

| 方式 | 拒绝率 | 峰值并发 |
|---|---|---|
| 按 max_seq_len 预留 | 80.0% | 22 |
| 按实际长度分配连续空间 | 0.5% | 126 |
| 分页（block_size 16） | 0.0% | 126 |

第二行的拒绝率之所以低，是因为模拟中假设分配时已知最终长度。真实场景不成立，因此实际系统只能在第一行与频繁搬移之间选择。PagedAttention 论文报告的「显存有效利用率 20%-40%」对应的就是第一行的情况。

---

## 2. 分页机制

### 2.1 三个概念

```
block（页）      : KV cache 的固定大小分配单位，容纳 block_size 个 token 的 KV
block table（页表）: 每个序列一张，逻辑块号 → 物理块号
slot（槽位）      : 单个 token 的 KV 存放位置，编号 = 物理块号 × block_size + 块内偏移
```

一个 40 token 的序列，block_size 为 16：

```
逻辑位置 0-15   → 逻辑块 0 → 物理块 31 → 槽位 496-511
逻辑位置 16-31  → 逻辑块 1 → 物理块 30 → 槽位 480-495
逻辑位置 32-39  → 逻辑块 2 → 物理块 29 → 槽位 464-471（该块只用了 8 个槽位）

block table = [31, 30, 29]
```

物理块在显存中的位置互不相邻。逻辑上连续的序列，物理上是分散的。

### 2.2 分配时机

| 场景 | 分配行为 |
|---|---|
| prefill | 一次分配 `ceil(prompt_len / block_size)` 个块 |
| decode | 只有当前块写满时才分配 1 个新块，即平均每 block_size 步分配一次 |
| 前缀命中 | 命中的块不分配，直接引用已有块（第 07 章） |
| 请求结束 | 全部块的引用计数减 1，归零者回到空闲列表 |

decode 阶段的分配频率是每 block_size 步一次，因此分配本身的开销可以忽略。真正需要处理的是**分配失败**：池中无空闲块时，调度器必须抢占（第 09 章）。

### 2.3 attention kernel 的改造

分页的代价在 kernel 侧。标准的 attention kernel 假设 KV 是一块连续张量，可以用步长计算地址。分页后地址不再连续，kernel 必须：

1. 读入该序列的 block table
2. 按逻辑位置计算逻辑块号与块内偏移
3. 查表得到物理块号
4. 计算实际地址

这引入两项开销：额外的间接访问（读 block table），以及访存的连续性下降（每 block_size 个 token 就要跳一次）。PagedAttention 论文报告的 kernel 开销在 20%-26%，但换来的并发提升远大于此。

写入侧的处理是 `slot_mapping`：调度器在 CPU 上为本步的每个 token 算好目标槽位编号，作为一个整数张量传给 kernel，kernel 按它把新算出的 K、V 写入对应位置。这样 kernel 不需要理解块结构，只需要按槽位编号做散射写。

---

## 3. block_size 的选择

block_size 是唯一需要调的参数。代码给出的权衡（2000 个请求，平均长度 642）：

| block_size | 空间利用率 | 平均内部碎片 | 平均 block table 长度 |
|---|---|---|---|
| 1 | 100.0% | 0.0 | 641.9 |
| 4 | 99.8% | 1.5 | 160.8 |
| 16 | 98.8% | 7.5 | 40.6 |
| 64 | 95.4% | 31.2 | 10.5 |
| 256 | 83.6% | 126.1 | 3.0 |

四个方向的影响：

| block_size | 内部碎片 | block table 开销 | 访存连续性 | 前缀共享粒度 |
|---|---|---|---|---|
| 小 | 少 | 大 | 差 | 细，命中率高 |
| 大 | 多 | 小 | 好 | 粗，命中率低 |

vLLM 默认 16，SGLang 使用 1（配合基数树，见第 07 章）。选 16 的理由是内部碎片已降到 1% 左右，再小收益递减，而 block table 的读取开销与访存分散度会上升。

**block_size 与前缀共享的关系**值得注意：共享的粒度是块，因此只有恰好对齐到块边界的前缀才能共享。block_size 越大，能共享的前缀越短。若 system prompt 长度为 100、block_size 为 64，则只有前 64 个 token 能共享，剩下 36 个要重算。

---

## 4. 引用计数与写时复制

### 4.1 需要共享的场景

| 场景 | 共享内容 |
|---|---|
| 并行采样（一个 prompt 生成 n 个候选） | 整个 prompt 的 KV |
| beam search | 各候选分支的公共前缀 |
| 跨请求的前缀缓存 | 相同 system prompt、相同文档前缀 |
| Agent 多轮对话 | 上一轮的完整历史 |

### 4.2 机制

每个物理块有一个引用计数。fork 一个序列时，复制 block table（一个整数列表，开销可忽略），并把每个块的引用计数加 1。

写入时检查：若目标块的引用计数大于 1，说明有其他序列在用，必须先复制一份新块，改写自己的 block table，再写入。这就是写时复制。

代码中的实测（100 token 的父序列，fork 出 4 个子序列）：

```
不共享时需要 35 个 block（5 × 7）
共享后需要 7 个 block，节省 80%

各子序列各生成 1 个 token 后：
  最后一个未满的块被复制 4 次（每个子序列各一份）
  前面 6 个已满的块继续共享，引用计数保持 5
```

**已满的块不会再被写入，因此永远不需要复制**。这使得共享的收益随 prompt 长度增长而增大：prompt 越长，已满块越多，复制的比例越低。

### 4.3 实现上的注意点

- 引用计数的增减必须与 block table 的修改保持一致，否则会泄漏或提前释放。代码中用一个断言在结束时校验池是否复原。
- 抢占（第 09 章）时释放块，要按引用计数正确处理共享块。
- 前缀缓存（第 07 章）引入了「引用计数为 0 但内容仍有效」的状态，需要在空闲列表之外再维护一个可复用池。

---

## 5. 显存池的初始化

引擎启动时如何确定池的大小：

1. 加载模型权重，记录占用
2. 用一个构造的最大负载（`max_num_batched_tokens` 个 token）跑一次前向，测量激活值的峰值占用
3. 可用 KV 空间 = 总显存 × `gpu_memory_utilization` − 权重 − 激活峰值
4. 块数 = 可用空间 / (block_size × 每 token 的 KV 字节数)
5. 一次性分配这块显存，运行期不再向 CUDA 申请

第 2 步是必要的：激活峰值与 batch 配置相关，无法用公式准确算出。这也是引擎启动慢的原因之一（需要跑一次探测前向）。

`gpu_memory_utilization` 调高会增加 OOM 风险：探测时的激活峰值未必是运行期的真实峰值（例如某个请求触发了未探测到的代码路径）。生产环境通常留 5%-10% 余量。

---

## 6. 与操作系统分页的对照

相同的部分：分页消除外部碎片、页表做地址翻译、引用计数与写时复制、按需分配。

不同的部分：

| 项 | 操作系统 | PagedAttention |
|---|---|---|
| 地址翻译 | MMU 硬件完成，对程序透明 | 由 kernel 显式查表，需要改写 attention 实现 |
| 缺页 | 触发中断，从磁盘换入，程序无感 | 没有缺页机制，分配失败就必须抢占整个请求 |
| 页大小 | 4 KB 起，由硬件决定 | 16 个 token，由软件决定，可调 |
| 交换 | 换出到磁盘，由内核策略决定 | 换出到主机内存，由调度器显式决定（第 09 章） |
| 访问模式 | 随机 | 高度可预测：顺序追加写、全量顺序读 |

最后一行是关键差异，它带来两个后果：

- 因为访问模式可预测，引擎不需要 LRU 之类的通用页替换算法，调度器可以精确知道每个请求下一步需要多少块。
- 因为没有缺页机制，「显存不足」是一个必须由调度器在计算之前处理的问题，不能等到 kernel 执行时才发现。这决定了调度器与 block manager 必须紧耦合。

---

## 7. 引擎实现对照

| 引擎 | 相关模块 |
|---|---|
| vLLM V0 | `BlockSpaceManager`，管理 GPU 与 CPU 两个 `BlockAllocator`，支持 swap |
| vLLM V1 | `KVCacheManager` 与 `BlockPool`，前缀缓存成为默认路径，块的复用与淘汰统一处理 |
| SGLang | `TokenToKVPoolAllocator` 加基数树，块粒度为 1 个 token |
| TensorRT-LLM | `KVCacheManager`，支持 paged 与 contiguous 两种模式 |

V0 到 V1 的主要变化是把前缀缓存从可选路径变为默认路径，块的生命周期从「分配—使用—释放」变为「分配—使用—进入可复用池—被命中或被淘汰」。第 07 章展开。

读源码时的入口顺序建议：先看 `allocate`（prefill 时如何一次分配）、再看 `append_slots`（decode 时如何增量分配）、再看 `free`（如何处理共享块）、最后看 `fork` 与 swap。

---

## 8. 代码

`code/ch06_block_manager.py` 实现了一个可运行的 block manager，包含：

1. `BlockAllocator`：空闲列表与引用计数
2. `Sequence`：block table、槽位寻址、按需分配、写时复制、fork
3. 同容量下三种管理方式的对照实验
4. block_size 的权衡表
5. 并行采样场景的共享与复制，末尾有引用计数的泄漏校验

运行：

```bash
python3 code/ch06_block_manager.py
```

这份实现的结构与 vLLM 的 block manager 一致，去掉了 swap、前缀哈希、淘汰策略。后续章节会逐步补上：第 07 章加入前缀哈希与复用池，第 09 章加入 swap 与抢占。

建议的实验：把第 2 节的 `max_len` 从 4096 改到 32768，观察预留方式的拒绝率如何随支持的上下文长度上升而恶化。这是长上下文模型必须用分页的原因。
