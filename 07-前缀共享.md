# 07 · 前缀共享

第 06 章解决了 KV cache 怎么分配。本章解决一个相邻的问题：不同请求之间的相同前缀，能不能只算一次、只存一份。

这是当前推理服务中收益最大的单项优化。在 Agent 与多轮对话负载下，它可以消除八成以上的 prefill 计算。

---

## 1. 问题

### 1.1 重复来自哪里

| 场景 | 重复的内容 | 重复长度 |
|---|---|---|
| 相同 system prompt 的大量请求 | system prompt | 数百到数千 token |
| 多轮对话 | 前面所有轮的完整历史 | 随轮次线性增长 |
| Agent 循环 | 系统提示 + 工具定义 + 已有的执行轨迹 | 数千到数万 token |
| few-shot 提示 | 示例部分 | 数百到数千 token |
| RAG | 检索到的相同文档 | 取决于文档复用率 |
| 并行采样与 beam search | 同一个 prompt | prompt 全长 |

其中 Agent 场景最极端：一次任务中每一步都要把之前的全部对话与工具输出重新发一遍，第 N 步的 prompt 包含第 N-1 步的全部内容。若不做前缀共享，一个 20 步的 Agent 任务，prefill 的总计算量与步数的平方成正比。

### 1.2 收益的形式

命中前缀带来两项收益：

- **计算**：命中部分的 prefill 直接跳过，TTFT 按比例下降
- **显存**：命中部分的 KV 只存一份，等效于提高了可用容量

两项收益中通常计算更重要，因为 TTFT 是用户直接感知的指标。

---

## 2. 方案一：哈希块表

vLLM 的做法。核心是给每个块算一个能代表「从序列开头到本块结尾的全部内容」的哈希。

### 2.1 链式哈希

```
block_hash[0] = hash(0,               tokens[0:16])
block_hash[1] = hash(block_hash[0],   tokens[16:32])
block_hash[2] = hash(block_hash[1],   tokens[32:48])
...
```

每个块的哈希包含前一个块的哈希，因此哈希相同意味着从头到本块的全部 token 都相同。这是可以直接用哈希判断前缀相同的原因——不需要逐 token 比较。

任何一个 token 不同，其后所有块的哈希都会不同，因此不会误命中。代码的第 5 节做了这个验证。

### 2.2 查找与插入

```
查找：从第 0 块开始逐块查表，遇到第一个未命中就停止
      命中的块引用计数加 1，直接复用，不需要计算
插入：未命中的块在计算完成后，以其哈希为键登记到复用池
```

注意「遇到第一个未命中就停止」：前缀共享只能命中**从开头开始的连续前缀**。序列中间相同、开头不同的两个请求无法共享，因为 attention 依赖位置，同一段文本在不同位置的 KV 不同。

### 2.3 三种状态的块

引入前缀缓存后，块的生命周期比第 06 章多了一种状态：

| 状态 | 含义 | 可否被新请求分配 |
|---|---|---|
| 使用中 | 引用计数大于 0 | 否 |
| 可复用 | 引用计数为 0，但内容仍然有效且已登记哈希 | 可以，代价是丢弃其缓存价值 |
| 空闲 | 从未使用或已被清除 | 可以 |

分配时优先取空闲块，空闲耗尽时按 LRU 从可复用池中回收。这使得前缀缓存不占用「额外」显存——它使用的是本来就空闲的部分，只在显存紧张时才被回收。这是前缀缓存可以默认开启的原因。

### 2.4 不足一块的部分

只有完整的块才参与哈希。一个 100 token 的 system prompt，block_size 为 16 时只有前 96 个 token（6 块）能被缓存，剩下 4 个每次都要重算。这是块粒度方案的固有损失，第 4 节量化。

---

## 3. 方案二：基数树

SGLang 的 RadixAttention。用一棵压缩前缀树组织所有缓存的序列，匹配粒度是 1 个 token。

```
root
 ├── [system prompt A 的 760 个 token]
 │     ├── [用户问题 1] ── [回复 1] ── [用户问题 2] ── ...
 │     └── [用户问题 2]
 └── [system prompt B 的 420 个 token]
       └── ...
```

每个节点承载一段 token（压缩路径，避免每个 token 一个节点）。插入新序列时，沿树下探到匹配的最深位置；若在某个节点中途分叉，则分裂该节点。

淘汰按 LRU 从叶子开始：只有叶子可以被删除，因为删除内部节点会切断其子树的路径。

与哈希块表的对比：

| 项 | 哈希块表 | 基数树 |
|---|---|---|
| 匹配粒度 | block_size 个 token | 1 个 token |
| 查找复杂度 | 每块一次哈希查表 | 沿树下探，与前缀长度相关 |
| 插入复杂度 | 每块一次插入 | 可能需要分裂节点 |
| 淘汰 | LRU 队列，直接 | 只能从叶子淘汰，需要维护树结构 |
| 实现复杂度 | 低 | 中 |
| 与分页的关系 | 天然对齐 | 需要额外的 token 到块的映射 |

---

## 4. 命中率的量化

用第 8 节的负载（800 个请求，4 个 system prompt，30% 为多轮对话的后续轮，共 550492 个 prompt token）：

### 4.1 两种方案的对比

| 方案 | 跳过的 token | 节省比例 | 仍需 prefill |
|---|---|---|---|
| 无前缀缓存 | 0 | 0.0% | 550492 |
| 哈希块表（block_size 16） | 463584 | 84.2% | 86908 |
| 基数树（token 粒度） | 469085 | 85.2% | 81407 |

基数树的优势是 1.0 个百分点。**这个差距远小于两者与「无缓存」的差距**，因此方案选择的重要性远低于「是否启用」。

### 4.2 block_size 的影响

| block_size | 节省比例 | 相对 token 粒度的损失 |
|---|---|---|
| 1 | 85.2% | 0.0 |
| 4 | 85.1% | 0.1 |
| 8 | 84.9% | 0.3 |
| 16 | 84.2% | 1.0 |
| 32 | 82.7% | 2.5 |
| 64 | 79.2% | 6.0 |
| 128 | 73.1% | 12.1 |

损失随 block_size 近似线性增长，因为每个前缀末尾平均浪费 block_size/2 个 token。block_size 16 的损失约 1 个百分点，是可以接受的取舍；128 就明显了。

这与第 06 章的内部碎片分析共同构成 block_size 的选择依据：太小则索引开销与访存分散度上升，太大则内部碎片与前缀损失上升。

### 4.3 容量的影响

| 缓存容量（token） | 节省比例 | 淘汰的块数 |
|---|---|---|
| 2000 | 43.8% | 24630 |
| 10000 | 74.6% | 13518 |
| 50000 | 80.2% | 9096 |
| 100000 | 82.7% | 5116 |
| 200000 | 84.2% | 0 |
| 500000 及以上 | 84.2% | 0 |

命中率随容量上升到某个点后不再提高（本负载中约 20 万 token，即工作集大小）。这给出了容量规划的方法：**测出负载的工作集大小，缓存容量覆盖它即可，再多没有收益**。

需要注意的是前缀缓存与运行中请求共用同一块显存。缓存留得多，可用于并发的空间就少。两者的比例需要按负载调整：重复度高的负载（Agent、固定 system prompt）应当多留缓存，一次性请求为主的负载不需要。

### 4.4 按请求类型分解

| 类型 | 请求数 | prompt token | 跳过的 token | 节省比例 |
|---|---|---|---|---|
| 首轮 | 577 | 354683 | 284704 | 80.3% |
| 后续轮 | 223 | 195809 | 178880 | 91.4% |
| 合计 | 800 | 550492 | 463584 | 84.2% |

首轮的命中来自共享的 system prompt，后续轮的命中来自上一轮的完整历史（包括上一轮生成的输出——请求结束时输出的 KV 也在池中，同样可复用）。

**报告前缀缓存的收益时必须说明负载构成**。同一个系统在「完全独立的一次性请求」负载下命中率为 0，在 Agent 负载下可以超过 90%。脱离负载的命中率数字无法比较。

---

## 5. 正确性

### 5.1 为什么命中不改变输出

KV 只由三样东西决定：token 内容、token 在序列中的位置、模型权重。前缀相同意味着前两者相同，因此复用得到的 KV 与重新计算完全一致。

采样参数（temperature、top_p、随机种子）作用在 logits 上，不影响 KV。因此两个前缀相同但采样参数不同的请求可以安全共享前缀。

### 5.2 会破坏共享的因素

| 因素 | 原因 | 处理 |
|---|---|---|
| 不同的 LoRA 适配器 | 权重不同，KV 不同 | 哈希中加入 adapter id |
| 不同的多模态输入 | 图像 token 的内容由图像决定 | 哈希中加入图像内容的哈希 |
| 不同的模型或精度 | 权重不同 | 通常不同实例，天然隔离 |
| 滑动窗口注意力 | 有效历史被截断，位置语义改变 | 需要专门处理 |
| 位置编码的外推或缩放设置不同 | 位置嵌入不同 | 配置固定，通常不构成问题 |

前两项是实现中真实存在的坑：若哈希只覆盖 token id，多模态或多 LoRA 场景会产生错误的命中，表现为输出内容错乱且难以复现。

### 5.3 一个安全性问题

前缀缓存是跨请求的共享资源，因此它引入了一个侧信道：攻击者可以通过测量 TTFT 判断某个前缀是否被其他用户请求过。在多租户服务中，这可能泄漏其他租户的 prompt 内容。

处理方式是按租户隔离缓存命名空间（哈希中加入租户 id），代价是命中率下降。是否需要取决于威胁模型。

---

## 6. 与 API 侧 prompt caching 的关系

各家 API 提供的 prompt caching 与引擎内的前缀缓存是同一机制在不同层的体现：

| 项 | 引擎内前缀缓存 | API 侧 prompt caching |
|---|---|---|
| 位置 | 单个引擎实例的显存内 | 服务端，可能跨实例 |
| 生效条件 | 请求被路由到持有该前缀的实例 | 由服务端保证 |
| 有效期 | 到被淘汰为止，不确定 | 通常有明确的 TTL |
| 用户可见性 | 不可见，尽力而为 | 显式标记断点，计费上有区分 |
| 命中率的决定因素 | 负载重复度 + 路由策略 | 用户是否正确设置断点 |

对使用方而言，两者的优化方法一致：**把不变的内容放在前面，变化的内容放在后面**。system prompt、工具定义、长文档应当置于 prompt 开头；用户的当前问题置于末尾。把一个变化的时间戳放在 system prompt 开头，会让整个前缀失效。

对服务方而言，引擎内的前缀缓存只在请求落到同一实例时生效，因此需要前缀感知的路由（第 16 章）。轮询路由会让命中率降到接近 1/实例数。

---

## 7. 引擎实现对照

| 引擎 | 方案 | 默认状态 |
|---|---|---|
| vLLM V1 | 哈希块表，`BlockPool` 中维护 `cached_block_hash_to_block` | 默认开启 |
| vLLM V0 | 哈希块表，`--enable-prefix-caching` | 默认关闭 |
| SGLang | 基数树（RadixAttention） | 默认开启 |
| TensorRT-LLM | 块复用（`enableBlockReuse`） | 可配置 |

vLLM 从 V0 到 V1 把前缀缓存从可选变为默认，原因是命中率损失（未命中时的哈希计算开销）远小于命中收益，且实现上与块管理统一后不再有额外的复杂度。

读源码建议的入口：vLLM 的 `vllm/v1/core/kv_cache_manager.py` 与 `block_pool.py`，重点看 `get_computed_blocks`（查找命中前缀）与 `cache_full_blocks`（写入缓存）。

---

## 8. 代码

`code/ch07_prefix_cache.py` 实现了两种方案并在同一负载上对比：

1. `HashedBlockCache`：链式块哈希 + LRU 复用池
2. `RadixTree`：压缩前缀树，支持节点分裂与叶子淘汰
3. 负载生成器：可配置 system prompt 数量与多轮对话比例
4. block_size、容量、请求类型三个维度的分解
5. 正确性验证：前缀相同则命中，任一 token 不同则不命中

运行：

```bash
python3 code/ch07_prefix_cache.py
```

建议的实验：

- 把多轮对话比例从 0.30 改为 0，命中率降到只剩 system prompt 的贡献，观察下降幅度。
- 把 4 个 system prompt 改为 100 个（模拟多租户），观察在相同容量下命中率的变化。这是判断多租户服务是否需要扩大缓存的方法。
- 把容量设为你的实际 KV 预算（第 05 章算出的 token 数）的一半，看是否已经覆盖工作集。
