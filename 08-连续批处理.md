# 08 · 连续批处理与调度器

第 06、07 章解决了显存怎么管。本章解决时间怎么排：每一步迭代由哪些请求组成，谁先谁后，什么时候拒绝。

调度器是推理引擎中唯一直接决定所有指标的模块。它的输入是队列状态与显存余量，输出是本步的 batch 组成。

---

## 1. 问题：静态批处理的浪费

传统推理服务的做法是攒够 N 个请求组成一批，一起执行到全部完成。问题在于同一批内的请求输出长度差异很大：

```
请求 A：生成 20 个 token 后完成
请求 B：生成 500 个 token 后完成

A 完成后它在 batch 中的位置一直空着，直到 B 也完成。
```

输出长度服从长尾分布（少数请求很长），因此这个浪费很严重。第 8 节的模拟对比（同一负载，到达率 8 请求/秒）：

| 策略 | 吞吐 | TTFT P50 | goodput |
|---|---|---|---|
| 静态批处理 | 1065 token/s | 89.45 s | 3 token/s |
| 连续批处理 | 1074 token/s | 0.09 s | 1068 token/s |

吞吐几乎相同，TTFT 差三个量级。原因是静态批处理下请求必须等当前批完全结束才能进入，排队时间随批次长度累积。goodput（满足 SLO 的有效吞吐）从 3 升到 1068。

**这说明只看吞吐会得出错误结论**。静态批处理的吞吐不差，因为 GPU 一直在算；差的是排队时间，而排队时间不体现在吞吐里。

---

## 2. 连续批处理

出自 Orca 论文，原始表述是 iteration-level scheduling：调度决策的粒度从「一批请求」降到「一步迭代」。

```
每一步迭代：
  1. 接收新到达的请求，加入 waiting 队列
  2. 检查显存余量，必要时抢占运行中的请求
  3. 从 waiting 队列中挑选请求做 prefill（受 token 预算限制）
  4. 运行中的请求各做一步 decode（受序列数限制）
  5. 执行一次前向
  6. 采样，更新状态，移出已完成的请求，释放其 block
```

请求可以在任意一步加入或离开。这是现代推理引擎吞吐提升的主要来源。

### 2.1 两个预算

调度器受两个独立的上限约束：

| 预算 | 参数名（vLLM） | 约束的对象 | 为什么需要 |
|---|---|---|---|
| 单步 token 数 | `max_num_batched_tokens` | 本步处理的 token 总数 | 控制单步耗时，防止长 prompt 造成毛刺 |
| 并发序列数 | `max_num_seqs` | 同时处于 running 的序列数 | 控制 batch 维度，与 CUDA Graph 的分档配合 |

prefill 请求消耗大量 token 预算但只占 1 个序列位；decode 请求各消耗 1 个 token 预算。因此这两个预算分别主要约束 prefill 与 decode，对应第 04 章的结论：prefill 按 token 调度，decode 按序列调度。

### 2.2 显存约束

除两个预算外还有显存：每个进入 running 的请求需要 block。调度器必须在决定 batch 之前确认 block 够用，因为没有缺页机制（第 06 章）。

vLLM 的做法是在 `can_allocate` 中做三态判断：

| 结果 | 含义 | 处理 |
|---|---|---|
| OK | 当前空闲块足够 | 分配并加入 batch |
| LATER | 当前不够，但理论上可满足 | 留在队列，下一步再试 |
| NEVER | 请求需要的块数超过池总量 | 直接拒绝，返回错误 |

第三种情况对应 prompt 长度超过引擎配置的上下文上限，必须明确拒绝而不是无限等待。

---

## 3. 队列策略

### 3.1 先来先服务

默认策略。实现简单，无饥饿，但不区分请求代价：一个 8000 token 的 prompt 会阻塞后面 100 个 50 token 的短请求。

### 3.2 最短作业优先

按 prompt 长度排序。模拟结果（过载场景，到达率 30 请求/秒）：

| 策略 | 短请求 TTFT | 中等请求 TTFT | 长请求 TTFT | goodput |
|---|---|---|---|---|
| 先来先服务 | 9.00 s | 8.12 s | 4.54 s | 40 token/s |
| 最短作业优先 | 0.19 s | 0.92 s | 17.57 s | 289 token/s |

短请求的 TTFT 改善 47 倍，goodput 提高 7 倍，代价是长请求的 TTFT 从 4.54 秒恶化到 17.57 秒。这是典型的饥饿：**总体指标改善，部分请求显著变差**。

注意先来先服务下长请求的 TTFT 反而比短请求好（4.54 对 9.00）。原因是长请求占用 token 预算多，一旦被调度就会独占整步，而短请求要和其他短请求竞争队列位置。这个反直觉的现象说明队列指标必须分组看。

生产环境使用最短作业优先需要配合防饥饿机制：为每个请求维护等待时间，超过阈值后提升优先级。

### 3.3 优先级与公平

多租户场景需要按租户分配容量。常见做法：

| 机制 | 说明 |
|---|---|
| 静态优先级 | 高优先级请求插队。简单，但低优先级可能长期饥饿 |
| 加权公平队列 | 按权重分配调度机会，保证每个租户的最低份额 |
| 令牌桶限流 | 在网关层限制每个租户的请求速率，引擎内部不区分 |
| 虚拟时间 | 记录每个租户已消耗的 GPU 时间，优先调度消耗少的 |

工程上限流通常放在网关层（第 16 章），引擎内保持简单。原因是引擎内的公平性难以定义：一个请求消耗的资源与其 prompt 长度、输出长度、占用时长都相关，而输出长度不可预知。

---

## 4. 准入控制

### 4.1 为什么需要

无限制地接纳新请求会导致：显存耗尽 → 抢占运行中的请求 → 被抢占的请求重新排队并重新 prefill → 更多请求积压 → 更多抢占。这是一个正反馈，系统进入抖动状态，有效吞吐下降。

### 4.2 水位机制

在空闲块低于阈值（例如总量的 15%）时暂停接纳新的 prefill，只让运行中的请求继续 decode。模拟结果（过载场景）：

| 策略 | 吞吐 | TTFT P50 | goodput | 抢占次数 |
|---|---|---|---|---|
| 先来先服务 | 1183 token/s | 8.12 s | 40 token/s | 50 |
| 加准入控制 | 1234 token/s | 7.36 s | 68 token/s | 0 |

抢占从 50 次降到 0，吞吐与 goodput 都提高。代价是请求在队列里等待的时间变长——**准入控制把等待从显存里挪到了队列里**，但队列里等待不消耗显存，因此系统整体效率更高。

水位阈值的选择：太高则并发不足，太低则起不到保护作用。经验值在 10%-20%，需要按负载的输出长度分布调整（输出越长，单个请求占用 block 的增长越快，水位应当越高）。

---

## 5. chunked prefill 的效果

第 04 章给出了 chunked prefill 的静态代价分析。放到完整调度中，效果依赖负载状态。模拟结果：

| 场景 | 策略 | TTFT P50 | ITL P99 | goodput | 抢占 |
|---|---|---|---|---|---|
| 接近饱和 | prefill 优先 | 0.09 s | 342.1 ms | 1068 | 0 |
| 接近饱和 | chunked prefill | 0.13 s | 188.9 ms | 1066 | 0 |
| 过载 | prefill 优先 | 8.12 s | 374.3 ms | 40 | 50 |
| 过载 | chunked prefill | 7.81 s | 626.5 ms | 25 | 73 |

接近饱和时 chunked prefill 把 ITL P99 从 342 ms 降到 189 ms，符合预期。

过载时结论反转：ITL P99 从 374 ms 升到 627 ms，抢占次数从 50 升到 73。原因是 chunked prefill 让每个请求的 prefill 需要更多步才能完成，请求在系统中停留时间变长，占用显存的时间变长，从而触发更多抢占，而抢占带来的重新 prefill 又进一步加剧拥塞。

**这说明单项优化的效果依赖系统状态**。chunked prefill 是为了平滑 ITL 而设计的，它假设系统有余量；在过载状态下应当先用准入控制把系统拉回有余量的区间，再讨论 ITL 的平滑。

chunk 大小的扫描（过载场景）：

| chunk | 吞吐 | TTFT P50 | ITL P99 | goodput |
|---|---|---|---|---|
| 256 | 924 | 11.25 s | 466.6 ms | 20 |
| 512 | 1042 | 8.46 s | 419.7 ms | 22 |
| 1024 | 1144 | 7.81 s | 626.5 ms | 25 |
| 2048 | 1171 | 8.33 s | 531.6 ms | 39 |
| 4096 | 1184 | 7.94 s | 419.8 ms | 40 |
| 8192 | 1194 | 7.61 s | 628.0 ms | 40 |

过载状态下 chunk 越大越好，因为此时的目标是尽快清空队列，而不是平滑 ITL。

---

## 6. 调度器本身的开销

一步 decode 只需 10-20 毫秒，而调度决策与输入张量准备在 Python 中执行。当并发达到上百时，这部分开销可达毫秒级，占单步耗时的 10% 以上。

四个来源：

| 来源 | 说明 | 优化 |
|---|---|---|
| 队列扫描 | 每步遍历 waiting 与 running | 增量维护，避免全量扫描 |
| block table 构造 | 每步为每个序列构造映射张量 | 增量更新，只改变化的部分 |
| 张量拼接与拷贝 | CPU 到 GPU 的传输 | 用 pinned memory，异步拷贝 |
| Python 解释开销 | 循环与对象访问 | 下沉到 C++，或减少每步的对象操作 |

对应的架构手段是**把调度与执行重叠**：GPU 执行第 N 步时，CPU 已经在准备第 N+1 步。这要求调度决策不依赖第 N 步的输出，而实际上采样结果决定哪些序列结束，因此需要先按推测准备、事后修正。vLLM V1 的异步调度即为此设计。

---

## 7. 引擎实现对照

| 引擎 | 调度器的关键设计 |
|---|---|
| vLLM V0 | `Scheduler` 维护 waiting / running / swapped 三个队列；一步要么全 prefill 要么全 decode |
| vLLM V1 | 取消 swapped 队列，统一为 waiting / running；默认混合 prefill 与 decode；调度逻辑简化为「每个请求本步处理多少 token」的字典 |
| SGLang | `Scheduler` 支持多种策略（`--schedule-policy`），包括基于基数树命中长度的排序 |
| TensorRT-LLM | in-flight batching，由 `Executor` 管理，策略配置项较少 |

V1 的关键简化：调度结果不再区分请求处于哪个阶段，而是统一表示为 `{请求 id: 本步要处理的 token 数}`。prefill 请求的值是一个 chunk，decode 请求的值是 1。这使得混合执行成为默认路径，代码路径唯一。

SGLang 的按前缀命中长度排序值得注意：优先调度命中前缀最长的请求，可以最大化缓存复用并减少总计算量。这是把第 07 章的机制反馈到调度层。

---

## 8. 代码

`code/ch08_scheduler.py` 在同一负载上运行五种调度策略，在两个负载状态（接近饱和、过载）下对比：

- 吞吐、TTFT 的 P50 与 P99、ITL 的 P50 与 P99、goodput、抢占次数
- 按 prompt 长度分组的 TTFT，用于检查饥饿
- chunk 大小的扫描

运行：

```bash
python3 code/ch08_scheduler.py
```

goodput 的定义：TTFT ≤ 2 秒且该请求的平均 ITL ≤ 50 毫秒的请求所贡献的输出 token 数除以总时长。修改 `SLO_TTFT` 与 `SLO_TPOT` 会改变各策略的排名——这正是要说明的：**没有绝对最优的调度策略，只有在给定 SLO 下最优的策略**。

建议的实验：

- 把 `SLO_TPOT` 从 50 ms 放宽到 200 ms，观察 chunked prefill 的优势消失（因为它的收益就是压低 ITL）。
- 把准入控制的水位从 0.15 改为 0.05 与 0.30，找到抢占次数与 TTFT 的平衡点。
- 把输出长度分布从指数分布改为固定值，观察静态批处理与连续批处理的差距缩小——差距的来源正是输出长度的方差。
