# 09 · 抢占与公平性

第 08 章的调度器假设显存够用。本章处理不够用的情况：中止哪个请求，中止后怎么恢复，以及如何避免因此产生的饥饿。

---

## 1. 抢占不是优化，是正确性要求

第 01 章的模拟器在没有抢占机制时会死锁：运行中的序列需要新 block 才能继续 decode，等待中的请求需要 block 才能开始，池已空，两边都无法推进。

死锁的成因是分页机制没有缺页处理（第 06 章）。操作系统遇到内存不足可以换页并让程序继续；推理引擎遇到 block 不足时，正在执行的请求无法「暂停一下」——它必须要么拿到 block，要么被完整地移出运行状态。

因此每个引擎都必须实现抢占。差别只在于策略。

---

## 2. 两种恢复方式

被抢占的请求，其已有的 KV 有两种处理：

| 方式 | 做法 | 恢复时 |
|---|---|---|
| swap（换出） | 把 KV 拷贝到主机内存 | 拷回显存，继续 decode |
| recompute（重算） | 直接丢弃 KV | 重新做一次 prefill |

### 2.1 直接代价

第 6 节的代码计算（Llama-3-8B，A100，PCIe Gen4 单向 64 GB/s）：

| 序列长度 | KV 大小 | 换出+换入 | 重算 | 比值 |
|---|---|---|---|---|
| 1024 | 134 MB | 4.2 ms | 78.6 ms | 18.7 |
| 4096 | 537 MB | 16.8 ms | 323.3 ms | 19.3 |
| 8192 | 1074 MB | 33.6 ms | 684.9 ms | 20.4 |
| 32768 | 4295 MB | 134.2 ms | 3700.4 ms | 27.6 |

两者都与序列长度近似成正比，因此比值基本恒定。单看这张表，换出快 20 倍以上。

链路带宽的影响：

| 链路 | 带宽 | 8K 序列的换出+换入 |
|---|---|---|
| PCIe Gen3 | 16 GB/s | 134.2 ms |
| PCIe Gen4 | 64 GB/s | 33.6 ms |
| PCIe Gen5 | 128 GB/s | 16.8 ms |
| NVLink | 600 GB/s | 3.6 ms |

### 2.2 为什么实际选择重算

上表低估了重算的优势，有两个原因：

**block 不会立即失效。** 被抢占的请求释放 block 后，这些 block 进入可复用池（第 07 章的三态之一），内容仍然有效。若在显存压力缓解之前它们没有被其他请求覆盖，该请求重新调度时前缀 100% 命中，重算代价为 0。

**重算可以与其他 prefill 合并。** 它不是独占一步，而是加入正在进行的 prefill 批次，权重只读一遍，边际代价远低于表中按独占计算的数值。

换出还有两项表中没有的代价：

- **主机内存占用**：换出 100 个 8K 序列需要 107 GB 主机内存。
- **同步阻塞**：换入必须在该请求恢复计算之前完成，这段时间该请求什么也做不了；而重算本身就是计算，可以直接混入正常批次。

因此 vLLM V1 取消了 swap 路径，只保留重算。V0 中保留 swap 是因为当时前缀缓存还不是默认开启。

---

## 3. 抢占对象的选择

需要腾出空间时牺牲谁。代码中对 40 个候选序列的对比（需要腾出 60 个 block）：

| 策略 | 牺牲数 | 腾出的 block | 需重算的 token | 重算代价 |
|---|---|---|---|---|
| 最晚加入优先 | 1 | 448 | 7169 | 590.9 ms |
| 最长序列优先 | 1 | 496 | 7951 | 662.6 ms |
| 最短序列优先 | 4 | 77 | 1263 | 101.4 ms |
| 剩余输出最多优先 | 2 | 443 | 7097 | 577.7 ms |
| 剩余输出最少优先 | 1 | 482 | 7717 | 640.9 ms |

三个目标互相冲突：

| 目标 | 倾向 | 问题 |
|---|---|---|
| 最小化重算代价 | 牺牲短序列 | 要牺牲多个请求，影响面大；且短序列腾出的空间少，可能不够 |
| 最小化影响的请求数 | 牺牲长序列 | 重算代价最高 |
| 最大化腾出空间的持续时间 | 牺牲剩余输出最多的 | 输出长度不可预知，无法准确判断 |

vLLM 采用最晚加入优先（running 队列的尾部）。理由：

1. 保证先到的请求先完成，避免长期饥饿
2. 实现简单，running 队列本身就按加入顺序排列
3. 与准入控制配合时，最晚加入的往往就是刚刚被接纳的，抢占它相当于撤销一个错误的接纳决策

---

## 4. 抢占的连锁效应

抢占的直接代价是重算，间接代价是它占用的这段时间里整个 batch 都在等。

| 被抢占序列的长度 | 重算代价 | 等价的 decode 步数 | batch 32 时系统少产出的 token |
|---|---|---|---|
| 1000 | 76.7 ms | 3.0 | 95 |
| 4000 | 315.3 ms | 12.2 | 392 |
| 8000 | 667.1 ms | 25.9 | 829 |
| 16000 | 1485.8 ms | 57.7 | 1847 |

抢占一个 16000 token 的序列，代价相当于系统少产出 1847 个 token。

更严重的是正反馈：抢占 → 该请求重新排队 → 队列变长 → 更多请求积压 → 显存竞争加剧 → 更多抢占。第 08 章的模拟中，加入准入控制后抢占从 50 次降到 0，goodput 从 40 升到 68。

**结论：抢占应当被视为异常而非常态。** 监控中抢占次数应当是一级指标，持续大于 0 说明容量不足或准入控制的水位设置不当。

---

## 5. 饥饿与公平

### 5.1 饥饿的来源

| 来源 | 表现 |
|---|---|
| 最短作业优先 | 长请求持续被插队，第 08 章测得长请求 TTFT 从 4.5 秒恶化到 17.6 秒 |
| 反复抢占同一个请求 | 请求进入「运行—被抢占—重排队—运行」的循环，永不完成 |
| 静态优先级 | 低优先级请求在高优先级持续到达时永远得不到调度 |

第二种最隐蔽：若抢占策略是最晚加入优先，而某个请求每次刚被调度就因显存不足被抢占，它会反复重算而不前进。防护手段是记录每个请求的被抢占次数，超过阈值后禁止再次抢占它（提升为不可抢占），或直接返回错误。

### 5.2 公平的定义困难

「公平」在推理服务中难以定义，因为一个请求消耗的资源由三个不可预知的量决定：

```
资源消耗 ≈ prompt 长度（已知）
         × 输出长度（未知）
         × 在系统中的停留时长（取决于并发状况）
```

只有第一项在调度时可知。因此引擎内部通常不实现复杂的公平机制，而是：

- 引擎内部保持先来先服务，只保证不饥饿
- 配额与限流放在网关层，按租户限制请求速率与 token 速率（第 16 章）
- 需要隔离时用独立实例，而不是在同一实例内做公平调度

### 5.3 优先级的实现

若必须支持优先级，最小可用的设计：

```
1. 请求携带优先级字段
2. waiting 队列按 (优先级, 到达时间) 排序
3. 抢占时优先牺牲低优先级的运行中请求
4. 为每个请求维护等待时长，超过阈值后提升优先级（防饥饿）
5. 记录每个优先级的 TTFT 分位数，作为 SLO 的分级依据
```

第 4 步是必需的，否则低优先级在高负载下会完全无法完成。

---

## 6. 引擎实现对照

| 引擎 | 抢占机制 |
|---|---|
| vLLM V0 | 支持 swap 与 recompute，`preemption_mode` 可配置；维护 swapped 队列 |
| vLLM V1 | 只有 recompute；被抢占的请求回到 waiting 队列头部；日志中会打印抢占告警 |
| SGLang | retract（回撤）机制，与 recompute 类似；按剩余长度选择牺牲对象 |
| TensorRT-LLM | 支持 KV block 的 offload，策略配置项较少 |

vLLM 在发生抢占时会打印一条包含累计抢占次数的日志，并建议调低 `max_num_seqs` 或调高 `gpu_memory_utilization`。看到这条日志说明当前配置的并发超出了显存能支撑的水平。

---

## 7. 代码

`code/ch09_preemption.py` 包含：

1. 换出与重算的代价曲线，以及链路带宽的影响
2. 前缀缓存命中率对重算代价的影响
3. 五种牺牲对象选择策略的对比
4. 抢占的连锁效应量化

运行：

```bash
python3 code/ch09_preemption.py
```

建议的实验：

- 把 `PCIE_BW` 改为 `NVLINK_BW`，对应把 KV 换到同机另一张卡而非主机内存。这是 PD 分离中 KV 传输的代价模型（第 10 章）。
- 在第 2 节中调整命中前缀的比例，找到重算与换出的分界点。这个分界点随负载的前缀重复度移动，说明抢占策略与前缀缓存是耦合的设计。
