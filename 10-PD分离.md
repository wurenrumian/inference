# 10 · chunked prefill 与 PD 分离

prefill 与 decode 的特性相反（第 04 章），放在同一张卡上执行会互相干扰。本章讨论两种处理方式：在单实例内切分，或者用不同实例分别承担。

---

## 1. 干扰的量化

聚合部署下，一次 prefill 会阻塞整个 decode 批次。以输入 4096、batch 32 为例：

```
一次 prefill（4096 token）  : 323.3 ms
一步 decode（batch 32）     : 25.7 ms
混在同一步                  : 349.1 ms，ITL 放大 13.6 倍
```

更严重的是资源占用比例。prefill 每处理一个请求需要 323 ms，因此：

| 请求到达率 | prefill 占用的时间比例 | 留给 decode 的时间 |
|---|---|---|
| 1 req/s | 32.3% | 67.7% |
| 2 req/s | 64.7% | 35.3% |
| 3.1 req/s | 100% | 0% |

**到达率超过 3.1 请求/秒时，这张卡已经没有时间做 decode**。此时无论 decode 侧还有多少显存余量都用不上。聚合部署的这个上限由输入长度决定，且无法通过配置调整。

---

## 2. chunked prefill

### 2.1 机制

把一个长 prompt 的 prefill 切成若干块，每步只处理一块，与 decode 请求混在同一批执行。

```
第 k 步：decode 32 个序列的各 1 个 token + prefill 请求的第 k 个 chunk
```

关键点是**这一步的 token 总数受 `max_num_batched_tokens` 控制**，因此单步耗时可以被限制在一个稳定范围内。

### 2.2 代价

第 04 章的数据（32768 token 的 prompt，与 32 个 decode 序列共存）：

| chunk | 步数 | 最大单步耗时 | prefill 总耗时 | 相对一次性完成 |
|---|---|---|---|---|
| 32768 | 1 | 3720.0 ms | 3720.0 ms | 1.00x |
| 2048 | 16 | 328.3 ms | 4044.1 ms | 1.09x |
| 512 | 64 | 99.2 ms | 5081.2 ms | 1.37x |
| 256 | 128 | 60.5 ms | 6464.1 ms | 1.74x |

代价来自两处：每个 chunk 都要重新读一遍模型权重；每个 chunk 的 attention 都要读一遍已有的全部前缀 KV。前者在 chunk 较小时主导。

### 2.3 效果依赖负载状态

第 08 章的完整调度模拟给出了一个重要结论：

| 场景 | 策略 | TTFT P50 | ITL P99 | goodput |
|---|---|---|---|---|
| 接近饱和 | prefill 优先 | 0.09 s | 342.1 ms | 1068 |
| 接近饱和 | chunked prefill | 0.13 s | 188.9 ms | 1066 |
| 过载 | prefill 优先 | 8.12 s | 374.3 ms | 40 |
| 过载 | chunked prefill | 7.81 s | 626.5 ms | 25 |

接近饱和时 chunked prefill 把 ITL P99 降低 45%；过载时反而恶化，因为它延长了每个请求在系统中的停留时间，加剧了显存竞争与抢占。

**chunked prefill 是一个平滑手段，不是扩容手段。** 使用它的前提是系统有余量。

### 2.4 chunk 大小的选择

| 目标 | 建议 |
|---|---|
| ITL 优先（对话、流式输出） | 512-1024 |
| TTFT 优先（批处理、离线任务） | 4096 或关闭 |
| 平衡 | 2048 |

选择方法是压测：固定负载，扫描 chunk 大小，找 goodput 的最大值。goodput 的定义依赖 SLO，因此这个最优值不能从别人的配置照搬。

---

## 3. PD 分离

### 3.1 动机不只是干扰

消除干扰只是 PD 分离的收益之一，且 chunked prefill 也能部分做到。真正无法用聚合部署实现的是**独立调整资源配比**。

计算方法：分别算出单个实例的 prefill 能力与 decode 能力，实例数与能力成反比。

| 场景 | 输入 | 输出 | P 实例吞吐 | D 实例吞吐 | 需要的 P : D |
|---|---|---|---|---|---|
| 对话 | 2048 | 512 | 6.34 req/s | 3.07 req/s | 1 : 2.1 |
| 长文摘要 | 32768 | 512 | 0.27 req/s | 0.41 req/s | 1.5 : 1 |
| Agent | 16384 | 256 | 0.65 req/s | 1.64 req/s | 2.5 : 1 |
| 代码补全 | 8192 | 32 | 1.46 req/s | 31.92 req/s | 21.9 : 1 |

配比跨越 40 倍以上的范围。聚合部署相当于强制 1:1，在代码补全这类负载下会浪费大量 decode 能力。

### 3.2 两侧可以用不同的配置

分离之后，两类实例可以各自优化：

| 项 | prefill 实例 | decode 实例 |
|---|---|---|
| 瓶颈 | 算力 | 显存带宽与容量 |
| 优选硬件 | 算力高的卡 | 显存大、带宽高的卡 |
| 并行方式 | 张量并行降低单次延迟 | 更倾向于大 batch |
| batch 策略 | 按 token 预算 | 按序列数，尽量大 |
| 量化收益 | 有限（计算受限） | 显著（访存受限） |
| KV 池大小 | 小，只需容纳在算的请求 | 大，容纳全部运行中请求 |

这一列差异是分离的根本理由：**两类工作对硬件的要求不同，混在一起只能按其中一类配置**。

### 3.3 架构

```
请求 → 路由 → prefill 实例
                 │
                 │ 传输 KV cache
                 ▼
              decode 实例 → 流式返回 token
```

需要额外解决的问题：

| 问题 | 说明 |
|---|---|
| KV 传输 | 见第 4 节 |
| 路由 | 需要知道哪个 decode 实例有余量，且要考虑前缀缓存的位置（第 16 章） |
| 首 token 的归属 | prefill 产出第一个 token，需要从 prefill 侧返回给客户端 |
| 故障处理 | decode 实例故障时，prefill 的结果作废；需要重试逻辑 |
| 显存配置不同 | 两侧的 block 布局与 block_size 必须一致，否则无法直接传输 |

---

## 4. KV 传输

### 4.1 传输量

传输的是整个 prompt 的 KV，大小为「每 token 的 KV 单价 × prompt 长度」。Llama-3-8B 为 128 KB/token，因此 8192 token 的 prompt 需要传 1.07 GB。

各链路的传输耗时：

| 链路 | 2K 序列 | 8K 序列 | 32K 序列 | 128K 序列 |
|---|---|---|---|---|
| 以太网 25Gb | 85.9 ms | 343.6 ms | 1374.4 ms | 5497.6 ms |
| 以太网 100Gb | 21.5 ms | 85.9 ms | 343.6 ms | 1374.4 ms |
| RDMA 200Gb | 10.7 ms | 42.9 ms | 171.8 ms | 687.2 ms |
| RDMA 400Gb | 5.4 ms | 21.5 ms | 85.9 ms | 343.6 ms |
| NVLink | 0.4 ms | 1.8 ms | 7.2 ms | 28.6 ms |

### 4.2 判据

传输耗时占 prefill 耗时的比例：

| 链路 | 2K | 8K | 32K | 128K |
|---|---|---|---|---|
| 以太网 25Gb | 54% | 50% | 37% | 18% |
| 以太网 100Gb | 14% | 13% | 9% | 5% |
| RDMA 200Gb | 7% | 6% | 5% | 2% |
| RDMA 400Gb | 3% | 3% | 2% | 1% |

25Gb 以太网上传输耗时接近 prefill 计算本身，分离得不偿失。**200Gb 以上的 RDMA 是 PD 分离的实际门槛**。

注意占比随序列变长而下降，因为 prefill 的计算量与长度的平方相关，而传输量只与长度线性相关。这意味着**长上下文场景对链路的要求反而更宽松**。

### 4.3 逐层流水

KV 是逐层产生的，第 1 层算完就可以开始传第 1 层的 KV，不必等 32 层全部算完。

序列长度 8192、prefill 计算 684.9 ms 时：

| 链路 | 传输耗时 | 串行 TTFT | 逐层流水 TTFT | 改善 |
|---|---|---|---|---|
| 以太网 25Gb | 343.6 ms | 1028.5 ms | 695.7 ms | 32.4% |
| 以太网 100Gb | 85.9 ms | 770.8 ms | 687.6 ms | 10.8% |
| RDMA 200Gb | 42.9 ms | 727.9 ms | 686.3 ms | 5.7% |
| NVLink | 1.8 ms | 686.7 ms | 685.0 ms | 0.3% |

流水之后只有最后一层的传输暴露在关键路径上，传输时间被计算掩盖。链路越慢收益越大。

代价是把 1 次大传输变成 32 次较小的传输，对链路的小包性能与发起开销有要求。RDMA 的 RC 传输在这种模式下优于基于 TCP 的实现：它避免了每次传输的系统调用、内核态拷贝与协议栈处理，单次发起的开销在微秒级。

这是 RDMA 在推理系统中的主要应用场景，与存储系统中的用法（大块数据的单边读写）不同：这里的模式是高频的中等大小消息，且对尾延迟敏感。

---

## 5. 适用条件

PD 分离需要同时满足：

| 条件 | 说明 |
|---|---|
| 规模 | 至少 4 卡以上。2 卡分离后每侧只剩 1 卡，失去批处理收益 |
| 链路 | 200Gb 以上 RDMA，或同机 NVLink |
| 负载稳定 | 输入输出长度分布稳定，否则配比需要频繁调整 |
| SLO 严格 | 对 ITL 毛刺敏感。若只关心吞吐，聚合部署更简单 |
| 运维能力 | 需要额外的路由、传输、故障处理逻辑 |

不满足时的替代方案是聚合部署加 chunked prefill：部署复杂度不变，在接近饱和的负载下能把 ITL P99 降低约 45%。

---

## 6. 引擎实现对照

| 引擎 | PD 分离方案 |
|---|---|
| vLLM | 通过 KV connector 接口对接外部传输层（如 NIXL、LMCache、Mooncake） |
| SGLang | 内置 PD 分离模式，配合 Mooncake 传输后端 |
| TensorRT-LLM | 有独立的 disaggregated serving 方案 |
| Mooncake | 专门的 KV 传输与池化层，支持跨实例的 KV 复用 |

演进方向是把 KV 从「实例私有的显存」变为「集群共享的存储层」：prefill 产出的 KV 写入一个跨节点的 KV 池，decode 实例按需读取，同一份 KV 可以被多个请求命中。这把第 07 章的前缀缓存从单实例扩展到集群范围，代价是对存储与网络的要求显著上升。

---

## 7. 代码

`code/ch10_disagg.py` 包含：

1. 聚合部署下 prefill 对 decode 时间的挤占
2. 四种负载下的 P : D 配比计算
3. 六种链路的 KV 传输耗时与占比
4. 逐层流水的收益（按链路）
5. 适用条件清单

运行：

```bash
python3 code/ch10_disagg.py
```

建议的实验：

- 改变第 2 节的输入输出长度，观察配比的变化幅度。这个比例就是容量规划中两类实例的数量比，也是回答「我们的负载适不适合 PD 分离」的依据。
- 第 3 节的占比表是选链路的依据。若占比超过 20%，分离带来的收益会被传输开销抵消。
