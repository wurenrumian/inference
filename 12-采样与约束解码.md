# 12 · 采样与约束解码

模型的前向输出是 logits，不是 token。从 logits 到 token 这一步决定了输出的质量与可控性，也是引擎中形状最不规则、最容易成为瓶颈的部分。

---

## 1. 位置与形状

一步前向的输出是 `[batch, 词表大小]` 的 logits。词表大小在 3 万到 15 万之间，因此这个张量本身可达数十 MB：

```
batch 128，词表 128256，fp32：128 × 128256 × 4 = 65.7 MB
```

对它的每一次遍历都是一次几十 MB 的访存。采样阶段若实现不当（例如逐请求循环、频繁的 CPU-GPU 同步），可以占到单步耗时的 10% 以上。

采样只作用于 logits，不影响 KV cache。这是第 07 章「采样参数不同的请求可以共享前缀」的原因。

---

## 2. logits 处理链

### 2.1 常见处理器与顺序

| 处理器 | 作用 | 参数 |
|---|---|---|
| logit bias | 直接给指定 token 加减分数 | 字典 |
| 重复惩罚 | 降低已出现 token 的分数 | presence / frequency / repetition penalty |
| 结构化约束掩码 | 把不合法 token 置为 -inf | 语法或 schema |
| 温度缩放 | 除以温度，控制分布的尖锐程度 | temperature |
| top-k | 只保留分数最高的 k 个 | k |
| top-p（核采样） | 保留累积概率达到 p 的最小集合 | p |
| min-p | 保留概率不低于「最大概率 × p」的 token | p |

代码中的执行示例（原始分布有 32 个候选）：

| 步骤 | 候选数 | 最大概率 |
|---|---|---|
| 原始 | 32 | 0.4066 |
| 重复惩罚 1.2 | 32 | 0.3202 |
| 温度 0.7 | 32 | 0.4264 |
| top-k = 5 | 5 | 0.4432 |
| top-p = 0.9 | 3 | 0.4738 |

**顺序有影响**。温度必须在 top-k 与 top-p 之前应用，因为后两者要基于缩放后的概率判断截断位置。不同引擎的处理顺序若不一致，同样的参数会产生不同的输出分布——这是「同一个模型在两个引擎上表现不同」的常见原因之一。

### 2.2 参数的效果

温度的影响（不做截断）：

| 温度 | 最大概率 | 分布的熵 |
|---|---|---|
| 0.0 | 1.0000 | 0.000 |
| 0.2 | 0.9184 | 0.305 |
| 0.5 | 0.6547 | 0.919 |
| 1.0 | 0.4066 | 1.892 |
| 2.0 | 0.1718 | 3.068 |

温度 0 等价于贪心解码。实现上不能真的除以 0，而是直接取 argmax。

top-p 的影响：

| top_p | 保留的候选数 | 被截断的概率质量 |
|---|---|---|
| 0.50 | 2 | 0.3468 |
| 0.80 | 3 | 0.1972 |
| 0.90 | 10 | 0.0994 |
| 0.95 | 21 | 0.0497 |
| 1.00 | 32 | 0.0000 |

top-p 的候选数随分布形状自适应：分布尖锐时保留很少，平坦时保留很多。这是它优于 top-k 的地方——top-k 的 k 是固定的，在模型很确定时会引入不必要的随机性，在模型不确定时又限制过强。

min-p 是更新的方案：按「最大概率的比例」截断，兼具自适应性且计算比 top-p 便宜（不需要排序与累加，只需一次比较）。

---

## 3. 结构化输出

### 3.1 机制

要求输出符合 JSON Schema、正则表达式或某个语法时，做法不是生成后校验重试，而是在每一步限制可选的 token 集合。

```
1. 把语法编译为状态机（FSM）或下推自动机
2. 每一步根据当前状态计算允许的 token 集合
3. 构造一个词表大小的掩码，不允许的位置置为 -inf
4. 掩码加到 logits 上，采样
5. 用采样出的 token 驱动状态机转移
```

由于不合法 token 的概率被置为 0，**输出从根本上不可能违反语法**，不需要重试。

代码中用一个只接受 `{"ab":"cd"}` 形式的最简状态机演示了这个过程，输出显示了每一步的状态、允许的 token 数量与实际采样结果。

### 3.2 代价

| 代价 | 说明 | 缓解 |
|---|---|---|
| 状态机编译 | 复杂 schema 的编译可能耗时数百毫秒 | 按 schema 缓存编译结果 |
| 每步的掩码计算 | 需要查当前状态的允许集合 | 预计算「状态 → 掩码」的表 |
| 掩码的显存与带宽 | 词表 15 万时每请求每步 600 KB | 用位图存储；只在约束生效时计算 |
| 与模型倾向冲突 | 模型想输出的 token 被禁止，可能降低质量 | 让 schema 与模型的自然输出格式接近 |

第四项容易被忽略：强制约束不改变模型的能力，只改变它的输出形式。若 schema 与模型训练时见过的格式差异很大，输出内容的质量会下降，表现为字段填写敷衍或语义错误。

### 3.3 主流实现

| 方案 | 机制 |
|---|---|
| Outlines | 把正则或 JSON Schema 编译为 FSM，预计算状态到允许 token 的映射 |
| XGrammar | 上下文无关文法，用下推自动机；针对掩码计算做了大量优化 |
| llguidance | 类似，强调低延迟 |
| 引擎内置 | vLLM 与 SGLang 都集成了上述一种或多种作为后端 |

---

## 4. 批内参数不一致

同一个 batch 中不同请求的采样参数不同，而 logits 是一个 `[batch, 词表]` 的张量。三种处理方式：

| 方式 | 说明 | 问题 |
|---|---|---|
| 逐请求循环 | 每行单独处理 | kernel 启动次数与 batch 成正比 |
| 分组批处理 | 参数相同的请求分为一组 | 组数不定，最坏退化为逐请求 |
| 全向量化 | 参数本身做成张量，每行一个值 | 实现复杂，top-k 尤其麻烦 |

引擎倾向于第三种。top-k 的难点在于每行的 k 不同，不能直接调用 topk 算子，需要先排序再按行索引取阈值。

### 4.1 确定性问题

贪心解码（温度 0）的结果理应可复现，但在批处理推理中不是天然成立的：

- batch 大小变化会改变矩阵乘的归约顺序
- 归约顺序改变导致浮点结果有微小差异
- 当两个 token 的 logits 非常接近时，argmax 的结果可能不同

因此「相同输入相同输出」需要专门的确定性配置（固定 batch、固定 kernel、禁用某些优化），代价是性能下降。这是评测复现中的一个常见困惑来源：同样的 prompt 与 temperature=0，两次请求得到不同结果，原因不是随机性，而是并发状况不同。

---

## 5. 其他相关处理

### 5.1 stop 字符串

结束条件中的 stop 字符串必须在文本层判断，因为 token 与字符不是一一对应。实现要点：

- 增量解码出文本后做匹配
- 一个 stop 字符串可能跨越两个 token，需要保留少量历史
- 命中后需要截断已经多输出的部分
- 流式返回时，可能已经把部分内容发给客户端，需要设计为延迟发送（保留一个与最长 stop 字符串等长的缓冲）

### 5.2 增量 detokenize

一个 UTF-8 字符可能由多个 token 编码（中文、emoji 尤其常见），单独解码某个 token 会得到替换字符。做法是维护每个序列已解码的文本前缀，每次解码时带上少量历史 token 再取差值。

### 5.3 logprobs

返回每个 token 的对数概率需要额外的排序与传输。请求 top-20 logprobs 时，每步每请求要传回 20 个 (token, logprob) 对，在高并发下这部分的 CPU 与网络开销可观。生产环境通常限制其使用。

---

## 6. 引擎实现对照

| 引擎 | 采样的组织 |
|---|---|
| vLLM V1 | `vllm/v1/sample/`，`Sampler` 与 `LogitsProcessor` 分离；参数打包为张量做向量化处理 |
| SGLang | `python/sglang/srt/layers/sampler.py`；结构化输出集成 XGrammar 与 Outlines |
| TensorRT-LLM | 采样在 plugin 中实现，参数按 batch 传入 |

vLLM V1 的采样重构方向是减少 CPU-GPU 同步：V0 中每步都要把采样结果拷回 CPU 判断结束条件，V1 尽量把这些判断留在 GPU 上或异步进行。这类优化在单步只有十几毫秒时是必要的。

---

## 7. 代码

`code/ch12_sampling.py` 包含：

1. 完整的 logits 处理链实现（温度、top-k、top-p、min-p、重复惩罚）
2. 参数对候选集合与分布熵的影响
3. 一个 JSON 对象的状态机，逐步生成词表掩码并采样
4. 批内参数不一致的三种实现方式说明

运行：

```bash
python3 code/ch12_sampling.py
```

建议的实验：

- 调换处理顺序（先 top-k 再温度），观察候选集合的变化。这能说明为什么引擎必须固定顺序。
- 把 logits 改为强烈偏向某个非法 token，观察掩码如何强制改变输出。这对应第 3.2 节的第四项代价。
