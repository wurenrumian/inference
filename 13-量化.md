# 13 · 量化

量化是唯一同时降低显存占用与访存量的手段，因此在 decode 阶段收益最直接。代价是精度损失，而精度损失的大小取决于方案细节。本章给出可测量的对比。

---

## 1. 分类

按量化对象分：

| 对象 | 记法 | 收益 | 主要影响 |
|---|---|---|---|
| 权重 | W8、W4 | 减少权重访存与显存 | decode 变快，显存腾出来给 KV |
| 激活 | A8、A16 | 可以用低精度乘法单元 | prefill 也受益 |
| KV cache | 单独标注 | 减少 KV 访存与容量 | 长序列大 batch 场景收益最大 |

常见组合：W8A16（只量化权重）、W8A8（SmoothQuant 类）、W4A16（GPTQ、AWQ 的典型配置）、fp8 全量化。

按位宽分：int8、int4、fp8（e4m3 与 e5m2）、更激进的 int2 与二值化（实用性有限）。

---

## 2. 量化的基本形式

```
对称量化   : q = round(x / scale)，   反量化 x' = q × scale
非对称量化 : q = round(x / scale) + zp，反量化 x' = (q - zp) × scale
```

`scale` 由该组数据的最大绝对值（对称）或最大最小值（非对称）决定。**分组的粒度是最关键的设计选择**。

### 2.1 粒度对误差的影响

代码在 4096 × 4096 的正态分布权重上的测量：

| 方案 | 位宽 | 相对误差 | 缩放参数密度 |
|---|---|---|---|
| per-tensor | 8 | 0.01204 | 1 个/整个张量 |
| per-channel | 8 | 0.00868 | 1 个/行 |
| per-group 128 | 8 | 0.00646 | 32 个/行 |
| per-tensor | 4 | 0.21851 | 1 个/整个张量 |
| per-channel | 4 | 0.15750 | 1 个/行 |
| per-group 128 | 4 | 0.11732 | 32 个/行 |
| per-group 32 | 4 | 0.09703 | 128 个/行 |

粒度越细误差越小，代价是缩放参数本身的存储与访存。

**per-group 128 的 int4 是当前的常见配置**：每 128 个 4 位权重配一个 16 位缩放值，等效位宽 = 4 + 16/128 = 4.125 位。这个配置在误差与开销之间取得了较好的平衡。

### 2.2 异常值

量化误差的主要来源是异常值。代码中在正态分布权重里注入异常值：

| 异常值设置 | 相对误差 | 相对基准 |
|---|---|---|
| 无（基准） | 0.00873 | 1.0x |
| 1 个 ±10 倍标准差 | 0.00909 | 1.0x |
| 1 个 ±100 倍标准差 | 0.02926 | 3.4x |
| 10 个 ±50 倍标准差 | 0.04164 | 4.8x |
| 100 个 ±50 倍标准差 | 0.07057 | 8.1x |

原因：缩放因子由最大绝对值决定，一个远大于其他元素的值会把其余元素挤到很少的几个量化档位上。

真实模型中的情况更严重：某些层的激活存在幅度差异达两个量级的通道，且这些通道是固定的（与模型结构相关，不是随机的）。

### 2.3 三种主流方案的思路

| 方案 | 做法 | 适用 |
|---|---|---|
| GPTQ | 逐列量化，用 Hessian 信息补偿已量化列引入的误差 | 权重量化，需要校准数据 |
| AWQ | 识别对输出影响大的通道，量化前放大它们、量化后缩回，等效于用更多档位表示它们 | 权重量化，需要少量校准数据 |
| SmoothQuant | 把激活的异常值按通道等比迁移到权重上，使两者都变得容易量化 | W8A8 |

三者的共同点是**承认误差无法消除，转而控制误差落在哪里**：让重要的部分误差小，不重要的部分误差大。

---

## 3. 误差如何传播

代码测量的矩阵乘输出误差：

| 方案 | 权重相对误差 | 输出相对误差 |
|---|---|---|
| int8 per-channel | 0.00867 | 0.00865 |
| int4 per-channel | 0.15729 | 0.15724 |
| int4 per-group 128 | 0.11737 | 0.11729 |
| fp8 e4m3 | 0.03491 | 0.03488 |

输出误差与权重误差在同一量级。矩阵乘是线性运算，误差不会被显著抵消，也不会爆炸。多层堆叠后误差逐层累积，这是量化影响模型质量的机制。

权重与激活都量化时：

| 配置 | 输出相对误差 |
|---|---|
| W8A16 | 0.00865 |
| W8A8 | 0.01227 |
| W4A16 | 0.15724 |
| W4A8 | 0.15750 |

W8A8 的误差比 W8A16 高 42%。W4 时激活量化的额外影响被权重误差淹没。

**重建误差只能用于初筛**。最终判断必须用下游任务指标：困惑度、标准评测集、以及目标场景的实际样本。量化对不同能力的影响不均匀，数学与代码任务通常比闲聊更敏感。

---

## 4. KV cache 量化

代码测量的 attention 输出误差（2048 长度序列）：

| KV 精度 | KV 相对误差 | attention 输出误差 |
|---|---|---|
| fp8 e4m3 | 0.02654 | 0.03888 |
| int8 per-token | 0.00645 | 0.00843 |
| int8 per-tensor | 0.01062 | 0.01464 |
| int4 per-token | 0.11700 | 0.15889 |
| int4 per-group 32 | 0.09648 | 0.11500 |

三点结论：

1. **粒度比位宽更关键**。int8 per-token（0.0084）优于 fp8 e4m3（0.0389），因为后者只有 3 位尾数且不带缩放参数。fp8 的优势在于动态范围大、不需要额外存缩放参数、有硬件原生支持。
2. **per-token 优于 per-tensor**。KV 的数值分布随位置变化，整块共用缩放参数会放大误差。实现上通常按 token 或按 head 分组。
3. **int4 KV 的误差比 int8 高一个量级**，需要更细粒度并做下游任务验证。

KV 量化是收益风险比较高的一项：它同时降低容量占用（提高并发）与访存量（降低 TPOT），而在 fp8 或 int8 下误差很小。第 05 章给出了收益数据。

---

## 5. 收益

Llama-3-8B、A100、batch 32、序列长度 8192 的访存与耗时：

| 配置 | 权重 | 总访存 | 单步耗时 | 相对基线 |
|---|---|---|---|---|
| fp16 / fp16 | 16.1 GB | 50.4 GB | 38.01 ms | 1.00x |
| fp8 / fp8 | 8.0 GB | 25.2 GB | 20.01 ms | 2.00x |
| int4 / fp16 | 4.0 GB | 38.4 GB | 29.41 ms | 1.31x |
| int4 / int8 | 4.0 GB | 21.2 GB | 17.14 ms | 2.38x |

注意 int4/fp16 只有 1.31x：这个场景下 KV 访存占比高，只量化权重的收益有限。**优化对象必须与瓶颈匹配**，第 02、03 章的占比分析是选择的依据。

实际加速比通常低于表中数值：

| 折损来源 | 说明 |
|---|---|
| 反量化的计算开销 | int4 需要解包、乘缩放、加零点，这些都要算 |
| kernel 成熟度 | 低位宽 kernel 的效率可能不如成熟的 fp16 实现 |
| prefill 无收益 | prefill 是计算受限的，权重量化对它几乎没有帮助 |
| 缩放参数的访存 | per-group 方案的缩放参数也要读 |

因此报告量化收益时必须区分 prefill 与 decode，且必须是实测。

---

## 6. 部署决策

| 场景 | 建议 |
|---|---|
| 显存不够装模型 | 先用 W4（GPTQ 或 AWQ），这是唯一选择 |
| 显存够但 KV 不够 | 优先 KV fp8，收益直接且风险低 |
| decode 慢，长序列 | KV 量化优先于权重量化（见第 5 节的对比） |
| decode 慢，短序列 | 权重量化优先 |
| prefill 慢 | 量化收益有限，应看 kernel 与 chunked prefill 配置 |
| 精度敏感 | fp8 优于 int8 优于 int4；先在目标任务上测 |

一个常被忽略的点：**量化模型的选择要考虑 kernel 支持**。某个量化格式在某个引擎、某个 GPU 架构上可能没有优化过的 kernel，此时虽然显存省了，速度反而更慢。部署前应当实测而不是照搬配置。

---

## 7. 引擎实现对照

| 引擎 | 量化支持 |
|---|---|
| vLLM | `--quantization` 支持 awq、gptq、fp8、bitsandbytes 等；`--kv-cache-dtype fp8` 单独控制 KV |
| SGLang | 类似的选项；对 fp8 与 MLA 的组合有专门优化 |
| TensorRT-LLM | 编译期指定量化配置，支持 fp8、int8 SmoothQuant、int4 AWQ |
| llama.cpp | GGUF 格式内置多种量化档位（Q4_K_M 等），面向 CPU 与边缘 |

llama.cpp 的量化档位命名值得了解一下：`Q4_K_M` 表示 4 位、K-quant 方案、中等大小。它的做法是对不同层用不同位宽（重要的层保留更高精度），这与 AWQ 的思路一致。

---

## 8. 代码

`code/ch13_quantization.py` 用 torch（CPU）做真实的量化与反量化：

1. 量化粒度对误差的影响
2. 异常值的影响
3. 误差在矩阵乘中的传播，以及权重量化与激活量化的对比
4. KV cache 量化的误差（含 attention 输出）
5. 显存与访存的收益

运行：

```bash
python3 code/ch13_quantization.py
```

建议的实验：

- 把权重改为带有固定异常通道的分布（例如某几列的幅度乘 50），观察 per-channel 与 per-group 的差距扩大。这更接近真实模型的情况，也解释了 AWQ 为什么有效。
- 在第 4 节中比较 per-token 与 per-tensor，验证 KV 量化必须按 token 或按 head 分组。
