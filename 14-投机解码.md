# 14 · 投机解码

decode 是访存受限的（第 02 章），意味着算力有富余。投机解码把这部分富余的算力用来一次验证多个候选 token，从而把权重访存的成本摊到多个 token 上。

它是唯一改变「一步一个 token」这一前提的优化，也是唯一在数学上可证明不改变输出分布的加速手段。

---

## 1. 机制

```
1. 用低成本的方法生成 k 个候选 token（草稿）
2. 用目标模型做一次前向，同时得到这 k+1 个位置的 logits
3. 从前往后逐个验证：接受则继续，拒绝则停止并修正
4. 一轮产出 1 到 k+1 个 token
```

第 2 步是关键：验证 k 个草稿 token 只需要**一次**前向，因为可以把它们拼成一个长度 k 的序列一次性输入（类似一次小型 prefill）。权重只读一遍，而这正是 decode 的主要成本。

### 1.1 草稿的来源

| 方案 | 做法 | 相对成本 c |
|---|---|---|
| n-gram / 前缀匹配 | 从 prompt 或已生成内容中查找重复片段 | 约 0.02 |
| Medusa | 在主干上加多个轻量预测头，各预测未来第 i 个位置 | 约 0.05 |
| EAGLE | 用主干的隐状态做自回归的轻量预测，精度高于 Medusa | 约 0.05 |
| 独立草稿模型 | 用同系列的小模型（1B 配 8B） | 约 0.1 |
| 过大的草稿模型 | 1.5B 配 7B | 约 0.2 及以上 |

`c` 是草稿一步的成本相对目标模型一步的比例，是决定收益的两个参数之一。

---

## 2. 加速比模型

设接受率为 α（每个草稿 token 被接受的概率），草稿长度 k，草稿成本比 c：

```
一轮期望产出的 token 数 = (1 - α^(k+1)) / (1 - α)
一轮的成本（以目标模型一步为单位）= k·c + 1
加速比 = 期望产出 / 成本
```

计算结果（c = 0.1）：

| 接受率 | k=1 | k=2 | k=4 | k=6 | k=10 | 最优 k | 最优加速 |
|---|---|---|---|---|---|---|---|
| 0.30 | 1.18 | 1.16 | 1.02 | 0.89 | 0.71 | 1 | 1.18x |
| 0.50 | 1.36 | 1.46 | 1.38 | 1.24 | 1.00 | 2 | 1.46x |
| 0.70 | 1.55 | 1.82 | 1.98 | 1.91 | 1.63 | 4 | 1.98x |
| 0.80 | 1.64 | 2.03 | 2.40 | 2.47 | 2.29 | 6 | 2.47x |
| 0.90 | 1.73 | 2.26 | 2.93 | 3.26 | 3.43 | 10 | 3.43x |
| 0.95 | 1.77 | 2.38 | 3.23 | 3.77 | 4.31 | 15 | 4.48x |

两个结论：

1. **接受率是决定性因素**。0.5 时上限约 1.5 倍，0.9 时可达 3.4 倍。
2. **草稿长度存在最优值**，且随接受率上升而变长。太短则验证的固定成本摊不开，太长则后面的 token 大概率被拒绝，算了白算。

### 2.1 什么时候会变慢

加速比小于 1 即为负收益。给定草稿成本，可用的最低接受率：

| 草稿成本 c | 自适应 k 的门槛 | 固定 k=5 的门槛 | 对应方案 |
|---|---|---|---|
| 0.02 | 0.03 | 0.10 | n-gram / 前缀匹配 |
| 0.05 | 0.06 | 0.21 | Medusa / EAGLE |
| 0.10 | 0.11 | 0.34 | 1B 草稿配 8B 目标 |
| 0.20 | 0.21 | 0.51 | 1.5B 草稿配 7B 目标 |
| 0.35 | 0.36 | 0.67 | 草稿模型过大 |

两列差别很大。**生产实现都带自适应的草稿长度**：根据最近几轮的接受情况动态调整 k，接受率高时加长，低时缩短甚至暂时关闭。固定 k 的实现在接受率波动时容易落入负收益区。

### 2.2 接受率取决于任务

经验值（需按实际负载实测）：

| 任务 | 接受率 | c=0.1 时的最优加速 |
|---|---|---|
| 代码补全（重复度高） | 约 0.85 | 2.85x |
| 结构化输出 / JSON | 约 0.80 | 2.47x |
| 摘要（可抄原文） | 约 0.75 | 2.19x |
| 常规对话 | 约 0.60 | 1.67x |
| 开放式创作 | 约 0.45 | 1.38x |

规律是**输出越可预测，接受率越高**。代码、JSON、从原文抄写这类任务有大量确定性的片段，n-gram 方案在这些场景下就能取得很高的接受率且几乎无成本。

---

## 3. batch 的影响

投机解码把 decode 的算术强度从 batch 提高到 batch × (k+1)。这在访存受限区间是纯赚，进入计算受限区后多算的 token 就要付出真实的算力代价。

Llama-3-8B、A100、序列长度 2048、k=4、接受率 0.7：

| batch | 逐 token 解码 | 投机解码 | 加速比 |
|---|---|---|---|
| 1 | 13.663 ms/token | 6.901 ms/token | 1.98x |
| 16 | 16.539 ms/token | 8.396 ms/token | 1.97x |
| 64 | 25.743 ms/token | 13.488 ms/token | 1.91x |
| 128 | 38.014 ms/token | 24.312 ms/token | 1.56x |
| 256 | 62.557 ms/token | 45.959 ms/token | 1.36x |

上表只计入计算成本。真实系统中还有草稿模型的调度开销、树形验证的掩码构造、草稿模型自身占用的显存与 KV，因此实际下降更快，大 batch 下可能变为负收益。

**结论：投机解码适合低并发、低延迟优先的场景**（单用户交互、代码补全、本地部署），不适合追求吞吐的高并发服务。生产中通常按当前 batch 大小动态开关：并发低时启用，并发高时关闭。

---

## 4. 正确性：为什么不改变输出分布

这是投机解码最重要的性质，也是面试中的高频问题。

### 4.1 拒绝采样

```
1. 用草稿模型采样得到 x ~ q
2. 以概率 min(1, p(x)/q(x)) 接受 x
3. 拒绝时，从修正分布 normalize(max(p - q, 0)) 重新采样
```

其中 p 是目标模型的分布，q 是草稿模型的分布。

代码的经验验证（40 万次试验，词表大小 8）：

```
目标分布 p : [0.30 0.25 0.15 0.10 0.08 0.06 0.04 0.02]
草稿分布 q : [0.20 0.30 0.10 0.15 0.10 0.05 0.07 0.03]
经验分布   : [0.3011 0.2511 0.1499 0.0992 0.0795 0.0597 0.0400 0.0195]
最大偏差   : 0.0011（采样噪声约 0.0016）
接受率     : 0.8391（理论值 0.8400）
```

经验分布与目标分布的偏差在采样噪声范围内。**投机解码是无损的**，不需要在质量上做取舍。这与量化、剪枝这类有损优化有本质区别。

### 4.2 接受率的理论值

```
接受率 = Σ min(p(x), q(x))
```

即两个分布的重叠面积。推论：**草稿模型的训练目标应当是拟合目标模型的输出分布，而不是拟合真实数据**。这是 EAGLE 这类方案用目标模型的隐状态做输入的原因——它能更直接地逼近目标分布。

### 4.3 贪心解码时的简化

温度为 0 时，p 是一个 one-hot 分布，验证退化为「草稿 token 是否等于目标模型的 argmax」，不需要拒绝采样。这也解释了为什么贪心解码下投机解码的实现更简单，接受率也更容易统计。

---

## 5. 树形草稿

上述模型假设草稿是一条线性序列。更进一步的做法是生成一棵候选树：每个位置有多个候选，一次验证整棵树。

```
线性：  A → B → C            验证 3 个位置
树形：  A → B → C
          ↘ D → E           验证 5 个位置，覆盖 2 条路径
```

收益是接受率提高（有多条路径可选），代价是：

- 验证的 token 数增加，计算量上升
- attention 需要特殊的掩码（树形注意力）：每个节点只能看到自己的祖先，不能看到兄弟分支
- 掩码的构造与传递增加了 CPU 侧开销

Medusa 与 EAGLE 都使用树形草稿。这也是 attention kernel 需要支持自定义掩码的原因之一（第 11 章）。

---

## 6. 引擎实现对照

| 引擎 | 投机解码支持 |
|---|---|
| vLLM | `--speculative-config` 支持草稿模型、n-gram、EAGLE、Medusa |
| SGLang | 支持 EAGLE 系列，对树形验证有专门优化 |
| TensorRT-LLM | 支持草稿模型与 Medusa，编译期配置 |
| llama.cpp | 支持草稿模型；低并发的本地场景收益明显 |

实现上的共同难点：

1. **草稿模型也需要 KV cache**，占用显存并需要独立的 block 管理
2. **被拒绝的 token 的 KV 要回滚**，block manager 需要支持截断
3. **与连续批处理的配合**：batch 内不同请求接受的 token 数不同，下一步的输入形状不规则
4. **接受率的统计与自适应**：需要按请求维护滑动窗口的接受率

第 2 点值得注意：投机解码打破了「KV 只增不减」的假设，block manager 必须支持回退。这是引擎中一处容易出错的地方。

---

## 7. 代码

`code/ch14_speculative.py` 包含：

1. 加速比公式与最优草稿长度的扫描
2. 负收益区的门槛（自适应 k 与固定 k 两种）
3. batch 大小对收益的影响
4. 拒绝采样的经验验证（含分布一致性断言）

运行：

```bash
python3 code/ch14_speculative.py
```

建议的实验：

- 把 c 从 0.1 改为 0.02（对应 n-gram 方案），观察最优草稿长度变长、加速比提高。
- 把第 4 节的 q 改得更接近 p，观察接受率上升并对照理论值 `Σ min(p, q)`。这个实验能直接说明草稿模型的优化目标是什么。
