# 15 · 分布式推理

单卡装不下模型，或者单卡的显存带宽不足以满足延迟要求时，需要把模型切到多张卡上。本章讨论三种切法的通信量、适用条件与选择顺序。

---

## 1. 三种并行方式

| 方式 | 切分对象 | 通信 | 主要收益 | 代价 |
|---|---|---|---|---|
| 张量并行 TP | 每层的权重按维度切分 | 每层 2 次 all-reduce | 降低单请求延迟；扩大 KV 预算 | 通信频繁，需高速互联 |
| 流水并行 PP | 按层切分，每卡负责若干层 | 层间传递激活，量小 | 跨机可行；通信量小 | 有流水气泡；单请求延迟不降 |
| 数据并行 DP | 每卡一份完整模型 | 无 | 线性扩展吞吐；实现最简单 | 显存需求不降 |

推理与训练的差别：训练中数据并行是主力，推理中数据并行退化为「多起几个独立实例」，由服务层的负载均衡实现（第 16 章），不属于引擎内部的并行。

---

## 2. 张量并行

### 2.1 切分方式

以一层为例：

```
attention:
  W_q, W_k, W_v  按输出维度（列）切分 → 每卡算部分头
  W_o            按输入维度（行）切分 → 算完需要 all-reduce

MLP:
  W_gate, W_up   按列切分
  W_down         按行切分 → 算完需要 all-reduce
```

这个切法的设计目的是**每层只需要 2 次通信**：列切分的输出可以直接进入行切分的输入，中间不需要同步。

KV cache 也随之切分：每卡只持有自己负责的那些头的 KV。因此 TP 会等比例扩大总的 KV 预算。

### 2.2 通信量

```
每次 all-reduce 的单卡收发量 = 2 × (TP-1)/TP × 本步 token 数 × hidden × 2 字节
每步的通信次数 = 2 × 层数
```

Llama-3-8B、batch 32、一步 decode：

| TP | 单步通信量 |
|---|---|
| 2 | 16.78 MB |
| 4 | 25.17 MB |
| 8 | 29.36 MB |

通信量随 TP 增大而增加，但趋于饱和（系数 2(TP-1)/TP 上限为 2）。

### 2.3 通信占比决定了能否跨机

| 链路 | TP=2 | TP=4 | TP=8 |
|---|---|---|---|
| NVLink（同机） | 3.4% | 6.9% | 13.1% |
| PCIe Gen4（同机） | 5.6% | 12.7% | 24.1% |
| RDMA 200Gb（跨机） | 9.2% | 21.3% | 37.9% |
| 以太网 100Gb（跨机） | 14.5% | 32.2% | 52.1% |

表中为通信耗时占单步总耗时的比例。

**跨机 TP=8 时超过三分之一的时间在通信**，这是张量并行通常限制在单机内（NVLink 域）的原因。跨机需要用流水并行或数据并行。

注意 decode 阶段的通信是小消息（16-30 MB），且每层都要同步，因此对链路的**延迟**比对带宽更敏感。每次 all-reduce 的固定发起开销（几微秒）乘以 64 次就是几百微秒，与通信本身的传输时间同量级。

### 2.4 TP 对延迟的作用

Llama-3-8B、batch 1、序列 2048、NVLink：

| TP | 计算 | 通信 | 合计 | 相对 TP=1 |
|---|---|---|---|---|
| 1 | 13.66 ms | 0 | 13.66 ms | 1.00x |
| 2 | 6.83 ms | 0.32 ms | 7.15 ms | 1.91x |
| 4 | 3.42 ms | 0.32 ms | 3.74 ms | 3.66x |
| 8 | 1.71 ms | 0.32 ms | 2.03 ms | 6.73x |

batch 1 时 TP 能接近线性地降低单请求延迟，因为权重访存被分摊到多卡的带宽上。这是低延迟场景使用大 TP 的理由。

表中的数字是理想值。实际会因为 kernel 在小切片上的效率下降、通信与计算无法完全重叠而低于此，TP=8 的实际加速通常在 4-5 倍。

**但总吞吐并未按比例提高**：8 张卡跑一个 TP=8 的实例，吞吐低于 8 张卡跑 8 个独立实例。TP 是用吞吐换延迟。

---

## 3. 显存分摊

单卡 80 GB，按 90% 可用：

| 模型 | 参数量 | fp16 权重 | 最小 TP | 该 TP 下的 KV 预算 | 8K 序列的并发 |
|---|---|---|---|---|---|
| Llama-3-8B | 8.0B | 16.1 GB | 1 | 51.9 GB | 48 |
| Llama-2-13B | 13.0B | 26.0 GB | 1 | 42.0 GB | 6 |
| Qwen2.5-7B | 7.6B | 15.2 GB | 1 | 52.8 GB | 112 |
| Qwen2.5-72B | 72.7B | 145.4 GB | 4 | 126.6 GB | 47 |

两个观察：

1. **Llama-2-13B 的并发只有 6**，远低于参数量更小的 Llama-3-8B（48）。原因是它用 MHA，KV 单价是后者的 6.25 倍（第 05 章）。选模型时这个因素的影响大于参数量。
2. **TP 不只是为了装下模型**。即使单卡装得下，增大 TP 也能成倍扩大 KV 预算，从而提高并发。代价是通信开销与卡数增加。

---

## 4. 流水并行

按层切分，每卡负责连续的若干层。通信量很小（只传层间的激活，一个 `[tokens, hidden]` 的张量），因此可以跨机。

问题是流水气泡：

| PP 级数 | batch 1 时的气泡占比 | 填满流水需要的并发 |
|---|---|---|
| 2 | 50% | 2 |
| 4 | 75% | 4 |
| 8 | 88% | 8 |

batch 1 时 4 级流水中只有 1 级在工作，其余空转。

推理中缓解气泡的方式与训练不同：训练需要显式切分 microbatch，推理则由连续批处理天然提供请求流——不同请求处于不同的流水级。因此 **PP 在高并发下可用，在低并发下浪费严重**。

PP 还有一个性质：它不降低单请求延迟。请求仍然要顺序经过所有层，只是这些层分布在不同卡上，中间多了传输。

---

## 5. 选择顺序

经验规则：

```
1. 单卡装得下 → 数据并行（多实例），最简单且吞吐最高
2. 单卡装不下 → 单机内 TP（NVLink 域内），TP 取 2/4/8
3. 单机 8 卡仍装不下 → 跨机加 PP，机内保持 TP
4. 延迟要求极严且卡有富余 → 提高 TP，用吞吐换延迟
5. MoE 模型 → 加专家并行
```

一个常见的配置错误是「卡多就把 TP 开满」。TP=8 相比 4 个 TP=2 的实例，延迟更低但吞吐更差。选择依据是 SLO：TPOT 已经满足要求时，应当降低 TP 提高吞吐。

---

## 6. MoE 与专家并行

### 6.1 MoE 在 decode 阶段的问题

MoE 把 MLP 换成多个专家，每个 token 只激活其中几个。以 64 个专家、每 token 激活 8 个、32 层为例：

```
全部专家的权重       : 103.1 GB
单 token 激活的权重  : 12.9 GB（12.5%）
```

看起来计算量与访存量都只有 12.5%。但 batch 增大后，不同 token 会选中不同的专家：

| batch | 期望激活的专家数 | 需读取的权重 | 相对单 token |
|---|---|---|---|
| 1 | 8.0 | 12.9 GB | 1.0x |
| 4 | 26.5 | 42.7 GB | 3.3x |
| 8 | 42.0 | 67.7 GB | 5.3x |
| 32 | 63.1 | 101.6 GB | 7.9x |
| 64 | 64.0 | 103.1 GB | 8.0x |

**batch 32 时几乎全部专家都要读一遍**，权重访存量接近稠密模型，而计算量仍然只有激活部分。

结论：MoE 在 decode 阶段的算术强度**低于**同等激活参数量的稠密模型。它节省的是计算，而 decode 的瓶颈是访存。这是 MoE 部署困难的根本原因，也解释了为什么 MoE 模型通常需要更多的卡（把专家分散到多卡，让每卡只读自己那部分）。

### 6.2 专家并行

把专家分布到多张卡，每卡持有部分专家。

```
token 到达 → 路由决定去哪些专家 → all-to-all 发到对应的卡
          → 各卡计算 → all-to-all 发回原卡 → 继续下一层
```

通信量与激活的 token 数和 hidden 成正比，与专家数无关。

主要难点是**负载不均**：专家的选中频率不均匀，热门专家所在的卡成为瓶颈，其他卡空等。缓解手段包括热门专家的冗余部署、按统计结果重新分布专家、以及在路由中加入负载均衡项（训练时的辅助损失）。

---

## 7. 引擎实现对照

| 引擎 | 并行支持 |
|---|---|
| vLLM | `--tensor-parallel-size`、`--pipeline-parallel-size`、`--data-parallel-size`；MoE 支持 EP |
| SGLang | 类似；对 MLA + EP 的组合有专门优化 |
| TensorRT-LLM | 编译期确定并行配置，运行时不可改 |

vLLM 的 TP 实现依赖 NCCL 做 all-reduce，进程模型是每卡一个 worker 进程，由一个 driver 统一调度。读源码时注意：调度决策只在 driver 上做一次，然后广播给所有 worker，因此各卡的 batch 组成完全一致。

---

## 8. 代码

`code/ch15_parallel.py` 包含：

1. 张量并行的通信量与各链路下的占比
2. 显存分摊表与 TP 对延迟的作用
3. 三种并行方式的对比与流水气泡
4. MoE 的专家激活覆盖率与专家并行

运行：

```bash
python3 code/ch15_parallel.py
```

建议的实验：

- 把链路换成跨机的 25 GB/s，观察 TP=8 的通信占比升到 38%，这解释了张量并行不跨机的原因。
- 第 4 节把 batch 从 1 扫到 128，观察 MoE 的权重读取量如何从「只读激活部分」退化为「读全部专家」。这个曲线是理解 MoE 部署的关键。
