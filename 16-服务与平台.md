# 16 · 推理服务与平台

前面各章讨论的是单个引擎实例。本章讨论多实例之上的服务层：请求怎么分配、容量怎么规划、扩缩怎么触发、指标怎么定义。

这一层的问题形态与通用的在线服务控制面相似（作业生命周期、状态一致性、幂等、对账），差别在于三点：单个实例是有状态的（KV cache 与前缀缓存）、扩容需要分钟级的模型加载、以及请求的资源消耗在结束前不可知。

---

## 1. 服务层的职责

| 职责 | 内容 |
|---|---|
| 路由 | 把请求分配到实例，见第 2 节 |
| 容量规划 | 决定实例数量与规格，见第 3 节 |
| 弹性 | 按负载扩缩，见第 4 节 |
| 多租户 | 配额、限流、隔离，见第 5 节 |
| 可观测性 | 指标、日志、追踪，见第 6 节 |
| 版本管理 | 模型与引擎的灰度、回滚 |
| 故障处理 | 健康检查、摘除、重试 |

---

## 2. 路由

### 2.1 与无状态服务的差别

普通的无状态服务用轮询或最少连接即可。推理实例是有状态的：它的前缀缓存里存着之前处理过的内容。把同前缀的请求发到同一个实例，可以直接命中缓存。

第 8 节的模拟（1200 个请求，819055 个 prompt token，8 个实例）：

| 策略 | 前缀命中率 | 需 prefill 的 token | 负载不均衡度 | 计算量相对轮询 |
|---|---|---|---|---|
| 轮询 | 73.0% | 220863 | 0.00 | 1.00x |
| 随机 | 72.5% | 224943 | 0.22 | 0.98x |
| 最少请求数 | 73.0% | 220863 | 0.00 | 1.00x |
| 前缀哈希（前 64 token） | 83.0% | 138895 | 3.27 | 1.59x |
| 前缀感知（查缓存） | 83.4% | 136367 | 3.23 | 1.62x |
| 前缀感知 + 负载上限 | 83.4% | 135839 | 0.03 | 1.63x |

前缀感知路由把需要计算的 token 减少了 38%，即 prefill 的算力需求下降到六成。

### 2.2 集群越大，路由越重要

| 实例数 | 轮询命中率 | 前缀感知命中率 | 计算量之比 |
|---|---|---|---|
| 1 | 79.2% | 79.2% | 1.00x |
| 2 | 77.4% | 81.9% | 1.25x |
| 4 | 76.1% | 83.4% | 1.44x |
| 8 | 73.0% | 83.4% | 1.62x |
| 16 | 70.6% | 83.4% | 1.77x |
| 32 | 67.0% | 83.4% | 1.98x |

轮询下前缀被打散到所有实例，每个实例都要各存一份、各算一次，命中率随实例数上升而下降。前缀感知路由基本不受影响。32 实例时两者的计算量相差一倍。

### 2.3 收益体现在 TTFT

命中不同比例时，一个 2048 token 的 prompt 的 prefill 耗时：

| 命中比例 | 需计算的 token | prefill 耗时 | 相对未命中 |
|---|---|---|---|
| 0% | 2048 | 157.6 ms | 1.00x |
| 50% | 1024 | 81.1 ms | 1.94x |
| 75% | 512 | 41.9 ms | 3.77x |
| 90% | 205 | 18.0 ms | 8.74x |
| 100% | 0 | 0 ms | — |

### 2.4 三种实现方式

| 方式 | 做法 | 优点 | 问题 |
|---|---|---|---|
| 一致性哈希 | 按 prompt 前 N 个 token 哈希 | 无状态，实现最简单 | 实例增减时哈希环变动导致缓存大面积失效；无法处理前缀长度不同的情况 |
| 路由器影子索引 | 路由器记录每个实例缓存过哪些块哈希 | 命中率最高 | 路由器需维护状态；与实例的实际淘汰可能不一致 |
| 实例上报 | 各实例周期上报缓存摘要（布隆过滤器等） | 状态量小 | 有延迟 |

模拟结果显示一致性哈希（83.0%）已接近查缓存（83.4%），且实现最简单，是多数生产系统的起点。

### 2.5 亲和与均衡的冲突

纯前缀感知路由的实例负载分布：

```
纯前缀感知   : [485, 361, 224, 130, 0, 0, 0, 0]
加负载上限   : [152, 152, 148, 151, 151, 151, 148, 147]
```

负载中只有 4 个 system prompt，纯前缀感知把它们固定到 4 个实例上，另外 4 个实例完全空闲——真实系统中意味着一半的卡在空转。

加负载上限后 8 个实例均分请求，命中率没有下降：热点前缀被复制到多个实例，每个各缓存一份，总缓存容量足够。

命中率与均衡度并非总能兼得。缓存容量紧张时，复制会挤占空间导致淘汰增多。生产实现通常用「命中长度 - 负载惩罚」的加权打分，而不是硬阈值。

---

## 3. 容量规划

### 3.1 计算方法

```
1. 测出目标负载下单实例的 goodput（第 19 章）
2. 实例数 = 峰值请求速率 / 单实例 goodput × 冗余系数
3. 校验显存：用第 05 章的公式确认并发数满足要求
4. 校验 SLO：在目标并发下 TTFT 与 TPOT 是否达标
```

关键是第 1 步必须用**目标负载**测量。输入输出长度分布不同，单实例吞吐可以相差一个量级（第 04 章的表）。

### 3.2 需要报告的规格

一份可用的容量规划文档应当包含：

| 项 | 例 |
|---|---|
| 模型与精度 | Llama-3-8B，fp16 权重，fp8 KV |
| 卡型与并行配置 | A100-80GB，TP=1，8 实例 |
| 负载特征 | 输入 P50/P95，输出 P50/P95，QPS 峰值与均值 |
| SLO | TTFT P95 ≤ 2s，TPOT P95 ≤ 50ms |
| 单实例 goodput | 在上述 SLO 下的实测值 |
| 冗余 | 故障域数量、扩容耗时、预留比例 |

---

## 4. 弹性扩缩

### 4.1 指标选择

| 指标 | 是否适合作为扩缩依据 |
|---|---|
| GPU 利用率 | 不适合。decode 阶段利用率天然不高（访存受限），高利用率不代表饱和 |
| 显存占用 | 不适合。引擎预分配 KV 池，占用恒定 |
| 队列长度 | 适合。直接反映需求超出供给的程度 |
| KV 池使用率 | 适合。反映显存侧的余量 |
| TTFT 分位数 | 适合，但滞后 |
| 抢占次数 | 适合作为告警。持续大于 0 说明容量不足 |

**GPU 利用率是最容易误用的指标**。`nvidia-smi` 报告的利用率只表示「有 kernel 在执行」，不表示算力被用满。decode 阶段利用率可能显示 90% 以上而算力实际只用了几个百分点。

推荐组合：以队列长度与 KV 池使用率为扩缩依据，以 TTFT 分位数与抢占次数为告警。

### 4.2 冷启动

扩容的耗时构成：

| 阶段 | 耗时 |
|---|---|
| 调度到节点、拉起容器 | 10 秒到数分钟（镜像是否已缓存） |
| 加载模型权重 | 数十秒到数分钟（模型大小、存储带宽） |
| 显存探测与 CUDA Graph 录制 | 10 秒到数分钟 |
| 预热与健康检查 | 数十秒 |

总计通常在 2-10 分钟。**这个量级决定了弹性策略必须是预测式而非响应式**：等到队列积压再扩容，用户已经受影响数分钟。

缓解手段：镜像与权重预热到节点本地、保留一定比例的空闲实例、按历史流量曲线做定时扩缩、把冷启动实例先接入低优先级流量。

### 4.3 缩容的额外问题

缩容时实例上还有运行中的请求。需要优雅下线：

```
1. 从路由中摘除，不再接新请求
2. 等待运行中的请求完成（可能需要数分钟，长输出请求更久）
3. 超时后中止剩余请求并返回错误，或迁移到其他实例
```

第 2 步的等待时间由最长的输出决定。设置 `max_tokens` 上限是让缩容可控的前提。

---

## 5. 多租户

| 手段 | 位置 | 说明 |
|---|---|---|
| 请求速率限流 | 网关 | 令牌桶，按租户 |
| token 速率限流 | 网关 | 更贴近实际资源消耗，但输出长度事前不可知，只能事后计费 |
| 并发数限制 | 网关 | 限制每租户同时在途的请求数，简单有效 |
| 优先级 | 引擎 | 见第 09 章，需要防饥饿 |
| 实例隔离 | 服务层 | 强隔离要求时为租户分配独占实例 |

**限流放在网关，引擎内部保持简单**是常见的分层原则。原因是引擎内部难以定义公平（第 09 章），而网关有完整的租户上下文。

并发数限制是最实用的一项：它直接对应租户能占用的 KV 空间，且不需要预知输出长度。

一个安全事项：前缀缓存是跨请求共享的，在多租户场景下会引入侧信道（第 07 章）。若威胁模型要求，需要按租户隔离缓存命名空间，代价是命中率下降。

---

## 6. 可观测性

### 6.1 必备指标

| 层 | 指标 |
|---|---|
| 请求级 | TTFT、TPOT、端到端延迟、输入输出 token 数、状态码、租户、模型 |
| 引擎级 | 运行中序列数、等待队列长度、KV 池使用率、前缀命中率、抢占次数、每步耗时 |
| 实例级 | GPU 利用率与显存（作为参考而非依据）、进程存活、NCCL 错误 |
| 服务级 | 各实例的请求分布、路由命中率、扩缩事件、错误率 |

分位数是必需的：只有平均值时，长尾问题完全不可见（第 01 章的对照表说明了这一点）。

### 6.2 排查路径

TTFT 变高时的分层定位：

```
1. 看队列长度：接近 0 → 不是排队问题，进入第 2 步
                较长 → 容量不足或准入控制过严
2. 看前缀命中率：下降 → 路由问题或缓存被挤占
                正常 → 进入第 3 步
3. 看 prefill 耗时与输入长度分布：输入变长 → 负载变化
                                 输入未变 → 进入第 4 步
4. 看抢占次数与 KV 池使用率：有抢占 → 显存不足
                            无抢占 → 检查网关与网络
```

TPOT 变高时：

```
1. 看运行中序列数：变大 → batch 增大导致，符合预期
2. 看是否有 prefill 混入：chunked prefill 的 chunk 是否过大
3. 看序列平均长度：变长 → KV 访存增加（第 02 章的表）
4. 看抢占：抢占会造成 ITL 的长尾
```

---

## 7. 灰度与回滚

推理服务的版本变更有三类，风险不同：

| 变更 | 风险 | 验证方式 |
|---|---|---|
| 引擎版本升级 | 性能与稳定性 | 影子流量对比延迟与吞吐 |
| 引擎配置变更 | 性能 | 小比例灰度，看 goodput |
| 模型版本变更 | 输出质量 | 需要离线评测 + 在线 A/B |

第三类最难：输出质量的变化无法用系统指标发现。必须有独立的评测环节，且线上要保留可回滚的旧版本实例。

**模型变更与引擎变更应当分开发布**，否则出问题时无法定位是哪一个引起的。

---

## 8. 代码

`code/ch16_routing.py` 在同一负载上比较六种路由策略：

1. 六种策略的前缀命中率与负载均衡度
2. 命中率随实例数的变化
3. 命中比例对 TTFT 的影响
4. 三种实现方式的对比
5. 亲和与均衡的冲突及其缓解

运行：

```bash
python3 code/ch16_routing.py
```

建议的实验：

- 把 `CACHE_TOKENS_PER_INSTANCE` 减半，观察「前缀感知 + 负载上限」的命中率开始下降。这是命中率与均衡度不可兼得的情形。
- 把负载生成器中的 system prompt 数量从 4 改为 64（模拟多租户），观察一致性哈希与查缓存两种方式的差距变化。
