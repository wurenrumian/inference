# 18 · 引擎对照

本章横向对比主流推理引擎，并给出选型方法。

**时效说明**：这个领域的迭代很快，各引擎的功能差距在收窄，具体特性以官方文档为准。本章的价值在于对比的维度与选型的方法，而不是某个时点的功能清单。投递前应当查目标团队使用哪一个。

---

## 1. 主要引擎

| 引擎 | 定位 | 主要特点 |
|---|---|---|
| vLLM | 通用推理引擎，事实上的默认选择 | PagedAttention 的原始实现，模型支持最广，社区最活跃 |
| SGLang | 通用推理引擎，偏 Agent 与结构化场景 | RadixAttention 前缀缓存，结构化输出优化，MLA 与 MoE 支持较早 |
| TensorRT-LLM | NVIDIA 官方，性能上限导向 | 编译式流程，图级融合，与 Triton 配合 |
| llama.cpp | CPU 与边缘设备 | GGUF 量化格式，无依赖，单请求场景优化 |
| Hugging Face TGI | 早期方案 | 工程完整度高，与 HF 生态集成 |
| LMDeploy | 国内团队维护 | TurboMind 后端，对国产模型支持较好 |
| Ollama | 面向本地使用的封装 | 基于 llama.cpp，模型管理与易用性 |

---

## 2. 逐维度对比

### 2.1 显存与前缀缓存

| 引擎 | 分页 | 块粒度 | 前缀缓存 | 默认开启 |
|---|---|---|---|---|
| vLLM | 有 | 16 token | 哈希块表 | 是（V1） |
| SGLang | 有 | 1 token | 基数树 | 是 |
| TensorRT-LLM | 可选 | 可配置 | 块复用 | 可配置 |
| llama.cpp | 无（按上下文上限预留） | — | 有限支持 | — |

第 07 章的模拟显示两种前缀缓存方案的命中率差距约 1 个百分点，远小于「有无缓存」的差距。方案选择不是主要矛盾。

### 2.2 调度

| 引擎 | 连续批处理 | chunked prefill | 抢占 | 可配置策略 |
|---|---|---|---|---|
| vLLM V1 | 是 | 默认 | recompute | 少（有意简化） |
| SGLang | 是 | 支持 | retract | 多（含按前缀命中长度排序） |
| TensorRT-LLM | in-flight batching | 支持 | KV offload | 少 |
| llama.cpp | 有限 | — | — | — |

SGLang 的「按前缀命中长度排序」值得注意：优先调度命中前缀最长的请求，能最大化缓存复用。这是把第 07 章的机制反馈到调度层。

### 2.3 量化

| 引擎 | 权重量化 | KV 量化 | 特点 |
|---|---|---|---|
| vLLM | AWQ、GPTQ、fp8、bitsandbytes 等 | fp8 | 覆盖面广 |
| SGLang | 类似 | fp8 | 对 fp8 + MLA 组合有优化 |
| TensorRT-LLM | fp8、int8 SmoothQuant、int4 AWQ | fp8 | 编译期确定，性能上限高 |
| llama.cpp | GGUF 的多种档位（Q4_K_M 等） | 支持 | 面向 CPU，逐层不同位宽 |

### 2.4 并行

| 引擎 | TP | PP | EP | 跨机 |
|---|---|---|---|---|
| vLLM | 是 | 是 | 是 | 支持（Ray 或原生） |
| SGLang | 是 | 是 | 是 | 支持 |
| TensorRT-LLM | 是 | 是 | 是 | 支持 |
| llama.cpp | 有限 | 有限 | — | — |

### 2.5 其他能力

| 能力 | vLLM | SGLang | TensorRT-LLM | llama.cpp |
|---|---|---|---|---|
| 结构化输出 | 是（多后端） | 是（优化较深） | 有限 | 有限 |
| 投机解码 | 是 | 是 | 是 | 是 |
| LoRA 多适配器 | 是 | 是 | 是 | 有限 |
| 多模态 | 是 | 是 | 部分 | 部分 |
| PD 分离 | 通过 connector | 内置 | 有方案 | — |

---

## 3. 架构风格的差异

三种引擎代表三种不同的取舍。

### 3.1 vLLM 与 SGLang：解释式

模型用 PyTorch 定义，运行时按 Python 逻辑逐层执行，kernel 从库中选择。

| 优点 | 代价 |
|---|---|
| 新模型接入快（写一个 Python 类即可） | 每步有 Python 开销 |
| 配置改动即时生效 | 跨算子的融合受限 |
| 便于修改与调试 | 性能上限低于编译式 |

### 3.2 TensorRT-LLM：编译式

模型先编译为引擎文件，包含针对具体形状与配置生成的 kernel 与融合结果。

| 优点 | 代价 |
|---|---|
| 图级融合，性能上限高 | 编译耗时（分钟到小时级） |
| 无 Python 运行时开销 | 配置改动需要重新编译 |
| 与 NVIDIA 硬件特性结合紧密 | 新模型支持滞后；调试困难 |

选择依据：**模型与配置是否稳定**。长期固定跑一个模型的生产服务适合编译式；需要频繁切换模型或试验配置的场景适合解释式。

### 3.3 llama.cpp：面向单请求

针对 batch 1 与 CPU/边缘设备优化。没有分页、没有复杂调度，因为这些机制的收益来自高并发，而它的目标场景没有高并发。

它的优化重点是权重量化（降低访存量）与 CPU 上的 SIMD kernel。这是第 02 章的模型在另一个参数区间下的结论：batch 1 时算术强度为 1，量化的收益接近线性。

---

## 4. 选型方法

按约束逐步排除：

```
1. 硬件是什么？
   NVIDIA GPU              → 继续
   CPU / Mac / 边缘设备      → llama.cpp 或 Ollama
   国产加速卡               → 查厂商适配的引擎版本

2. 模型是什么？
   常见开源模型             → 继续
   新发布或结构特殊          → 优先 vLLM 或 SGLang（支持最快）

3. 场景是什么？
   高并发在线服务           → vLLM 或 SGLang
   Agent / 结构化输出为主    → SGLang
   固定模型的极致性能        → TensorRT-LLM
   本地开发与试验            → vLLM（易用）或 Ollama（最简）

4. 团队约束
   需要改引擎源码           → vLLM 或 SGLang（Python 为主）
   已有 Triton 部署体系      → TensorRT-LLM
```

**选型的第一原则是「团队能维护」**。性能差距通常在 20%-50% 的量级，而选一个团队无法定位问题的引擎，代价远高于此。

---

## 5. 对比测试的方法

要自己比较两个引擎，必须控制的变量：

| 变量 | 说明 |
|---|---|
| 模型与权重文件 | 同一份权重，同一精度 |
| 硬件 | 同一张卡，同一驱动与 CUDA 版本 |
| 负载 | 同一份请求序列（输入输出长度分布相同），最好用固定的数据集 |
| 并发 | 相同的并发数或到达率 |
| 优化开关 | 明确列出各自启用了什么（前缀缓存、chunked prefill、量化、CUDA Graph） |
| 指标口径 | TTFT、TPOT 的定义一致（第 04 章） |
| 预热 | 都跑够预热，排除首次编译与缓存冷启动 |

常见的不公平对比：

- 一边开了前缀缓存另一边没开
- 一边用了量化另一边没用
- 用固定长度的合成负载，掩盖了调度差异（真实负载的长度方差才是调度器的考验）
- 只报吞吐不报延迟，或只报平均值不报分位数
- 预热不足，把首次 CUDA Graph 录制的耗时算进去

第 19 章给出完整的报告字段清单。

---

## 6. 生态与周边

选引擎时还要看周边工具是否齐备：

| 项 | 说明 |
|---|---|
| 压测工具 | vLLM 与 SGLang 都自带 benchmark 脚本，可以直接用 |
| 指标导出 | 是否有 Prometheus 端点，指标是否够用（第 16 章的清单） |
| 部署方案 | Kubernetes 的 Operator、Helm chart、KServe / Ray Serve 集成 |
| 路由层 | 是否有配套的前缀感知路由（vLLM 有 router 项目，SGLang 内置 Router） |
| 模型转换 | 权重格式的转换与量化工具链 |

对生产部署而言，这些周边的成熟度往往比引擎本身的性能更影响落地速度。

---

## 7. 面试中的回答框架

被问到「你会选哪个引擎」时，直接给出一个名字是不完整的回答。可用的框架：

```
1. 先问清楚约束：硬件、模型、负载特征（输入输出长度分布）、SLO
2. 说明这些约束如何影响选择（第 4 节的排除路径）
3. 给出默认选择与理由
4. 说明如何验证（第 5 节的对比方法）
5. 说明什么情况下会改变这个选择
```

这个框架的价值在于展示你知道**选型依赖于场景参数**，而不是背诵功能表。第 04 章的输入输出长度分布表是最有用的提问点：同一套系统在长输入短输出与短输入长输出两种负载下的最优配置完全不同。

本章没有配套代码。
