# 19 · 压测与性能分析

前面各章的所有结论最终都要靠测量验证。本章讲怎么测才有意义：口径怎么定、负载怎么构造、结果怎么读、瓶颈怎么定位。

一份不可比较的压测数据没有价值。本章的核心是让数据可比较。

---

## 1. 最重要的一件事：开环还是闭环

### 1.1 两种模式

| 模式 | 做法 | 对应真实场景 |
|---|---|---|
| 闭环 | 固定 N 个在途请求，一个完成立刻补一个 | 批处理作业、固定并发的爬虫 |
| 开环 | 按固定到达率发送，不管系统处理得过来与否 | 在线服务的真实用户流量 |

**多数压测工具的默认是闭环**（指定 `--concurrency N`），而真实的在线流量是开环的。

### 1.2 差别有多大

第 6 节的模拟（同一系统，同一负载分布）：

| 模式 | 参数 | 吞吐 | TTFT P50 | TTFT P99 | goodput |
|---|---|---|---|---|---|
| 闭环 | 并发 8 | 448 | 0.06 s | 0.49 s | 448 |
| 闭环 | 并发 32 | 1024 | 0.08 s | 1.74 s | 1021 |
| 闭环 | 并发 64 | 1308 | 0.11 s | 4.61 s | 1168 |
| 开环 | 4 req/s | 750 | 0.09 s | 0.89 s | 748 |
| 开环 | 8 req/s | 1238 | 0.28 s | 5.14 s | 731 |
| 开环 | 16 req/s | 1199 | 12.15 s | 32.31 s | 24 |
| 开环 | 32 req/s | 1218 | 18.42 s | 44.04 s | 40 |

闭环模式下 TTFT P50 始终在 0.1 秒左右，即使并发到 64。原因是**闭环是自限流的**：请求完成后才发下一个，队列长度恒为 0，永远不会出现积压。

开环模式下到达率超过系统能力时，队列持续增长，TTFT 从 0.28 秒涨到 18 秒。

### 1.3 各自的用途

| 目的 | 应当用 |
|---|---|
| 测系统的最大吞吐 | 闭环，逐步加并发直到吞吐不再增长 |
| 测在给定流量下能否满足 SLO | 开环，用目标 QPS |
| 做容量规划 | 开环，扫描到达率找 goodput 拐点 |
| 对比两个引擎的峰值性能 | 闭环，控制其他变量 |

**只做闭环压测会得出「系统能扛住 N 并发」的乐观结论**，上线后在同样的负载下出现排队。这是压测中最常见的错误。

---

## 2. 分位数是必需的

同一次压测（开环 12 req/s）：

| 指标 | 平均值 | P50 | P95 | P99 | P99/P50 |
|---|---|---|---|---|---|
| TTFT | 9.891 s | 10.062 s | 23.075 s | 24.330 s | 2.4 |
| ITL | 53.7 ms | 22.3 ms | — | 349.7 ms | 15.7 |

ITL 的平均值是 53.7 ms，P50 是 22.3 ms，P99 是 349.7 ms。只报平均值会同时掩盖两件事：多数请求的实际体验（比平均值好得多）与最差请求的体验（比平均值差得多）。

长尾的来源：长请求、prefill 干扰（第 04 章）、抢占（第 09 章）。本次压测中抢占了 131 次。

**报告中至少需要 P50、P95、P99 三个分位数。**

---

## 3. 最大吞吐点不是最优工作点

扫描到达率（开环）：

| 到达率 | 吞吐 | TTFT P95 | ITL P99 | goodput | goodput 占比 | 抢占 |
|---|---|---|---|---|---|---|
| 2 | 397 | 0.35 s | 93.1 ms | 397 | 100% | 0 |
| 4 | 750 | 0.47 s | 134.1 ms | 748 | 100% | 0 |
| 6 | 1063 | 0.67 s | 179.7 ms | 1047 | 98% | 0 |
| 8 | 1238 | 4.16 s | 298.9 ms | 731 | 59% | 50 |
| 10 | 1188 | 17.35 s | 357.8 ms | 44 | 4% | 122 |
| 16 | 1199 | 31.16 s | 451.0 ms | 24 | 2% | 132 |
| 30 | 1168 | 42.98 s | 478.0 ms | 22 | 2% | 129 |

（SLO：TTFT ≤ 2 s 且平均 ITL ≤ 50 ms）

吞吐在到达率 8 时达到 1238 并饱和，之后不再增长。但 goodput 在到达率 6 时达到峰值 1047，到达率 8 时降到 731，到达率 10 时崩塌到 44。

**系统的可用容量是 6 req/s，不是吞吐峰值对应的 8 req/s。** 超过这个点后请求虽然最终都完成了，但已经违反 SLO，对用户而言等同于失败。

容量规划应当使用 goodput 的拐点，并在此基础上留冗余。只看吞吐会高估容量三分之一以上。

抢占次数是一个很好的先行指标：它在到达率 8 时从 0 跳到 50，与 goodput 开始下降的位置一致。

---

## 4. 报告字段清单

缺少任何一项，结果都无法与其他人的数据比较。

**环境**

- GPU 型号与数量
- 驱动与 CUDA 版本
- 引擎名称与版本
- 并行配置 TP / PP
- 宿主机 CPU 与内存

**模型**

- 模型名称与版本
- 权重精度
- KV cache 精度
- 量化方案与校准数据
- 上下文长度上限

**引擎配置**

- `gpu_memory_utilization`
- `max_num_seqs`
- `max_num_batched_tokens`
- `block_size`
- 是否启用前缀缓存
- 是否启用 chunked prefill 及 chunk 大小
- 是否启用 CUDA Graph
- 是否启用投机解码及其配置

**负载**

- 数据来源（真实日志 / 公开数据集 / 合成）
- 输入长度 P50 / P95 / 最大
- 输出长度 P50 / P95 / 最大
- 压测模式（开环到达率 / 闭环并发数）
- 总请求数与持续时间
- 预热请求数

**结果**

- 输出吞吐、总吞吐、请求吞吐
- TTFT 的 P50 / P95 / P99
- TPOT 或 ITL 的 P50 / P95 / P99
- 端到端延迟的 P50 / P95 / P99
- goodput 及其 SLO 定义
- 错误率
- 前缀缓存命中率
- 抢占次数
- 峰值并发序列数

最常被遗漏的四项：**压测模式、预热、前缀缓存是否开启、负载的长度分布**。

---

## 5. 负载的构造

### 5.1 优先用真实数据

按可信度排序：

| 来源 | 可信度 | 说明 |
|---|---|---|
| 线上日志回放 | 最高 | 长度分布、到达模式、前缀重复度都真实 |
| 公开数据集（ShareGPT 等） | 中 | 长度分布接近真实对话，但前缀重复度与实际业务不同 |
| 按线上统计合成 | 中 | 用真实的长度分布参数生成 |
| 固定长度合成 | 最低 | 掩盖调度差异 |

**固定长度的合成负载是最常见的问题**。所有请求输入 1024 输出 128 时，调度器面对的是一个理想化的场景：没有长度方差，没有长请求阻塞，静态批处理与连续批处理的差距会大幅缩小（第 08 章）。这类数据无法反映真实表现。

### 5.2 前缀重复度

如果目标场景是 Agent 或多轮对话，负载中必须包含真实比例的前缀重复。第 07 章的数据显示命中率从 0 到 84% 时，prefill 的计算量相差 6 倍以上。用无重复的合成负载测出的结果，与开了前缀缓存的线上表现完全不同。

### 5.3 预热

需要预热的原因：

- CUDA Graph 的首次录制
- kernel 的 JIT 编译（Triton 后端）
- 前缀缓存从空开始
- GPU 的频率爬升

预热请求数建议不少于总请求的 10%，且预热部分的数据不计入统计。

---

## 6. 瓶颈定位

### 6.1 先算再测

在测之前先用第 02 章的模型算一个预期值。实测与估算差 3 倍以上说明存在配置问题，而不是需要微调。

### 6.2 排查顺序

| 现象 | 排查 |
|---|---|
| 吞吐远低于估算 | 对照 roofline 估算；先查 batch 是否上得去（看峰值并发序列数） |
| 并发上不去 | 查 KV 池容量（第 05 章）与 `max_num_seqs`；看是否有抢占 |
| TTFT 高但队列为空 | prefill 计算本身慢：查输入长度、前缀命中率、chunk 大小 |
| TTFT 高且队列很长 | 容量不足：扩容或调整准入控制 |
| ITL 毛刺大 | prefill 干扰：查 chunked prefill 配置；查抢占次数 |
| 吞吐正常但延迟差 | batch 过大：降低 `max_num_seqs` 换取延迟 |
| 结果不可复现 | 查是否有其他负载共享 GPU；查温度墙与功耗限制；查预热 |

### 6.3 工具

| 工具 | 用途 |
|---|---|
| 引擎自带的 benchmark 脚本 | 首选。vLLM 与 SGLang 都有，指标口径与引擎一致 |
| 引擎的 Prometheus 指标 | 队列长度、KV 使用率、抢占次数，是分层定位的依据 |
| `nvidia-smi` / DCGM | 显存占用、功耗、温度。利用率仅作参考（第 16 章） |
| PyTorch profiler | 定位到算子级，看单步内的时间分布 |
| Nsight Systems (`nsys`) | 看 kernel 时间线、CPU 与 GPU 的重叠、通信与计算的重叠 |
| Nsight Compute (`ncu`) | 单个 kernel 的深度分析，看是否达到带宽或算力上限 |

无 GPU 时可用的部分：引擎指标的理解、报告口径的设计、数据处理与分析。第 7 节的代码覆盖这些。

---

## 7. 一份可用的压测流程

```
1. 明确目标：是找最大吞吐，还是验证 SLO，还是对比两个配置
2. 用第 02 章的模型算出预期值
3. 准备负载：优先真实日志；记录长度分布
4. 选模式：验证 SLO 用开环；找峰值用闭环
5. 预热，丢弃预热数据
6. 扫描参数（到达率或并发），每个点跑足够长（至少数百请求）
7. 记录第 4 节清单中的全部字段
8. 画三条曲线：吞吐-负载、延迟分位数-负载、goodput-负载
9. 找 goodput 拐点，作为容量规划的工作点
10. 若实测与估算差 3 倍以上，回到第 6 节排查
```

第 8 步的三条曲线是压测报告的核心。只有一个数字的报告无法支撑任何决策。

---

## 8. 代码

`code/ch19_benchmark.py` 包含：

1. 闭环与开环两种压测模式的实现与对比
2. 平均值与分位数的对比
3. goodput 随到达率的扫描，找拐点
4. 报告字段清单与排查顺序表

运行：

```bash
python3 code/ch19_benchmark.py
```

建议的实验：

- 把负载改为固定长度（去掉 `lognormvariate` 与 `expovariate` 的随机性），观察闭环与开环的差距缩小、长尾消失。这说明为什么固定长度的合成负载会给出乐观的结论。
- 调整 `SLO_TTFT` 与 `SLO_TPOT`，观察 goodput 拐点的移动。这是「容量取决于 SLO 定义」的直接体现。
