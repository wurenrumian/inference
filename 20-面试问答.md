# 20 · 面试问答与自查清单

本章把前面各章的内容整理为可回答的形式，并给出自查方法。

**使用原则**：标注为「未验证」或「原因未查明」的内容不要作为结论陈述。面试中说「这一点我没有实测过，只看过论文的数据」是可接受的；把估算说成实测则不可接受，且很容易被追问暴露。

---

## 1. 基础机制

### Q1 一个请求进来之后发生了什么

**考察点**：是否理解排队点，而不是流程背诵。

**要点**：反序列化与聊天模板 → tokenize → 进入 waiting 队列 → 调度器决策 → 分配 KV block → 构造输入张量 → 前向 → 采样 → 更新状态与结束判定 → detokenize → 流式返回 → 释放 block。

**关键是指出三个排队点**：等待进入 running、调度器因预算不足退回、block 池耗尽。并说明 TTFT = 前处理 + 排队 + prefill 计算 + 首次采样，低负载时由 prefill 决定，高负载时由排队决定，两者的优化手段不同。

**加分**：提到 CPU 与 GPU 的重叠、客户端断开的检测、prefill 与 decode 的混合。

参见第 01 章。

### Q2 prefill 与 decode 有什么区别

**要点**：输入（整个 prompt vs 1 个 token）、计算形态（矩阵乘矩阵 vs 矩阵乘向量）、算术强度（约等于序列长度 vs 约等于 batch）、瓶颈（算力 vs 显存带宽）、对应指标（TTFT vs TPOT）。

**加分**：说明批处理的作用相反——decode 增大 batch 几乎不增加单步耗时（权重访存与 batch 无关），prefill 增大 batch 不提高吞吐（已经计算受限）。给出量级：Llama-3-8B 在 A100 上 batch 1 到 32，decode 单步从 13.7 ms 到 19.6 ms，吞吐从 73 到 1632 token/s。

参见第 02、04 章。

### Q3 为什么 decode 是访存受限的

**要点**：一步 decode 要把全部模型权重从 HBM 读一遍，而计算量只有 `2 × 参数量 × batch`。因此算术强度约等于 batch。A100 的算力带宽比是 156，要进入计算受限区需要 batch 超过 156，而 KV cache 容量通常在此之前就限制住了。

**加分**：指出这是所有 decode 优化的出发点——既然权重读取的成本已经付出，就应当让它服务尽量多的请求。也是量化在 decode 上收益显著、在 prefill 上收益有限的原因。

参见第 02 章。

### Q4 KV cache 有多大，怎么算

**要点**：`2 × 层数 × KV头数 × head_dim × 精度字节数`。注意是 KV 头数不是 Q 头数。Llama-3-8B 是 128 KB/token/序列。

**加分**：给出显存总账——A100 80GB 跑 Llama-3-8B，权重 16 GB，运行时预留 8 GB，KV 可用 56 GB，8K 序列时最大并发 52。指出 KV cache 通常是权重的 2-4 倍，因此单卡跑多实例几乎总是错误配置。

**常见错误**：按参数量比例推 KV 大小。Llama-2-13B 的参数量只比 Llama-3-8B 多 60%，但 KV 单价高 6.25 倍（MHA vs GQA-8）。

参见第 05 章。

---

## 2. 显存与调度

### Q5 PagedAttention 解决什么问题

**要点**：连续分配的三种方案都不可行——按上限预留浪费 85% 以上；按当前长度分配则增长时需要搬移；按预测长度分配则需要预知输出长度。**根本原因是输出长度在生成结束前不可知**。

分页把 KV cache 切成固定大小的块，用 block table 做逻辑到物理的映射，消除外部碎片，内部碎片降到平均半个块。

**加分**：说明 kernel 需要改造（间接寻址，论文报告 20%-26% 开销）、`slot_mapping` 的作用、与操作系统分页的差异（没有缺页机制，因此分配失败必须由调度器在计算前处理）。

参见第 06 章。

### Q6 block_size 怎么选

**要点**：四个方向的权衡——内部碎片（越小越少）、block table 开销（越小越大）、访存连续性（越小越差）、前缀共享粒度（越小命中率越高）。vLLM 默认 16，SGLang 用 1（配合基数树）。

**加分**：给数据。block_size 从 16 到 128，空间利用率从 98.8% 降到 91.1%，前缀命中率从 84.2% 降到 73.1%。

参见第 06、07 章。

### Q7 前缀缓存怎么实现，会不会影响输出

**要点**：链式块哈希——每个块的哈希由「前一个块的哈希 + 本块 token」计算，因此哈希相同意味着从头到本块的内容完全相同。查找时从第 0 块开始逐块匹配，遇到第一个未命中就停止。

**不影响输出**：KV 只由 token 内容、位置、模型权重决定，采样参数作用在 logits 上不影响 KV。

**加分**：指出会破坏共享的因素——不同 LoRA、不同多模态输入必须计入哈希，否则会误命中且难以复现。提到多租户下的侧信道问题。

**加分**：给数据。Agent 类负载下命中率可达 84%，prefill 计算量降到六分之一。但收益完全取决于负载重复度，报告时必须说明负载构成。

参见第 07 章。

### Q8 连续批处理是什么，收益从哪来

**要点**：调度粒度从「一批请求」降到「一步迭代」，每步重新决定 batch 组成。静态批处理下短请求要等最长的那个完成，而输出长度是长尾分布，浪费严重。

**加分**：给数据。同一负载下静态批处理与连续批处理的吞吐接近（1065 vs 1074 token/s），但 TTFT P50 相差三个量级（89.45 s vs 0.09 s），goodput 从 3 升到 1068。**说明只看吞吐会得出错误结论**。

参见第 08 章。

### Q9 调度器每一步要做哪些决策

**要点**：
1. 接收新到达的请求
2. 检查显存余量，必要时抢占
3. 在 token 预算内选请求做 prefill
4. 运行中的请求各做一步 decode，受序列数上限约束
5. 执行前向、采样、更新状态、释放已完成请求的 block

两个预算：`max_num_batched_tokens` 约束 prefill，`max_num_seqs` 约束 decode。

**加分**：说明 vLLM V1 把调度结果统一为「每个请求本步处理多少 token」，prefill 是一个 chunk，decode 是 1，从而让混合执行成为唯一代码路径。

参见第 08 章。

### Q10 显存不够时怎么办

**要点**：必须抢占，否则死锁——运行中的序列需要新 block 才能继续，等待中的请求需要 block 才能开始，两边都无法推进。分页没有缺页机制，请求不能「暂停一下」。

两种恢复：换出到主机内存，或丢弃 KV 重新 prefill。

**加分**：说明为什么实际选重算——被抢占的块进入可复用池而非立即清零，重新调度时前缀常能 100% 命中，代价接近 0；且重算可以与其他 prefill 合并。vLLM V1 已取消 swap 路径。

**加分**：说明抢占的连锁效应——抢占一个 16000 token 的序列，代价相当于系统少产出 1847 个 token（batch 32 时）。因此抢占应视为异常，准入控制比抢占策略更重要。第 08 章的数据：加准入控制后抢占从 50 次降到 0，goodput 从 40 升到 68。

参见第 09 章。

---

## 3. 性能优化

### Q11 chunked prefill 的作用与代价

**要点**：把长 prompt 的 prefill 切成小块，与 decode 混在同一步，使单步耗时均匀。代价是每个 chunk 都要重读一遍权重、重读一遍已有前缀的 KV。

**加分**：给数据。32768 token 的 prompt，chunk 从 32768 降到 512，最大单步耗时从 3720 ms 降到 99 ms（37 倍），prefill 总耗时上升 37%。

**加分（重要）**：说明效果依赖负载状态。接近饱和时 chunked prefill 把 ITL P99 从 342 ms 降到 189 ms；过载时反而恶化到 627 ms，因为它延长了请求在系统中的停留时间，加剧显存竞争。**chunked prefill 是平滑手段不是扩容手段。**

参见第 04、08、10 章。

### Q12 PD 分离的动机是什么，什么条件下值得做

**要点**：消除干扰只是收益之一（chunked prefill 也能部分做到）。真正无法用聚合部署实现的是**独立调整资源配比**。不同负载需要的 P:D 比例跨越 40 倍——对话是 1:2.1，代码补全是 21.9:1。

**加分**：给传输账。8K 序列的 KV 是 1.07 GB，25Gb 以太网上传输 343 ms，占 prefill 计算的一半，不可用；200Gb RDMA 上是 43 ms，占 6%。逐层流水可以把传输隐藏，25Gb 上收益 32%，NVLink 上收益 0.3%。

**加分**：说明适用条件——至少 4 卡、200Gb 以上 RDMA、负载稳定、SLO 严格、有运维能力。不满足时用聚合部署加 chunked prefill。

参见第 10 章。

### Q13 FlashAttention 省的是什么

**要点**：**省的是显存与访存，不是计算，也不是精度**。它不是近似算法，分块计算的结果与一次性计算完全一致（误差在浮点舍入范围内）。

机制是 online softmax：分块计算，用运行中的最大值与归一化因子增量更新输出，遇到更大的最大值时按比例重缩放已累计的结果。

**加分**：给数据。中间矩阵从 `[N, N]`（32K 序列时 2 GB）降到固定的 `[64, 64]`（8 KB）。访存量在 N=32768 时减少 42.8 倍。指出计算量没有减少，甚至因为重复读 KV 略有增加。

**加分**：提到 FlashDecoding——decode 时 query 只有 1 个 token，并行度只有 batch × 头数，batch 小时 GPU 空闲，因此需要把 KV 序列维度也切开并行。

参见第 11 章。

### Q14 量化能带来多少收益

**要点**：先问场景。decode 受益（访存受限），prefill 收益有限（计算受限）。

**加分**：给数据并指出优化对象要匹配瓶颈。Llama-3-8B、batch 32、序列 8192 时，int4 权重 + fp16 KV 只有 1.31x，因为该场景下 KV 访存占 69%；int4 权重 + int8 KV 是 2.38x。

**加分**：说明误差的主要来源是异常值，以及三种方案的思路（GPTQ 用 Hessian 补偿、AWQ 保护重要通道、SmoothQuant 迁移异常值）。指出粒度比位宽更关键——int8 per-token 的误差优于 fp8 e4m3。

**常见错误**：只报重建误差不报下游任务指标。

参见第 13 章。

### Q15 投机解码什么时候有用，什么时候会变慢

**要点**：利用 decode 阶段富余的算力，一次前向验证多个候选 token，把权重访存成本摊到多个 token 上。

加速比 = 期望产出 token 数 / (k·c + 1)，其中期望产出 = (1-α^(k+1))/(1-α)。接受率 α 是决定性因素：0.5 时上限约 1.5 倍，0.9 时可达 3.4 倍。

**会变慢的情况**：
1. 接受率低于门槛（c=0.1、固定 k=5 时需要 α > 0.34）
2. batch 大（算术强度进入计算受限区）。batch 1 时 1.98x，batch 256 时降到 1.36x

**加分**：说明它是无损的——拒绝采样保证输出分布严格等于目标模型的分布，接受率的理论值是 `Σ min(p, q)`（两个分布的重叠面积）。因此草稿模型的训练目标是拟合目标模型的分布，不是拟合真实数据。

**加分**：说明实现难点——草稿模型也要 KV cache；被拒绝的 token 的 KV 要回滚，打破了「KV 只增不减」的假设。

参见第 14 章。

---

## 4. 分布式与服务

### Q16 张量并行为什么不跨机

**要点**：每层 2 次 all-reduce，一步 decode（32 层）就是 64 次通信。通信占单步耗时的比例：NVLink 上 TP=8 是 13.1%，跨机 RDMA 200Gb 是 37.9%，100Gb 以太网是 52.1%。

**加分**：指出 decode 的通信是小消息（16-30 MB）且每层同步，因此对延迟比对带宽更敏感——64 次通信的固定发起开销累加起来与传输时间同量级。

**加分**：说明 TP 是用吞吐换延迟。batch 1 时 TP=8 能把延迟降到 1/6.7，但 8 卡跑一个 TP=8 实例的总吞吐低于 8 个独立实例。选择依据是 SLO：TPOT 已满足要求时应降低 TP 提高吞吐。

参见第 15 章。

### Q17 MoE 为什么部署困难

**要点**：MoE 节省的是计算，而 decode 的瓶颈是访存。batch 增大后不同 token 选中不同专家，激活的专家集合迅速覆盖全部专家——64 个专家、每 token 激活 8 个的配置下，batch 32 时期望激活 63.1 个专家，权重读取量是单 token 的 7.9 倍。

**结论**：MoE 在 decode 阶段的算术强度低于同等激活参数量的稠密模型。

**加分**：说明专家并行的通信模式（all-to-all）与负载不均问题。

参见第 15 章。

### Q18 多实例怎么路由

**要点**：推理实例是有状态的（前缀缓存），因此轮询不是最优。前缀感知路由把需要计算的 token 减少 38%。

**加分**：给数据。实例数越多轮询的命中率下降越快——32 实例时轮询 67.0%，前缀感知 83.4%，计算量相差一倍。**集群规模越大，路由策略越重要。**

**加分**：说明亲和与均衡的冲突。纯前缀感知在只有 4 个 system prompt 的负载下会让 8 个实例中 4 个完全空闲。生产实现用「命中长度 - 负载惩罚」的加权打分。

**加分**：说明三种实现方式与各自的问题（一致性哈希在实例增减时缓存大面积失效；影子索引与实际淘汰不一致）。

参见第 16 章。

### Q19 用什么指标做弹性扩缩

**要点**：**不能用 GPU 利用率**。`nvidia-smi` 的利用率只表示有 kernel 在执行，decode 阶段访存受限，利用率可能显示 90% 而算力只用了几个百分点。显存占用也不行，因为引擎预分配 KV 池。

应当用：队列长度、KV 池使用率作为扩缩依据；TTFT 分位数、抢占次数作为告警。

**加分**：说明冷启动耗时 2-10 分钟（拉容器、加载权重、显存探测、CUDA Graph 录制、预热），因此弹性策略必须是预测式而非响应式。

参见第 16 章。

---

## 5. 压测与实践

### Q20 怎么压测才有意义

**要点**：先区分开环与闭环。多数压测工具默认闭环（固定并发），而真实在线流量是开环的。闭环是自限流的，队列长度恒为 0，会掩盖过载时的排队延迟。

**加分**：给数据。同一系统，闭环并发 64 时 TTFT P50 是 0.11 s；开环 16 req/s 时是 12.15 s。只做闭环压测会得出乐观结论。

**加分**：说明最大吞吐点不是最优工作点。吞吐在 8 req/s 饱和于 1238 token/s，但 goodput 在 6 req/s 达到峰值 1047，8 req/s 时降到 731，10 req/s 时崩塌到 44。容量规划应当用 goodput 拐点。

**加分**：给出报告字段清单（环境、模型、引擎配置、负载、结果五类），并指出最常被遗漏的四项：压测模式、预热、前缀缓存是否开启、负载长度分布。

参见第 19 章。

### Q21 一个开放题：怎么优化我们的推理服务

**这是最常见的系统设计题。不要直接给方案。**

回答框架：

```
1. 先问负载特征
   - 输入输出长度的分布（P50、P95）
   - QPS 的峰值与均值、波动模式
   - 前缀重复度（是否 Agent、是否固定 system prompt）
   - SLO：TTFT 与 TPOT 的目标与分位数

2. 先问现状
   - 当前的引擎、模型、卡型、并行配置
   - 当前的 TTFT / TPOT 分位数、吞吐、goodput
   - 队列长度、KV 池使用率、抢占次数、前缀命中率

3. 按数据定位瓶颈（第 19 章的排查表）

4. 按瓶颈选手段
   - 排队为主 → 扩容、准入控制、路由
   - prefill 为主 → 前缀缓存、前缀感知路由、chunked prefill 调参
   - decode 为主 → 提高 batch、KV 量化、权重量化、TP 配置
   - ITL 毛刺 → chunked prefill、准入控制减少抢占

5. 说明验证方法与预期收益的量级
```

**这个框架的价值在于展示你知道优化手段的选择依赖场景参数。** 直接说「用 vLLM 开前缀缓存」是不完整的回答。

---

## 6. 自查清单

能独立回答下列问题（不查资料、能给出量级）即为掌握：

**计算**

- [ ] 7B 模型一步 decode 要读多少字节？算多少 FLOPs？
- [ ] A100 的算力带宽比是多少？它意味着什么？
- [ ] Llama-3-8B 的 KV cache 每 token 多少 KB？怎么算的？
- [ ] 80GB 卡跑 8B 模型，8K 序列能并发多少？
- [ ] prompt 长度 8192 时 attention 占 prefill 计算量的多少？

**机制**

- [ ] 三个排队点分别在哪里？
- [ ] block table 与 slot mapping 分别是什么？
- [ ] 块哈希为什么要链式计算？
- [ ] 抢占为什么是正确性要求而不是优化？
- [ ] online softmax 中的重缩放系数起什么作用？
- [ ] 拒绝采样为什么能保证分布不变？

**权衡**

- [ ] block_size 的四个权衡方向？
- [ ] chunk 大小影响哪两个指标？往哪个方向？
- [ ] 最短作业优先的代价是什么？
- [ ] TP 增大对延迟与吞吐的影响方向相反，为什么？
- [ ] 前缀亲和与负载均衡为什么冲突？

**方法**

- [ ] 开环与闭环压测分别用于什么？
- [ ] 一份可比较的压测报告需要哪些字段？
- [ ] TTFT 变高时的排查顺序？
- [ ] 为什么不能用 GPU 利用率做扩缩依据？

---

## 7. 在没有 GPU 的条件下能做什么

按说服力排序：

| 产出 | 说明 |
|---|---|
| 合入的 PR | 文档修正、测试补充、good first issue。说服力最高 |
| 源码分析文档 | 画出 vLLM 一步迭代的完整调用图，标注每个模块的职责与关键数据结构 |
| 机制的复现实现 | 本教材的代码就是这类产出：block manager、前缀缓存、调度器、FlashAttention 的可运行实现，带数值一致性验证 |
| 定量分析 | 用 roofline 模型分析某个模型在某张卡上的理论性能，与公开的实测数据对比并解释差异 |
| 调度策略的模拟对比 | 第 08、19 章的模拟器，能给出策略选择的定量依据 |

面试中描述这类工作时的要点：**说清楚你验证了什么**。「我实现了一个 block manager」不如「我实现了 block manager 并用引用计数的泄漏检查验证了 fork 与写时复制的正确性，同时对比了三种显存管理方式在同一容量下的拒绝率」。

租用 GPU 做一次完整的压测（第 19 章的流程）是性价比最高的补充：一份带完整口径的性能数据与分析，本身就可以作为简历上的一个项目条目。所需成本是数小时的按量计费。

---

## 8. 与已有经历的衔接

本教材面向的读者已有系统方向的经历。几个可以直接对接的点：

| 已有经历 | 对接点 |
|---|---|
| RDMA 数据面 | PD 分离的 KV 传输（第 10 章）。注意模式不同：这里是高频中等消息、对尾延迟敏感的逐层流水，而非大块单边读写 |
| 调度控制面 | 第 16 章的服务层。差别是实例有状态、扩容需分钟级、请求资源消耗事前不可知 |
| 高性能存储 | 第 06 章的 block manager 与操作系统内存管理、数据库缓冲池是同类问题 |
| LLM Agent 后端与提示词缓存 | 第 07 章。API 侧的 prompt caching 与引擎内的前缀缓存是同一机制在不同层的体现 |

**衔接的说法要准确**。「我做过 RDMA，所以懂 PD 分离」是不成立的；「PD 分离的 KV 传输用的是逐层流水的中等消息模式，我在 RecStore 做的是大块单边读写，两者对链路的要求不同，前者更看重单次发起开销」是可以展开的。

---

本教材到此结束。各章的代码位于 `code/`，可以直接运行并修改参数复现全部数据。
