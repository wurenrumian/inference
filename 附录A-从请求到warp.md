# 附录 A · 从一个请求到一个 warp

第 01 章追踪了一个请求在引擎中的路径，终点是「模型前向」这一步。第 11 章讲了 kernel 层的算法。本章把两者之间的空隙补上：一次前向到底怎么在 GPU 上执行，一个 warp 在其中做什么。

读完本章应当能回答：为什么 decode 达不到理论带宽、为什么算子融合有上限、为什么 batch 小时 GPU 会空闲、profile 报告里的那些指标各自意味着什么。

**本机无 GPU，本章的全部数值为计算结果而非实测。** 硬件参数以 A100（SM80）为准，属于量级参考。

---

## 1. 先建立词汇

### 1.1 执行单元的层次

```
GPU
 └── SM（Streaming Multiprocessor）× 108        ← 硬件上的独立处理单元
       └── 处理块（processing block）× 4         ← 每个有一个 warp 调度器
             └── warp（32 个线程）
                   └── 线程
```

对应到软件侧：

```
kernel 启动时指定 grid 与 block
 └── grid：线程块的总数，例如 (1024, 32)
       └── 线程块（thread block）：被整体分派到某一个 SM，不会跨 SM
             └── warp：块内的线程按 32 个一组自动划分
                   └── 线程
```

三个关键约束：

| 约束 | 数值（A100） | 后果 |
|---|---|---|
| 一个线程块只能在一个 SM 上执行 | — | 块内可以用共享内存通信，跨块不行 |
| 每个 SM 最多驻留 64 个 warp | 2048 线程 | 超出的线程块要等前面的执行完 |
| 整卡最大并发线程数 | 221184 | 这是能同时「在飞」的上限 |

### 1.2 warp 是调度的最小单位

一个 warp 里的 32 个线程执行同一条指令，作用在不同的数据上。这带来两个后果：

**分支发散。** 若 warp 内一半线程走 if 分支、一半走 else，硬件会先执行 if 分支（else 的线程闲置），再执行 else 分支（if 的线程闲置），耗时是两者之和。因此 kernel 里的分支应当尽量让整个 warp 走同一条路。

**访存合并。** 32 个线程同时发出访存请求。若它们的地址连续，硬件把这些请求合并成少数几次显存事务；若地址分散，则要发多次。这是第 5 节的内容。

### 1.3 内存层次

| 层级 | 容量 | 延迟（周期） | 作用域 |
|---|---|---|---|
| 寄存器 | 256 KB/SM | 1 | 单个线程私有 |
| 共享内存 | 164 KB/SM | 30 | 线程块内共享，软件管理 |
| L2 | 40 MB | 200 | 全卡共享，硬件管理 |
| HBM | 80 GB | 500 | 全卡 |
| 对端卡显存（NVLink） | 80 GB | 2000 | 跨卡 |
| 主机内存（PCIe） | TB 级 | 20000 | 跨设备 |

延迟数字比容量更重要。把它换算成「等待期间本可以做多少运算」：

一个 SM 每周期可发射约 2049 次 fp16 tensor core 运算（312 TFLOPS / 108 SM / 1.41 GHz）。于是：

| 等待一次 | 周期 | 相当于浪费的运算次数 |
|---|---|---|
| 共享内存访问 | 30 | 61466 |
| L2 命中 | 200 | 409771 |
| HBM 访问 | 500 | 1024429 |

**一次 HBM 访问的等待时间，足够做一百万次浮点运算。** 这个数字解释了 GPU kernel 设计的全部动机：把数据留在高层级，以及在等待时切换到其他 warp。

---

## 2. T0：请求到达，CPU 侧

以第 01 章的路径为起点，重点看接近硬件的部分。

### 2.1 调度决策完成之后

调度器（第 08 章）产出 `SchedulerOutput`，其中最关键的字段是「每个请求本步处理多少 token」。以一步混合执行为例：

```
32 个 decode 请求，各 1 个 token
1 个 prefill 请求，一个 1024 token 的 chunk
本步 token 总数 = 1056
```

### 2.2 构造输入张量（CPU）

引擎在 CPU 上组装几个数组：

| 张量 | 形状 | 内容 |
|---|---|---|
| `input_ids` | [1056] | 全部 token 的 id，decode 与 prefill 的拼在一起 |
| `positions` | [1056] | 每个 token 在其序列中的绝对位置 |
| `slot_mapping` | [1056] | 每个 token 的 KV 要写入的物理槽位编号 |
| `block_tables` | [33, 最大块数] | 每个序列的逻辑块到物理块的映射 |
| `seq_lens` | [33] | 每个序列的当前长度 |
| `query_start_loc` | [34] | 变长序列的分界（前缀和），kernel 用它定位每个序列 |

`query_start_loc` 值得单独说明：本步 33 个序列的 query 长度不同（32 个是 1，1 个是 1024），拼成一维张量后，kernel 需要知道每个序列从哪里开始。这个前缀和数组就是索引，形如 `[0, 1, 2, ..., 32, 1056]`。这是变长 attention 接口的核心，也是混合执行成为可能的前提。

这些数组的构造是纯 CPU 工作，耗时在 0.5-3 毫秒。当一步 decode 只有 19.6 毫秒时，这部分占比可观，因此引擎会做增量更新（只改变化的部分）而不是每步重建。

### 2.3 拷贝到 GPU

```
cudaMemcpyAsync(d_input_ids, h_input_ids, ..., stream)
```

用 pinned memory（页锁定内存）作为源，拷贝可以由 DMA 引擎完成，不占用 CPU，也可以与 GPU 上正在执行的 kernel 重叠。若用普通内存，驱动需要先拷到一块内部的 pinned 缓冲区，多一次拷贝且无法异步。

这些张量很小（1056 个 int32 只有 4 KB），传输时间在微秒级，真正的成本是发起开销。

### 2.4 kernel 启动

CPU 把 kernel 提交到 stream 里，返回，不等待。这是异步的。

```
launch(embedding_kernel, grid, block, args, stream)
launch(rmsnorm_kernel,   ...)
launch(qkv_gemm_kernel,  ...)
...
```

每次启动有 5-10 微秒的固定开销（驱动侧的参数打包、命令写入队列）。一个 32 层的模型，每层 10 个 kernel，一步就是 320 次启动，仅此一项就是 1.6-3.2 毫秒。这是 CUDA Graph 要消除的（第 8.3 节）。

---

## 3. T1：第一个 kernel 落到 SM 上

以最简单的 embedding lookup 为例，建立「线程如何映射到数据」的图像。

### 3.1 任务

```
输入：input_ids [1056]，embedding 表 [128256, 4096]
输出：hidden    [1056, 4096]
做的事：对每个 token，把它的 id 对应的那一行 4096 个数拷出来
```

总共要搬 `1056 × 4096 × 2 字节 = 8.65 MB`。

### 3.2 线程配置

一种典型配置：

```
grid  = (1056,)        每个线程块负责一个 token
block = (256,)         每块 256 个线程 = 8 个 warp
```

块内的 256 个线程要搬 4096 个元素，每个线程搬 16 个：

```
线程 t 负责的元素下标：t, t+256, t+512, ..., t+3840
```

**为什么用这种跨步划分，而不是让线程 t 负责连续的 16 个元素？** 因为访存合并。跨步划分时，同一个 warp 的 32 个线程在同一条指令里访问的是 `t` 到 `t+31`，地址连续，合并成少数几次事务。若每个线程负责连续的 16 个，则 32 个线程访问的地址间隔 16 个元素，分散在 32 个不同的位置，事务数上升到 32 倍。

这是 GPU kernel 里最基础也最容易出错的一条：**相邻线程应当访问相邻地址。**

### 3.3 分派

1056 个线程块要分派到 108 个 SM 上。硬件按块为单位分派，一个 SM 上可以同时驻留多个块（受寄存器与共享内存限制，见第 6 节）。

```
1056 块 / 108 SM ≈ 每个 SM 分到 10 块
每块 8 个 warp → 每 SM 80 个 warp
但每 SM 最多驻留 64 个 warp
→ 实际先驻留 8 块（64 warp），剩下的排队等待
```

这个 kernel 是纯访存的，算术强度接近 0，因此它的耗时完全由 8.65 MB / 2 TB/s ≈ 4.3 微秒决定（加上启动开销和延迟隐藏不足的损失，实际约 10 微秒）。

---

## 4. QKV 投影：一个 GEMM kernel 的内部

这是 prefill 阶段最重要的一类 kernel。用具体形状：

```
输入 A: [2048, 4096]     2048 个 token 的隐状态
权重 B: [4096, 6144]     合并的 Q、K、V 投影
输出 C: [2048, 6144]
计算量: 2 × 2048 × 4096 × 6144 = 0.103 TFLOPs
```

### 4.1 为什么必须分块

最朴素的做法是每个线程算一个输出元素。那么每个输出元素要读 A 的一行（4096 个）和 B 的一列（4096 个）：

```
总读取 = 2 × 2048 × 6144 × 4096 × 2 字节 = 206 GB
算术强度 = 0.103e12 / 206e9 = 0.5 FLOPs/byte
```

算术强度 0.5，远低于平衡点 156。这个 kernel 会慢得没法用：206 GB / 2 TB/s = 103 毫秒，而计算本身只要 0.33 毫秒。

分块的做法是：一个线程块负责输出的一个 `[Tm, Tn]` 小块。它读入 A 的 `[Tm, K]` 和 B 的 `[K, Tn]`，产出 `Tm × Tn` 个结果。同一份数据被复用了 `Tm` 或 `Tn` 次。

| tile 边长 T | 读取量 | 相对不分块 | 算术强度 |
|---|---|---|---|
| 1 | 206.2 GB | 1.000 | 0.5 |
| 16 | 12.9 GB | 0.062 | 8.0 |
| 64 | 3.2 GB | 0.016 | 32.0 |
| 128 | 1.6 GB | 0.008 | 64.0 |
| 256 | 0.8 GB | 0.004 | 128.0 |

**算术强度约等于 tile 边长。** 这个表的模型假设每个 tile 都从 HBM 读；实际上权重会被多个 tile 复用并命中 L2（40 MB，本例的权重是 48 MB，大部分能装下），因此真实的 HBM 访存量更低。这是实际 kernel 用 T=128 左右就能接近算力上限的原因。

### 4.2 tile 大小的上限

tile 要放进共享内存。典型做法是沿 K 方向每次取 32，配合双缓冲（一边算当前块，一边预取下一块）：

| tile 边长 | 需要的共享内存 | 每 SM 可放几块 |
|---|---|---|
| 32 | 8.0 KB | 20 |
| 64 | 16.0 KB | 10 |
| 128 | 32.0 KB | 5 |
| 256 | 64.0 KB | 2 |

共享内存 164 KB/SM 是硬上限。tile 越大，能同时驻留的块越少，延迟隐藏能力越差。这是一个需要权衡的点，不是越大越好。

### 4.3 数据流

一个线程块的完整数据流：

```
HBM ──(1)──> L2 ──(2)──> 共享内存 ──(3)──> 寄存器 ──(4)──> tensor core
                              ↑                              │
                              └────────(5) 累加器留在寄存器 ───┘
```

1. 从 HBM 读入 A 的一个 `[128, 32]` 片段与 B 的一个 `[32, 128]` 片段
2. 经 L2（权重片段大概率命中，因为其他线程块刚读过）
3. 存入共享内存，供块内所有 warp 共享
4. 每个 warp 从共享内存把自己需要的部分读到寄存器
5. 累加结果全程留在寄存器里，直到整个 K 维度走完才写回

**第 5 步是关键。** 输出的 `[128, 128]` 个累加值一直在寄存器里，`K = 4096` 意味着要累加 128 轮（每轮 32）。若每轮都把中间结果写回显存，访存量会增加 128 倍。

### 4.4 一个 warp 在其中做什么

假设线程块是 256 线程（8 个 warp），负责 `[128, 128]` 的输出 tile。8 个 warp 划分这个 tile，例如每个 warp 负责 `[64, 32]` 的子块。

一个 warp 的循环体：

```
for k_step in range(K / 32):            # 128 轮
    等待共享内存中的第 k_step 块就绪（__syncthreads）
    从共享内存读 A 的片段到寄存器        # ldmatrix 指令
    从共享内存读 B 的片段到寄存器
    执行若干次 MMA 指令                  # tensor core
    发起下一块的异步拷贝                  # cp.async，与计算重叠
```

**MMA 指令**（matrix-multiply-accumulate）是 tensor core 的入口。Ampere 上的一条 fp16 MMA 指令形如 `m16n8k16`：一个 warp 的 32 个线程协作完成一次 `[16, 16] × [16, 8]` 的矩阵乘累加。

```
一条 MMA 的计算量 = 2 × 16 × 16 × 8 = 4096 FLOPs
一个 warp 用一条指令完成
```

对比普通的 fp32 乘加指令：一个 warp 32 个线程各做 1 次乘加 = 64 FLOPs。**MMA 指令的效率是它的 64 倍**，这就是 tensor core 与普通计算单元差两个量级的来源。

要覆盖 `[64, 32]` 的子块、K 方向 32 个元素：

```
需要的 MMA 次数 = (64/16) × (32/8) × (32/16) = 4 × 4 × 2 = 32 条
```

### 4.5 双缓冲与异步拷贝

`cp.async` 指令（Ampere 引入）允许从全局内存直接拷到共享内存，不经过寄存器，且是异步的：发起之后 warp 可以继续做别的事，等真正需要数据时再同步。

配合双缓冲，共享内存里同时存两块：

```
时刻 t：  计算第 k 块（数据在缓冲 0）    同时预取第 k+1 块（写入缓冲 1）
时刻 t+1：计算第 k+1 块（缓冲 1）        同时预取第 k+2 块（写入缓冲 0）
```

这样访存与计算完全重叠，只要计算时间不短于访存时间，HBM 延迟就被完全掩盖。这也是共享内存需求要乘 2 的原因（第 4.2 节的表）。

### 4.6 这个 kernel 的效率

prefill 的 GEMM 是所有 kernel 中效率最高的一类，实测通常能达到峰值算力的 70%-85%。达不到 100% 的原因：

| 损失来源 | 说明 |
|---|---|
| tile 边界不整除 | M、N 不是 tile 的整数倍时，边缘的块只做部分有效计算 |
| 首尾的流水填充 | 第一块没有可重叠的前一块，最后一块没有后一块 |
| 同步开销 | 每轮的 `__syncthreads` |
| 共享内存 bank conflict | 布局不当时同一 bank 被多个线程同时访问，串行化 |
| occupancy 不足 | tile 大时块少，延迟隐藏能力下降 |

---

## 5. FlashAttention kernel 的内部

### 5.1 grid 映射

```
grid = (查询块数, 头数, batch)
```

每个线程块负责「某个序列的某个头的某一段查询」。以 2048 长度、32 个头、block_q = 128 为例：

```
grid = (2048/128, 32, 1) = (16, 32, 1) = 512 个线程块
```

512 个块分派到 108 个 SM 上，每个 SM 约 5 块。足够填满。

### 5.2 块内的工作

一个线程块（假设 128 线程 = 4 个 warp）负责 `[128, 128]` 的查询块：

```
1. 把 Q 的 [128, 128] 载入共享内存（或直接到寄存器）
2. 初始化运行状态：m（每行的最大值）、l（指数和）、o（累加输出）
   这三个都留在寄存器里 —— 每行一个 m 和 l，共 128 个
3. for 每个 KV 块 j:
     a. 把 K_j、V_j 的 [128, 128] 载入共享内存（cp.async）
     b. 计算 S = Q · K_j^T          用 MMA，结果在寄存器
     c. 应用因果掩码（若整块被掩码则跳过，见第 11 章）
     d. 更新 m_new、alpha、p、l、o    纯寄存器运算
     e. 累加 o += p · V_j            用 MMA
4. 输出 o / l 写回 HBM
```

### 5.3 三个关键点

**运行状态在寄存器里。** m、l、o 全程不落显存。这是「中间矩阵大小固定」的实现方式——真正在共享内存里的只有当前的 K、V 块和 S 矩阵，尺寸由 block 大小决定，与序列长度无关。

**S 矩阵不落显存。** `[128, 128]` 的 S 算出来后立刻用于更新状态，然后丢弃。这就是第 11 章说的「省的是访存」——朴素实现要把 `[2048, 2048]` 的 S 写回再读出，FlashAttention 完全避免了。

**warp 之间的分工。** 4 个 warp 各负责查询块的一部分行（例如各 32 行）。由于 online softmax 的状态是按行维护的，不同 warp 处理不同行时互不干扰，不需要跨 warp 的规约。这是 FlashAttention-2 相对 v1 的一个改进点：v1 的划分方式需要更多的 warp 间同步。

---

## 6. PagedAttention 的 decode kernel

这是与 prefill 差别最大的地方。

### 6.1 形状的差别

```
prefill: 每个序列 2048 个 query，对 2048 个 KV
decode:  每个序列 1 个 query，对 2049 个 KV
```

query 从 2048 降到 1，意味着**没有可复用的维度**。prefill 里一份 K 被 2048 个 query 用，decode 里一份 K 只被 1 个 query 用。算术强度从上百降到约 1。

### 6.2 并行度问题

```
grid = (头数, batch)
```

batch 32、32 个头：`32 × 32 = 1024` 个线程块，分到 108 个 SM 上，约 10 块/SM。够用。

batch 1 时：`32 × 1 = 32` 个线程块。**108 个 SM 里有 76 个完全空闲。**

这就是 FlashDecoding 要解决的问题：把 KV 的序列维度也切开。

```
grid = (头数, batch, KV 分段数)
```

序列长 8192，切成 8 段，每段 1024：`32 × 1 × 8 = 256` 个块，SM 填满了。代价是每段各自算出一个部分结果（带各自的 m 和 l），最后需要一个额外的 kernel 把这些部分结果合并——合并时用的正是 online softmax 的那套加权逻辑（第 11 章 2.1.1 节）。

### 6.3 block table 的处理

分页之后 KV 不连续，kernel 要做间接寻址：

```
1. 线程块启动时，把该序列的 block table 载入共享内存
   （长度 = ceil(seq_len / block_size)，8192 长度、block_size 16 时是 512 个 int32 = 2 KB）
2. 遍历逻辑块：
     phys = block_table[逻辑块号]           从共享内存读，30 周期
     基址 = kv_cache_base + phys * block_stride
     从基址开始读 block_size 个 token 的 KV  连续访存，可合并
```

**关键在于 block table 只读一次**（载入共享内存），之后 512 次查表都命中共享内存。若每次都从全局内存读，会多出 512 次 HBM 访问。

### 6.4 访存合并的代价

块内连续、跨块跳跃。量化这个损失：

| block_size | 跳跃出现的频率 | 粗略的带宽损失 |
|---|---|---|
| 1 | 100% | 50% |
| 4 | 25% | 12.5% |
| 8 | 12.5% | 6.2% |
| 16 | 6.2% | 3.1% |
| 32 | 3.1% | 1.6% |
| 64 | 1.6% | 0.8% |

block_size 16 时跳跃损失约 3%，是可接受的。block_size 1（SGLang 的选择）在访存连续性上最差，需要在分配策略上做补偿——尽量把同一序列的块分配在相邻的物理位置。

这张表与第 06 章的内部碎片表构成 block_size 的完整权衡：太小则访存分散、block table 变长；太大则内部碎片、前缀共享粒度粗。

---

## 7. decode 的 GEMV：为什么达不到理论带宽

这是全书多次提到的「效率系数 0.7」的来源，值得单独展开。

### 7.1 形状

batch 32、hidden 4096 的一个投影层：

```
输入 A: [32, 4096]
权重 B: [4096, 4096]
输出 C: [32, 4096]
```

M = 32 很小。tile 划分时 `Tm` 最多只能是 32，而 GEMM kernel 的高效配置需要 `Tm` 在 128 以上。因此 decode 的矩阵乘无法用 prefill 那套 tile 策略，通常走专门的 GEMV 或 skinny-GEMM kernel。

### 7.2 一个 warp 的生命周期

典型的 GEMV kernel 划分：每个线程块负责输出的一段，块内的 warp 各负责若干列，每个线程沿 K 方向累加。

一个 warp 的循环体：

```
1. 发出一次 load，读 32 个连续的权重元素（合并访存，一次事务）
2. 等待约 500 个周期
3. 数据到达，做 32 次乘加（每线程 1 次）
4. 回到第 1 步
```

计算与访存的比例：

```
每次访存后的计算量 W ≈ 1 个周期（一次 FMA）
访存延迟 L = 500 个周期
```

### 7.3 需要多少 warp 才能掩盖

要让 SM 不空转，需要 `L / W = 500` 个 warp 轮流工作。而 A100 每 SM 最多驻留 64 个。

| 访存来源 | 每次计算 W | 需要的 warp 数 | 64 够不够 |
|---|---|---|---|
| 共享内存（30） | 1 | 30 | 够 |
| L2（200） | 1 | 200 | 不够，缺 136 |
| HBM（500） | 1 | 500 | 不够，缺 436 |
| HBM（500） | 4 | 125 | 不够，缺 61 |
| HBM（500） | 16 | 32 | 够 |
| HBM（500） | 64 | 8 | 够 |

**decode 的 GEMV 落在第三行**：需要 500 个 warp，只有 64 个可用。

### 7.4 离散事件模拟

用第 10 节的代码模拟一个 SM 上 N 个 warp 交替执行「访存 → 计算」，统计计算单元的忙碌比例：

| warp 数 | W=1 | W=4 | W=16 | W=64 |
|---|---|---|---|---|
| 1 | 0.2% | 0.8% | 3.2% | 11.5% |
| 4 | 0.8% | 3.2% | 12.6% | 45.8% |
| 8 | 1.6% | 6.4% | 25.1% | 90.9% |
| 16 | 3.2% | 12.9% | 50.0% | 90.0% |
| 32 | 6.5% | 25.7% | 99.2% | 89.5% |
| 64 | 13.0% | 51.4% | 98.9% | 89.2% |

三个观察：

1. **W = 1 那一列（decode 的 GEMV）**：即使用满 64 个 warp，计算单元的忙碌比例也只有 13%。这与第 02 章「batch 32 时算力只用了 21%」的结论一致——两者从不同角度看同一件事。
2. **W = 16 时 32 个 warp 就能填满**。这对应 prefill 的 GEMM：每读一份数据做很多次运算。
3. **W = 64 那一列在 8 个 warp 之后不再上升**，因为计算单元已经饱和，多余的 warp 只能排队。这时的瓶颈从访存转到了算力。

### 7.5 结论

decode 阶段实际达到的显存带宽在峰值的 60%-80%，原因是：

- warp 数量不足以掩盖 HBM 延迟（本节）
- M 维度太小，无法使用高效的 tile 策略（第 7.1 节）
- kernel 启动开销在总耗时中占比高（第 2.4 节）

这三条共同构成了第 02 章那个 0.7 的经验系数。它不是随手取的数，而是这几个具体机制的合成结果。

---

## 8. 一步 decode 的完整时间线

把前面各节串起来。Llama-3-8B、batch 32、序列长度 2048、A100。

### 8.1 GPU 侧的 kernel 序列

一层的典型 kernel 序列（已做常见融合）。耗时按各 kernel 的访存量除以有效带宽（2 TB/s × 65%）估算，逐元素 kernel 取启动开销的下界 3 μs：

| # | kernel | 访存量 | 估算耗时 | 占一层 |
|---|---|---|---|---|
| 1 | RMSNorm（融合残差相加） | 1.0 MB | 3.0 μs | 0.5% |
| 2 | QKV 投影（GEMV） | 50.3 MB | 38.7 μs | 7.0% |
| 3 | RoPE（融合 KV cache 写入） | 0.8 MB | 3.0 μs | 0.5% |
| 4 | PagedAttention | 268.4 MB | 206.5 μs | 37.2% |
| 5 | O 投影（GEMV） | 33.6 MB | 25.8 μs | 4.6% |
| 6 | RMSNorm（融合残差） | 1.0 MB | 3.0 μs | 0.5% |
| 7 | MLP gate+up（GEMV） | 234.9 MB | 180.7 μs | 32.6% |
| 8 | SiLU 与逐元素乘（融合） | 5.5 MB | 4.2 μs | 0.8% |
| 9 | MLP down（GEMV） | 117.4 MB | 90.3 μs | 16.3% |
| | **一层合计** | 712.9 MB | **555 μs** | 100% |

```
32 层    : 555 μs × 32   = 17.77 ms
lm_head  : 1.05 GB       =  0.81 ms
采样等    :               =  0.30 ms
合计                      = 18.88 ms
```

与第 02 章用整体访存量算出的 19.61 ms 相差 4%，两条路径互相印证。

**时间分布的三个特征**：

- **attention 一个 kernel 占 37%**。它读的是 KV cache（268 MB），比任何单个权重矩阵都大。序列越长这个比例越高——第 03 章在长度 8192 时测得 KV 占访存量的 69%。
- **MLP 三个 kernel 合计占 50%**。MLP 的参数量本来就占模型的三分之二。
- **逐元素 kernel 各只占 0.5% 左右，但它们存在**。RMSNorm 的实际访存只有 1 MB、0.5 μs 就能读完，耗时却是 3 μs——**这 3 μs 里大部分是 kernel 启动开销**。一层有 4 个这样的 kernel，32 层就是 128 个，合计约 0.4 ms。这是算子融合要消除的部分，也是 CUDA Graph 的价值所在。

### 8.2 CPU 侧与 GPU 侧的重叠

理想情况：

```
GPU:  [ 第 N 步的 32 层 kernel ................. ][ 第 N+1 步 ... ]
CPU:  [ 准备第 N+1 步的输入 ][ 处理第 N-1 步的输出 ][ 空闲 ]
```

CPU 的工作（调度决策、张量构造、detokenize）与 GPU 的执行完全重叠，GPU 不等 CPU。

被打破的情况：

| 打破原因 | 后果 |
|---|---|
| CPU 侧要读采样结果才能决定下一步调度 | 必须等 GPU 算完，重叠失效 |
| 用了同步的 `cudaMemcpy` 而非异步版本 | 同上 |
| Python 侧的工作超过一步的 GPU 时间 | GPU 出现空档 |

vLLM V1 的异步调度就是针对第一条：先按推测准备下一步（假设没有序列结束），拿到结果后再修正。

### 8.3 CUDA Graph 消除了什么

上表的 9 个 kernel × 32 层 = 288 次启动，加上 lm_head 与采样约 300 次。每次 5-10 μs：

```
启动开销 = 300 × 7 μs = 2.1 ms
```

这 2.1 ms 是纯开销，与计算无关。占一步 19.6 ms 的 11%。

CUDA Graph 把这 300 个 kernel 录制为一张图，一次提交即可执行全部，启动开销降到接近 0。

代价是图内的张量形状必须固定。而 batch 每步都在变，因此要按 batch 分档录制多张图（1, 2, 4, 8, ..., 256）。运行时向上取整到最近的档位，多出的位置用 padding 填充。

三个实际影响：

1. 启动时间变长（录制多张图），显存占用增加（每张图有自己的固定缓冲区）
2. 实际 batch 略小于档位时浪费部分计算，例如 batch 33 会被取整到 64，近一半的计算是空转
3. `max_num_seqs` 应当设置在档位上

---

## 9. 用这些知识能解释的现象

| 现象 | 解释 | 相关节 |
|---|---|---|
| decode 达不到理论带宽 | W≈1，64 个 warp 掩盖不了 500 周期的延迟 | 7.3、7.4 |
| batch 小时 GPU 利用率低但 `nvidia-smi` 显示很高 | 利用率只统计「有 kernel 在跑」，不看算力用了多少 | 7.4 |
| batch 1 时增加 batch 几乎不增加耗时 | 访存量不变，且 SM 本来就在等访存 | 7.3 |
| 算子融合有时反而变慢 | 融合后寄存器用量上升，occupancy 下降 | 附录代码第 4 节 |
| block_size 太小会变慢 | 跨块跳跃破坏访存合并 | 6.4 |
| 长 prompt 的 prefill 效率高于短 prompt | M 大才能用高效的 tile 策略 | 4.1、7.1 |
| 模型越小，框架开销占比越高 | kernel 变快而启动开销不变 | 8.3 |
| profile 里逐元素算子耗时不为零 | 每个 kernel 至少有启动开销与一轮访存 | 8.1 |

### 9.1 occupancy 的具体限制

每 SM 能驻留多少 warp，取三个限制的最小值：

| 每线程寄存器 | 每块共享内存 | 寄存器限制 | 共享内存限制 | 实际 warp 数 | occupancy |
|---|---|---|---|---|---|
| 32 | 0 | 64 | 64 | 64 | 100% |
| 64 | 0 | 32 | 64 | 32 | 50% |
| 128 | 0 | 16 | 64 | 16 | 25% |
| 255 | 0 | 8 | 64 | 8 | 12% |
| 64 | 48 KB | 32 | 24 | 24 | 38% |
| 64 | 96 KB | 32 | 8 | 8 | 12% |

（每块 256 线程）

**这是算子融合的隐藏代价**：融合后的 kernel 要在寄存器里保存更多中间结果，每线程寄存器用量上升，occupancy 下降，延迟隐藏能力变差。第 11 章说的「融合后的 kernel 占用的寄存器与共享内存不能超限，否则占用率下降反而变慢」指的就是这张表。

---

## 10. 代码

`code/chA_gpu_model.py` 包含本章全部数值的计算：

1. 内存层次的容量、带宽、延迟，以及延迟换算成的运算次数
2. GEMM 分块的访存量与算术强度，以及共享内存的限制
3. 延迟隐藏所需的 warp 数
4. occupancy 的三个限制
5. 访存合并的效率与 block_size 的影响
6. warp 调度的离散事件模拟

运行：

```bash
python3 code/chA_gpu_model.py
```

建议的实验：

- 第 2 节改变 M（token 数），观察 prefill 与 decode 在 tile 策略上的差别。M = 32（decode）时无论 tile 怎么选，算术强度都上不去。
- 第 6 节把访存延迟从 500 改为 200（假设数据命中 L2），观察所需 warp 数下降。这说明为什么提高 L2 命中率对 decode 有意义。
- 第 4 节调整每线程寄存器数，找到 occupancy 开始下降的临界点。这是判断一个融合方案是否划算的依据。

---

## 11. 这一章的局限

本机无 GPU，全部数值为计算结果。真实值需要在目标硬件上实测：

| 想知道 | 工具 |
|---|---|
| 每个 kernel 的实际耗时与调用次数 | `nsys profile` |
| CPU 与 GPU 是否重叠 | `nsys` 的时间线视图 |
| 单个 kernel 的带宽利用率、occupancy、warp 停顿原因 | `ncu`（Nsight Compute） |
| 访存是否合并 | `ncu` 的 `gld_efficiency` 等指标 |
| 是否有 bank conflict | `ncu` 的 shared memory 指标 |

`ncu` 的输出里有一项 `Warp Stall Reasons`，它直接给出 warp 在等什么（等访存、等指令依赖、等同步）。decode 的 GEMV 上这一项应当显示以 `Long Scoreboard`（等全局内存）为主——这与第 7.3 节的分析对应。若显示的是别的原因，说明分析的方向要调整。

本章的价值在于建立预期：知道该看什么指标、该期待什么数值。拿到 profile 报告后，先与本章的估算对照，差异大的地方就是要查的地方。
